# ============================================================================
# Secure & Accelerated Agent Systems — 全局忽略规则
# 说明：分节组织，便于按技术栈演进时定位与增删。
# ============================================================================

# ---------------------------------------------------------------------------
# 密钥与本地配置（安全红线：任何情况下都不得提交）
# ---------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
credentials.json
secrets.yaml
secrets.yml
.codebuddy/*.local.json
.git/.git-credentials

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.venv/
venv/
ENV/
env/
.python-version
.tox/
.nox/
.coverage
.coverage.*
.coverage.xml
htmlcov/
coverage.xml
.pytest_cache/
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/
.hypothesis/
.ipynb_checkpoints/
prof/
.pytype/

# ---------------------------------------------------------------------------
# Node / 前端（如后续引入 Web 控制台或可视化）
# ---------------------------------------------------------------------------
node_modules/
.pnpm-store/
.yarn/
dist/
build/
.cache/
.parcel-cache/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---------------------------------------------------------------------------
# Rust / C / C++（如后续引入性能关键模块）
# ---------------------------------------------------------------------------
target/
*.rs.bk
*.o
*.obj
*.a
*.lib
*.lo
*.la
*.gch
*.pch
cmake-build-*/
CMakeCache.txt
CMakeFiles/
compile_commands.json

# ---------------------------------------------------------------------------
# 大模型与数据产物（大文件一律走 Git LFS 或对象存储，禁止直接入库）
# ---------------------------------------------------------------------------
*.gguf
*.safetensors
*.ckpt
*.pt
*.pth
*.onnx
*.h5
*.pb
*.tflite
*.mlmodel
*.bin
*.npy
*.npz
*.arrow
*.parquet
*.index
*.faiss
models/
checkpoints/
weights/
data/raw/
data/processed/
datasets/
wandb/
runs/
mlruns/
outputs/
lightning_logs/
*.log

# ---------------------------------------------------------------------------
# 构建 / 运行产物（本项目自定义）
# ---------------------------------------------------------------------------
artifacts/
reports/
# 例外：docs/research/reports/ 是"调研与实验原始数据"的归档位置
# （见 docs/research/README.md 笔记模板第 3 条），属于必须版本化的过程证据，
# 体积小且经人工筛选，因此从上面的 reports/ 规则中豁免。
!docs/research/reports/
!docs/research/reports/**
tmp/
# 删除文件时的回收站目录（由开发环境/IDE 工具生成，不是项目产物）。
# 它若残留在工作区内，会被 ruff 扫到（ruff 默认遵循 .gitignore）或误入提交，
# 从而在 `make check` 中产生与项目无关的报错，故显式忽略。
.Trash-0/
.Trash-*/
.cache/
sandbox/
*.prof
*.trace
*.svg.html

# ---------------------------------------------------------------------------
# 编辑器与 IDE
# ---------------------------------------------------------------------------
.idea/
*.iml
*.ipr
*.iws
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
*.swp
*.swo
*~
.project
.classpath
.settings/
.DS_Store

# ---------------------------------------------------------------------------
# 操作系统
# ---------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini
*~

# ---------------------------------------------------------------------------
# 基准自动化（见 docs/engineering/benchmark-automation.md）
#   - `.bench-data/`：本地演练用的数据根目录，**不入库**（试跑数据没有归档价值）；
#   - `bench/**`：数据分支（bench/data）上的机器产出，**必须入库**——
#     服务端日志与模型产物是性能/能力结论的原始证据，而上面的 `*.log` 与
#     `artifacts/` 规则恰好会把它们排除掉，故在此显式豁免（后面的规则覆盖前面的）。
# ============================================================================
.bench-data/
!bench/
!bench/**

# ---------------------------------------------------------------------------
# Agent 团队运行时（Agent Teams）
#   `.codebuddy/teams/<team>/` 由团队机制在本地生成，含 config.json 与
#   `inboxes/*.json`（成员信箱、会话内容），属于运行产物而非项目资产。
#   2026-09-18 实测：该目录不在既有任何规则覆盖范围内，会被 `git add -A` 误入库
#   （与 devlog 0013 §3 记录的那次误提交同一类风险），故显式忽略。
#   注意：`.codebuddy/agents/` 与 `.codebuddy/rules/` 是必须版本化的项目配置，不在此列。
# ---------------------------------------------------------------------------
.codebuddy/teams/

# ---------------------------------------------------------------------------
# 课程交付包（供下载的一次性归档，**不入库**）
#   `lowspec-交付包-*.zip` 是「需求分析文档 + 源码 + docs」的打包产物，
#   由本机构建后下载提交，属**分发物**而非项目资产；
#   且它含 `.git/`、`docs/` 的副本，入库会造成第二份真源。
#   2026-09-22 新增；其余 `*.zip` 一并忽略（本项目无其它 zip 资产）。
# ---------------------------------------------------------------------------
*.zip

# ---------------------------------------------------------------------------
# 本地截图（**不入库**）
#   所有者把用量/界面截图直接贴到工作目录供分析用，属**临时输入**而非项目资产；
#   文件名形如 `屏幕截图1.png`（中文 + 序号）。显式忽略，避免被误 commit
#   （多会话纪律已禁止 `git add -A`，这里是第二道保险）。
#   若将来确需把某张图作为证据入库，应先重命名到 `docs/` 下的明确路径再 `git add`。
# ---------------------------------------------------------------------------
屏幕截图*.png
