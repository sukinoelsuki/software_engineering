---
description: Git 分支模型、提交规范、PR 与发布流程（始终生效）
alwaysApply: true
enabled: true
updatedAt: 2026-09-14T00:00:00.000Z
provider:
---

# Git 工作流

完整定义见 [`docs/engineering/git-workflow.md`](../../../docs/engineering/git-workflow.md)。

## 分支模型

```text
main        稳定发布线（保护分支，只能通过 PR 合入）
 └─ develop 集成分支（保护分支，下一版本的内容汇聚于此）
     ├─ feat/<issue>-<slug>      新功能
     ├─ fix/<issue>-<slug>       缺陷修复
     ├─ perf/<issue>-<slug>      性能优化
     ├─ security/<issue>-<slug>  安全加固（最高优先级）
     ├─ docs/<slug>              文档
     ├─ exp/<slug>               探索性实验（允许失败，结论必须归档）
     └─ chore/<slug>             杂项（依赖、CI、构建）
```

- 命名：全小写，单词用 `-` 连接，禁止中文与空格；必须带 Issue 编号（`docs`/`chore` 可省略）。
- 发布相关：`release/<version>`、`hotfix/<version>`。
- **禁止**直接向 `main` / `develop` 提交（已由 pre-commit 的 `no-commit-to-branch` 拦截）。

## 提交规范（Conventional Commits，强制）

```text
<type>(<scope>): <subject>

<body>

Refs: #<issue>
```

- `type` ∈ `feat fix docs refactor perf test build ci chore revert security`
- `subject`：祈使句、小写开头、结尾不加句号、≤ 72 字符。
- 破坏性变更：`feat!:` 或 footer 中 `BREAKING CHANGE: <说明>`。
- 一次提交只做一件事；禁止"顺便改了别的"。
- 提交信息正文用中文；type/scope 用英文。

校验方式：`make commit-check`（commitizen），pre-commit 的 `commit-msg` 钩子会自动拦截。

## 合并策略

| 方向 | 策略 | 理由 |
| --- | --- | --- |
| 短期分支 → `develop` | **Squash merge** | 保持线性、可读的历史 |
| `develop` → `main` | **Merge commit** | 保留发布节点，便于回溯 |
| `hotfix/*` → `main` | Merge commit + 回合 `develop` | 保证修复不丢失 |

合并后**立即删除**已合并的源分支。

## 发布流程

1. 从 `develop` 切出 `release/<version>`，只做版本号、CHANGELOG、文档的收敛。
2. 通过 PR 合入 `main`（Merge commit）。
3. 在 `main` 上打**带注释的标签** `v<version>`（SemVer）。
4. 将 `main` 回合至 `develop`，删除 `release/*`。

## Agent 硬性限制

### 远端操作授权（最高优先级）

- ❌ **未经事先明确批准，禁止执行 `git push`**。这是项目所有者的硬性要求。
- ✅ **必须先提交文件内容审查**：推送前把**将要推送的实际文件与变更内容**呈现给所有者，
  不得只给摘要或"已完成"之类的说法。
- ✅ **审查通过后才能推送**：只推送被批准的分支，并说明目标远端、分支名与提交列表。
- ⚠️ 审查未通过时，按意见修改后**重新提交审查**，禁止直接推送修改结果。
- ❌ 禁止 `--force` / `--force-with-lease` 强推、`git reset --hard`、
  `git rebase` 改写已推送历史、删除远端分支。
- ❌ 禁止 `git commit --amend` 已推送的提交。

### 本地操作

- ✅ 本地 `git add` / `git commit` 可在当前任务范围内进行，但需满足：
  在独立短期分支上、提交粒度单一、可回滚。
- ⚠️ **合并到 `main` / `develop` 需事先批准**（即使只在本地）。
- ✅ 提交前必须确认 `make check` 全绿。
- ✅ 每次提交都必须符合 Conventional Commits，否则先修正信息再提交。
- ⚠️ 禁止使用 `--no-verify` 跳过钩子；确需跳过必须有 ADR 记录。
