---
description: Git 分支模型、提交规范、PR 与发布流程（始终生效）
alwaysApply: true
enabled: true
updatedAt: 2026-09-22T00:00:00.000Z
provider:
---

# Git 工作流

完整定义见 [`docs/engineering/git-workflow.md`](../../../docs/engineering/git-workflow.md)。

## 分支模型

```text
main        稳定发布线（保护分支，只能通过 PR 合入）
 └─ develop 工作主干（**可直推**，见 ADR-0013）
     ├─ feat/<issue>-<slug>      新功能
     ├─ fix/<issue>-<slug>       缺陷修复
     ├─ perf/<issue>-<slug>      性能优化
     ├─ security/<issue>-<slug>  安全加固（最高优先级）
     ├─ docs/<slug>              文档
     ├─ exp/<slug>               探索性实验（允许失败，结论必须归档）
     ├─ test/<slug>              跑测专用（**借云原生开发环境，零提交**）
     └─ chore/<slug>             杂项（依赖、CI、构建）
```

- 命名：全小写，单词用 `-` 连接，禁止中文与空格；必须带 Issue 编号（`docs`/`chore` 可省略）。
- 发布相关：`release/<version>`、`hotfix/<version>`。
- ⚠️ **`test/<slug>` 是 test-only 分支：只用于借一台云原生开发环境，绝不允许任何提交**
  （跑测入口：自动 `api_trigger_bench` / 人工 `make bench`，**都走云原生开发桶**；
   唯一持久化出口是 `bench/data` 分支，大文件走制品库）。
  **不许把测试数据推到代码分支**（含 `test/<slug>`、`develop`、`main`）。
  ⚠️ 环境**不会自毁**（2026-09-25 实测：stages 结束不销毁、`keepAliveTimeout` 到期不回收、
  流水线内自毁缺账号级权限）⇒ **跑测结束必须人工关闭**，成本 = `cpus` × 存活时长。
  环境必须从与 `develop` 一致的提交拉起（命中镜像缓存 ⇒ 不额外消耗构建桶）
  ⇒ 推论：**`.ide/Dockerfile` 不要频繁改**（每改一次，所有环境下次拉起都要重建镜像）。
- ⚠️ **例外（长驻：不合回、不得删除）**：`bench/nightly`、`bench/data`
  ——基准数据专用，理由见 [ADR-0014 §2.1](../../../docs/adr/0014-benchmark-automation.md)、
  例外条件见 [ADR-0013 §9](../../../docs/adr/0013-branch-model-for-solo-dev.md)；
  **2026-09-24 起 `bench/data` 只由跑测脚本写**（`make bench` → `scripts/bench/publish.sh`，
  来源分支白名单 `bench/nightly` / `test/*`），**人手不得直接提交**
  （CI 已不再跑测，见 [ADR-0023](../../../docs/adr/0023-ci-downgrade-to-manual-trigger.md)）。
  **这一行不能省**：上面的默认规则是"短期分支合回 `develop`"，
  省略它就会推出相反的错误动作。
- 完整分支矩阵与合并策略以 [`docs/engineering/git-workflow.md`](../../../docs/engineering/git-workflow.md)
  与 [ADR-0013](../../../docs/adr/0013-branch-model-for-solo-dev.md) 为准；
  本摘要只写"**日常会用到 + 省略会误导**"的内容（分工口径见该文 §2）。
- **禁止**直接向 `main` 提交（已由 pre-commit 的 `no-commit-to-branch --branch=main` 拦截）；
  `develop` 是**工作主干、允许直推**
  （[ADR-0013](../../../docs/adr/0013-branch-model-for-solo-dev.md)）。

## 提交规范（Conventional Commits，强制）

```text
<type>(<scope>): <subject>

<body>

Refs: #<issue>
```

- `type` ∈ `feat fix docs refactor perf test build ci chore revert`
  （**`security` 不是 type**：安全类改动写成 `fix(security): …` / `feat(security): …`，
  分支名仍用 `security/<issue>-<slug>`；理由见 `docs/engineering/git-workflow.md` §3 表注）
- `subject`：祈使句、小写开头、结尾不加句号、≤ 72 字符。
- 破坏性变更：`feat!:` 或 footer 中 `BREAKING CHANGE: <说明>`。
- 一次提交只做一件事；禁止"顺便改了别的"。
- 提交信息正文用中文；type/scope 用英文。

校验方式：`make commit-check`（commitizen），pre-commit 的 `commit-msg` 钩子会自动拦截。

## 合并策略

| 方向 | 策略 | 理由 |
| --- | --- | --- |
| 短期分支 → `develop` | **Merge commit**（默认） | devlog / CHANGELOG 会引用提交哈希，squash 会让引用指向不存在的对象 |
| 单提交且无文档引用的分支 → `develop` | 可 Squash merge | 无哈希引用时，线性历史更整洁 |
| `develop` → `main` | **Merge commit** | 保留发布节点，便于回溯 |
| `hotfix/*` → `main` | Merge commit + 回合 `develop` | 保证修复不丢失 |

合并后**立即删除**已合并的源分支。

## 版本与发布

- **版本号只表达"到达了哪个里程碑"，不由提交历史推导**：只能取 `pyproject.toml` 的
  `[tool.lowspec.releases]` 台账里列出的值，由 **`make release VERSION=x.y.z`** 落定。
  **`make bump` 已废除**（会拒绝执行）——用提交历史推导会绕过里程碑判据。
  版本号另有 3 处副本（`[tool.commitizen]` / 包内 `__version__` / `uv.lock`），
  一致性由 `tests/unit/test_release_policy.py` 检查。
- 流程：`make release` → PR `develop`→`main`（Merge commit）→ CI 全绿后合并 →
  在 `main` 上打**带注释标签** `v<version>` → 把 `main` 回合 `develop`。
- **合并到 `main` 恒为所有者动作**（B 类）；**开 PR、打标签、回合 `develop` 可由代理代执行**
  （`ADR-0019` §5.7）；⚠️ 放开的只是"谁执行已获准的发布动作"，**不是**"代理可自行决定发布"。
- `release/<version>` 分支**不再强制**（单人项目里只多一次往返；确需冻结发布候选时仍可用）。
- 理由见 [ADR-0019](../../../docs/adr/0019-release-and-version-policy.md)。

## Agent 硬性限制

### 远端操作授权（**按可回退性分级**，最高优先级）

**A 与 B~F 的界线必须记住**（完整定义与判据见
[`docs/engineering/git-workflow.md`](../../../docs/engineering/git-workflow.md) §4）：

| 类 | 范围 | 规则 |
| --- | --- | --- |
| **A** | `develop` / 短期分支上的**代码·文档·测试·配置** | **推送后立即报告** |
| **B** | `main` | **事先批准** |
| **C** | **凭据入库**（`*.env` / `*.key` / token / 证书） | **事先拦截 + 禁止** |
| **D** | **强推 / 改写已推送历史 / 删远端分支** | **禁止** |
| **E** | `bench/data` | **只由跑测脚本写**（`make bench`），**人手不得直接提交**；禁止把测试数据推到任何代码分支 |
| **F** | **新增依赖 / 改 CI 门禁强度 / 规则本身** | **事先确认** |

- ⚠️ **不得把 A 类做法外推到 B~F**：这是一条**分级**规则，不是"取消远端审查"。
  省略分级会让默认推导落到"任何远端写入都需事先批准"或反向的"都用不着批准"，两者都是错的。
- ⚠️ **A 类不是"免审"**：`make check` 全绿是推送前必要条件；**事后报告不得替代门禁**。
  A 类报告含四项：**分支名 / 提交列表（`A..B`）/ 文件清单 / 验证方式**，
  落 `docs/devlog/` 当前篇（**由记录员落盘**，见 `CODEBUDDY.md` §10.1）；
  **报告与仓库不一致即视为未报告**。
- ⚠️ **本分级不可由 CI 强制**（CI 看不到"是否事先批准 / 是否事后报告"）⇒ 唯一护栏是
  事后可核对 ⇒ 按威胁模型口径只能记为**部分缓解**，**不得**表述为机制。
- ❌ 禁止 `--force` / `--force-with-lease` 强推、`git reset --hard`、
  `git rebase` 改写已推送历史、删除远端分支。
- ❌ 禁止 `git commit --amend` 已推送的提交。

### 本地操作

- ✅ 本地 `git add` / `git commit` 可在当前任务范围内进行，需满足：提交粒度单一、可回滚。
- ✅ **`develop` 是工作主干，允许直推、无需事先批准**（仍需 Conventional Commits、
  pre-commit 与 `make check` 全绿）；**`main` 受保护，写入需事先批准**
  （[ADR-0013](../../../docs/adr/0013-branch-model-for-solo-dev.md)）。
- ⚠️ 短期分支只用于两类场景：`exp/*` 隔离实验、需要评审的大改动；
  且**必须在同一次会话内合回 `develop`**。
- ✅ 提交前必须确认 `make check` 全绿。
- ✅ 每次提交都必须符合 Conventional Commits，否则先修正信息再提交。
- ⚠️ 禁止使用 `--no-verify` 跳过钩子；确需跳过必须有 ADR 记录。

### 多会话并发（**提交 / 推送范围隔离**）

> 同一工作区可能**同时跑着多个会话**（多个对话）。它们之间**没有共享领导、没有信箱、
> 不共享上下文** ⇒ 不能靠"协调"解决，只能靠**缩小自己的范围 + 逐笔核对**。
> 完整定义见 [`docs/engineering/git-workflow.md`](../../../docs/engineering/git-workflow.md) §6；
> 决策见 [ADR-0022](../../../docs/adr/0022-multi-session-concurrency-discipline.md)。
> **省略本节的后果**：默认叙述会推到"直接 `git add -A` 提交"这一**相反且有害**的动作。

- ✅ **提交一律点名路径**：`git commit -o -m "<msg>" -- <本会话的显式路径>`（`--only`）。
  新文件须**先** `git add <该文件>`——`-o` 对**未跟踪**路径直接报错。
- ❌ **禁止整仓暂存**：`git add -A` / `git add .` / `git add -u` / `git commit -a` /
  **不带路径且不带 `-o` 的 `git commit`**——按整个工作树取内容，会暂存**他人未提交**的改动。
- ❌ **禁止波及他人的工作树清理**：`git stash`（**不带路径**）/ `git checkout -- .` /
  `git restore .` / `git clean -fd`；需要暂存时用 `git stash push -- <自己的路径>`。
- ✅ **推送前先核对归属**：`git fetch` → `git log --format='%h %s' @{u}..HEAD`。
  `push` 推的是**整条分支**，会带走**同一分支上别的会话尚未推送的提交**；
  A 类报告须覆盖"**实际推送的全部提交**"，非本会话的**单独标注**（§6.4）。
- ❌ **推送被拒时禁止 `--force` / `--force-with-lease`**（D 类）：唯一合法路径是
  `fetch` → 对**尚未推送**的本地提交 `pull --rebase`（或 merge）→ 再 `push`。
- ⚠️ **不与另一会话并发提交**：`.git/COMMIT_EDITMSG` 是**单例**，并发提交会互相覆盖提交信息。
- ⚠️ **冲突不自动合并**：同一文件被两个会话改过 ⇒ **停下、报告**，由所有者裁决取舍。
- ⚠️ **边界**：以上是**流程纪律**，**不可由 CI 强制**（CI 看不到"是否存在多个会话"）
  ⇒ 记为**部分缓解**，**不得**表述为机制。
