---
description: 代码质量规范：风格、类型、错误处理、日志、性能与依赖（编写或重构代码时加载）
alwaysApply: false
enabled: true
updatedAt: 2026-09-14T00:00:00.000Z
provider:
---

# 代码质量规范

## 风格

- Python 风格由工具决定，禁止手工争辩：`ruff format`（line-length 100）+ `ruff check --fix`。
- 命名：模块/函数/变量 `snake_case`，类 `PascalCase`，常量 `UPPER_SNAKE_CASE`。
- 禁止 `from x import *`；禁止未使用的导入与变量。
- 单函数建议 ≤ 50 行；嵌套深度 ≤ 3；超出时拆分或改为早返回。

## 类型

- 所有公开函数/方法必须有参数与返回类型标注；`mypy --strict` 必须通过。
- 禁止用 `Any` 逃避类型问题；确需时必须有 `# type: ignore[code]` 并写明原因。
- 优先使用 `Protocol` 而非继承，便于隔离与测试替身。

## 错误处理

- 禁止裸 `except:`；捕获具体异常类型。
- 禁止静默吞异常（`except X: pass`）；必须处理、记录或显式重抛（附上下文）。
- 自定义异常需有清晰层级与语义；错误信息不得包含密钥、路径或内部实现细节（对外暴露时）。
- 校验类逻辑遵循 **fail-secure**：不确定时拒绝，而非放行。

## 日志

- `src/` 中禁止 `print`；使用结构化日志（`logging` 或项目统一封装）。
- 日志级别语义：`DEBUG` 诊断 / `INFO` 状态变更 / `WARNING` 可恢复异常 /
  `ERROR` 操作失败 / `CRITICAL` 系统不可用。
- 日志中**禁止**出现密钥、令牌、个人数据、完整请求体（除非已脱敏）。

## 性能

- 以"优化"为名的改动必须遵循：**基准 → 改动 → 对比数据**。
- 必须给出：环境、复现命令、指标（P50/P95/P99、吞吐、内存）、重复次数与方差。
- 关注算法复杂度、不必要的拷贝、重复 I/O、锁粒度、缓存失效策略。
- 禁止过早优化；先测量，再优化，并记录测量方法。
- 优化不得削弱安全性；存在权衡时必须显式说明并交由人类决策。

## 依赖与配置

- 只使用标准库与 `pyproject.toml` 已声明的依赖。
- 新增依赖的决策流程：用途 → 至少一个替代方案对比 → 体积/许可证/安全影响 → 获确认。
- 配置项必须有默认值、类型校验与文档；敏感配置从环境注入，禁止硬编码默认密钥。

## 测试替身

- 优先面向接口编程，使测试可使用 fake/stub 替代真实外部依赖。
- 禁止为了让测试通过而在生产代码中加"测试专用分支"（除非有明确设计并写入 ADR）。
