---
description: 测试策略与用例编写规范：分层、命名、安全与性能测试（编写或评审测试时加载）
alwaysApply: false
enabled: true
updatedAt: 2026-09-14T00:00:00.000Z
provider:
---

# 测试规范

完整策略见 [`docs/engineering/testing-strategy.md`](../../../docs/engineering/testing-strategy.md)。

## 分层与目录

```text
tests/
├── unit/         单元测试：纯逻辑，无外部依赖，快速（毫秒级）
├── integration/  集成测试：跨模块、真实依赖或高保真替身
├── security/     安全与对抗性测试：攻击场景复现
└── benchmark/    性能基准：默认不参与快速回归
```

标记（pytest markers，已在 `pyproject.toml` 注册）：
`unit` / `integration` / `security` / `benchmark` / `slow`。

## 用例编写规则

- 命名必须描述**行为与预期**，而不是被测函数名：
  - ✅ `test_policy_denies_tool_when_capability_missing`
  - ❌ `test_policy_1`
- 一个用例只验证一个行为；断言必须具体，禁止只断言"不抛异常"。
- 必须覆盖：正常路径、边界值、非法输入、失败路径、并发/时序敏感场景。
- 每个 bug 修复必须附带**先失败后通过**的回归用例。
- 测试必须**确定性**：禁止依赖时间、随机数、网络与执行顺序；必要时注入时钟与随机源。
- 禁止在测试中 sleep 等异步；使用事件/条件同步。
- 测试数据必须为合成或脱敏数据；真实凭据一律禁止。

## 安全测试（必做）

- 安全相关功能必须附带**攻击者视角**用例：
  - 输入注入（提示注入、命令注入、路径穿越、模板注入）
  - 越权（缺失能力/权限时应被拒绝）
  - 逃逸与资源耗尽尝试
  - 不可信内容试图改变控制流或权限决策
- 用例名需体现安全意图；断言须验证**拒绝行为与审计记录**同时成立。
- 涉及提示注入的语料集应可复用、可扩充，纳入版本管理。

## 性能基准

- 基准必须可复现：固定硬件描述、固定输入规模、固定随机种子、预热轮次。
- 输出必须包含 P50/P95/P99、吞吐与内存峰值，并给出多次运行的方差。
- 基准结果以结构化文件归档（如 `reports/bench/`），并附运行命令与提交哈希。
- 基准不进入默认快速回归（`make test` 排除 `benchmark` 标记）。

## 覆盖率

- 目标是**关键路径全覆盖**，而非追求数字。安全与策略相关代码要求接近 100%。
- 新增代码必须自带测试；覆盖率不允许出现下降（CI 中检查）。
- 禁止用无意义断言刷覆盖率。

## 运行方式

```bash
make test            # 快速回归（排除 benchmark）
make test-cov        # 含覆盖率
make test-security   # 仅安全测试
uv run pytest -m benchmark   # 性能基准
```
