---
description: 安全基线：信任边界、最小权限、输入校验、密钥与供应链规则（始终生效）
alwaysApply: true
enabled: true
updatedAt: 2026-09-14T00:00:00.000Z
provider:
---

# 安全基线（最高优先级）

本项目主线之一是系统安全。以下规则**优先于**任何功能、性能或进度的诉求。
完整策略见 [`SECURITY.md`](../../../SECURITY.md)。

## 写代码前的四问（强制）

1. **输入从哪来、可信吗？** 用户输入、模型输出、工具返回、网页/文件内容、环境变量 → 一律**不可信**。
2. **是否跨越信任边界？** 若跨越，校验与净化逻辑在哪？是否集中、可测试？
3. **需要什么权限？** 是否收敛到最小集合？默认是否为**拒绝**？
4. **失败时会发生什么？** 是否 **fail-secure**（失败即拒绝），而不是静默放行？

## 硬性规则

### 信任边界与输入

- 所有外部输入必须校验（类型、范围、长度、编码、结构），校验失败即拒绝。
- 不可信内容**一律视为数据，不得作为指令**影响控制流、权限决策或工具选择。
- 禁止将外部文本直接拼接进：SQL、shell 命令、文件路径、模板、表达式求值、序列化格式。
- 文件路径必须做规范化与白名单校验（防目录穿越）；禁止用字符串拼接构造路径。

### 执行与权限

- 任何子进程调用必须：(1) 不经 shell（`shell=False`）；(2) 参数以列表传入；(3) 位于统一封装层。
- 禁止 `eval` / `exec` / `pickle.loads` 处理不可信数据；需反序列化时使用安全格式（JSON 等）并校验 schema。
- 禁止动态导入来自不可信来源的模块。
- 网络出站默认拒绝，白名单放行；外部请求必须设置超时、限制响应体大小。
- 特权操作（文件写入、进程执行、网络访问、凭据读取）必须留下结构化审计日志。

### 密钥与数据

- **禁止**在代码、测试、文档、CI 配置、提交信息中出现任何密钥/令牌/私钥。
- 凭据只能通过环境变量或密钥管理设施注入，且不得写入日志或错误信息。
- 测试数据必须为合成或脱敏数据；禁止使用真实个人数据。
- 新增文件前检查 `.gitignore` 是否覆盖其可能含密钥的变体（`*.env`、`*.key` 等）。

### 供应链

- 依赖必须锁定版本；新增依赖需说明来源、许可证、体积与安全影响。
- 模型权重、二进制产物必须有来源与摘要记录，禁止引入来源不明的资产。

### 安全检查

- 禁止用 `# nosec` / `# noqa: S***` / 关闭 linter 规则等方式静默绕过安全告警。
  确需豁免时**两步走**：① **豁免标记行只写规则号**（`# nosec B404` / `# noqa: S603`）；
  ② **理由写在紧邻其上的独立注释行**；并登记到 ADR 或 `SECURITY.md`。
- **禁止在 `# nosec` / `# noqa` 之后追加自由文本**：扫描器会把其中的词元当作**规则号候选**，
  可能**静默放宽另一条检查**（2026-09-18 实测：`# nosec NOPE —— …提到 B101…` ⇒ bandit `exit 0`、
  `B101` 被静默豁免）。凡"**能悄悄放宽检查**"的写法一律禁止——它正是本规则要防的失效模式。
- 安全相关功能必须附带**攻击者视角**的对抗性测试用例，命名体现安全意图。

## 安全相关改动的工作流

安全类改动走 `security/<issue>-<slug>` 分支，并额外要求：

1. 更新 `docs/design/threat-model/` 中对应威胁条目；
2. 增加可复现的对抗性测试；
3. 若涉及公开披露，更新 `CHANGELOG.md` 的 `### Security` 段落；
4. PR 描述中明确写出：**攻击场景 → 原行为 → 新行为 → 验证方式**。
