---
description: 项目总体约定：定位、语言、目录、依赖与变更最小化原则（始终生效）
alwaysApply: true
enabled: true
updatedAt: 2026-09-14T00:00:00.000Z
provider:
---

# 项目总体约定

## 项目定位

- 华中科技大学 计算机学院 软件工程综合实验，**单人独立开发**。
- 方向：**原生智能系统**，主线为**系统安全** + **模型/系统加速与优化**。
- 在既有开源前沿项目（base project）之上做增量研发，交付**完整、规范、便捷、安全、高效**的软件。

## 语言约定

- **代码、注释、标识符、分支名、提交 type/scope**：英文。
- **文档、Issue、PR、提交信息正文**：中文（技术术语保留英文原词）。
- 面向用户的界面文案：中文；代码内字符串默认英文。

## 目录约定

| 路径 | 用途 |
| --- | --- |
| `src/` | 生产代码，禁止放入实验脚本 |
| `tests/` | 测试，按 `unit/` `integration/` `security/` `benchmark/` 分类 |
| `scripts/` | 开发与运维脚本 |
| `docs/adr/` | 架构决策记录 |
| `docs/design/` | 设计与威胁模型 |
| `docs/engineering/` | 工程流程规范 |
| `docs/proposals/` | 选型与立项提案 |
| `docs/requirements/` | 需求规格 |
| `docs/research/` | 前沿调研与实验结论 |
| `docs/devlog/` | **开发日志**（按阶段分篇，见下） |
| `.cnb/` | CNB 平台协作配置 |

**新目录的建立必须先在 ADR 中说明理由。**

## 开发日志（强制）

- 每次提交后，必须在 `docs/devlog/` 的**当前篇**追加一条变更记录
  （含提交哈希、变更内容、验证方式）。
- **分篇规则**：以**议题/阶段的完整性**为主判据（讲完即收篇），
  硬上限为 **300 行** 或 **5 个工作日**，达到即新开一篇并在 `docs/devlog/README.md` 索引中登记。
  **禁止把日志堆积在同一个文件里**；同样禁止为卡行数把连贯议题硬切两半。
- 日志必须**当时写**，禁止事后大规模补写——那样会失真，失去日志的价值。
- 日志与 ADR 分工：ADR 记录**决策**（只增不改），日志记录**过程**（按时间演进）。

## 变更最小化原则（重要）

- 优先**编辑既有文件**，而非新建文件。
- 禁止顺手做与当前任务无关的重构；如确有必要，拆成独立提交。
- 禁止大范围重写既有文件（除非任务明确要求并已确认）。
- 修改公开接口、目录结构、CI 门禁强度前，必须获得人类确认。

## 规则变更的联动更新（强制）

**任何规则/决策一经修改，必须回头把所有被它覆盖的旧约束一并更新**——
不能只改"新写的那一处"，否则旧表述会继续以"现行规则"的身份误导后续判断。

| 变更类型 | 必须同步的位置 |
| --- | --- |
| 流程 / 技术决策 | `docs/adr/`（决策）+ `docs/engineering/`（完整定义）+ `.codebuddy/rules/`（摘要）+ `CODEBUDDY.md` / `AGENTS.md`（常驻结论） |
| 门禁强度 | `Makefile`、`.pre-commit-config.yaml`、`.cnb.yml` 三处口径必须一致 |
| 协作机制 | `.codebuddy/agents/` + `docs/engineering/agent-teams.md` + `CODEBUDDY.md` / `AGENTS.md` §10 |

**落地动作（改动后必做一次"反向搜索"）**：用变更涉及的**旧术语、旧字段名、旧分支名、
旧命令**在仓库内搜索，确认没有残留的旧表述。搜索范围要包含 `.codebuddy/`、
`docs/engineering/`、`docs/adr/`、`CONTRIBUTING.md`、`README.md`。

**例外（不要改）**：`docs/devlog/`、`docs/research/` 与 ADR 中**已接受决策的历史快照**
不改——它们记录"当时发生了什么、当时怎么想"，按时间演进，事后修改即失真。
若与现状冲突，在**新一篇**（或新的 ADR）里说明变更即可。

> 实例（2026-09-18）：`develop` 于 09-16 由 [ADR-0013](../../../docs/adr/0013-branch-model-for-solo-dev.md)
> 改为工作主干，但 `.codebuddy/rules/git-workflow/RULE.mdc` 与 `CODEBUDDY.md` §2
> 直到 09-18 才被发现仍写着"禁止直接提交 develop / 合并到 develop 需事先批准"。
> ADR-0013 §7 本就有"三处表述一致，无矛盾"的验证条目——这条验证**当时并未真正执行**。

## 依赖管理

- 只使用标准库与 `pyproject.toml` 中已声明的依赖。
- **新增依赖前必须**：说明用途 → 对比至少一个替代方案 → 说明体积/许可证/安全影响 → 获得确认。
- 依赖必须锁定版本，并记录在锁文件中。

## 技术栈现状

- 当前为**工程基线**阶段：Python ≥ 3.12 + `uv` + `make` + `ruff` + `mypy` + `pytest`。
- base project 选型冻结后，技术栈可能调整；任何调整都需 ADR 记录。
