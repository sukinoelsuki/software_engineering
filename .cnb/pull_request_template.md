# Pull Request

> 本模板对应 [`CONTRIBUTING.md`](CONTRIBUTING.md) 与 [`docs/engineering/definition-of-done.md`](docs/engineering/definition-of-done.md)。
> **未完整填写的 PR 不予合并。** 单人项目中，"评审"环节由自评审 + CI 门禁共同承担。

## 1. 基本信息

| 项 | 内容 |
| --- | --- |
| 关联 Issue | Closes # / Refs # |
| 变更类型 | feat / fix / perf / security / docs / refactor / test / chore |
| 影响范围 | 模块 / 组件 |
| 是否破坏性变更 | 是 / 否 |
| 是否包含安全相关改动 | 是 / 否 |

## 2. 变更动机

<!-- 为什么要做这个改动？解决什么问题？不做会怎样？ -->

## 3. 变更内容

<!-- 逐条列出实际改动。请聚焦"改了什么行为"，而非"改了哪个文件"。 -->

1.

## 4. 方案与取舍

<!-- 简述所选方案；如果存在被否决的替代方案，说明为什么否决。 -->

## 5. 验收标准对照

<!-- 逐条对照 Issue 中的验收标准，给出对应结论（已完成 / 已验证 / 不在本次范围）。 -->

| 验收标准 | 结论 | 证据（测试名 / 命令 / 数据） |
| --- | --- | --- |
|  |  |  |

## 6. 验证方式

```bash
# 请粘贴实际执行过的命令与关键输出
make check
```

- [ ] `make check` 全绿（format-check + lint + typecheck + test + security）
- [ ] 新增/修改的逻辑均有对应测试
- [ ] 已覆盖异常路径与边界条件

## 7. 安全影响评估

> 本项目安全优先。若本 PR 涉及外部输入、权限、文件/网络/进程操作，**必须**逐项回答。

- 是否引入新的**信任边界**？如果有，位置与校验方式：
- 是否引入新的**外部输入**？如果有，校验规则：
- 是否改变**权限模型**或新增特权操作：
- 是否新增**依赖 / 模型权重 / 二进制**？来源、版本与许可证：
- 失败时的行为是否为 **fail-secure**：
- 是否新增**对抗性测试用例**：

## 8. 性能影响评估

> 若本 PR 涉及性能敏感路径，**必须**填写；否则写"不涉及"。

| 指标 | 基线 | 变更后 | 变化 | 测量方法 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

- 测量环境与命令：
- 重复次数与方差：

## 9. 文档与留痕

- [ ] 已更新 `CHANGELOG.md` 的 `[Unreleased]`
- [ ] 影响架构/选型/依赖/流程的改动，已附 ADR（编号：）
- [ ] 涉及设计的改动，已更新 `docs/design/`
- [ ] 涉及安全的改动，已更新 `docs/design/threat-model/`
- [ ] 流程类改动，已更新 `docs/engineering/`
- [ ] 文档改动已同步 `README.md` 相关章节

## 10. 自评审清单

- [ ] 假设这是别人写的代码，我能否在 5 分钟内理解其意图？
- [ ] 变更是最小化的吗？有没有夹带与本次任务无关的改动？
- [ ] 有没有新增的密钥、令牌、真实数据、大体积文件？（应为无）
- [ ] 有没有用 `# noqa` / `no-verify` / 跳过检查来"让 CI 变绿"？（若有，是否已记录理由）
- [ ] 提交信息符合 Conventional Commits 吗？
- [ ] 回滚方式是否明确？（如何 revert、是否有数据/配置迁移）

## 11. 风险与回滚

<!-- 已知风险、未解决问题、后续跟进事项（应关联新 Issue） -->
