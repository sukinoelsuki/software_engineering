# 0025. 环境内 Agent 命令默认自动运行与启动期设置落地

- 时间范围：2026-09-25 ~
- 状态：**进行中**（§7 即当前活待办）
- 关联：决策 [ADR-0032](../adr/0032-agent-command-gate-and-ide-settings.md)（已接受，F 类授权放宽主 Agent 命令闸门）；
  机制与故障判据 [`engineering/agent-command-gate.md`](../engineering/agent-command-gate.md)；
  威胁模型 [T-15](../design/threat-model/untrusted-input-and-agentic.md)（部分缓解，联动 T-05）；
  平台事实资产 [`research/2026-09-25-cnb-platform-behavior-facts.md`](../research/2026-09-25-cnb-platform-behavior-facts.md) §9 / V7；
  参考实现 `compute-matrix` 的 `apply-workspace-settings`（同平台，本组织不可见，引用以本仓库 `.cnb.yml` 既有注释为准）
- 提交（3 笔）：`ec8791d`、`43a57a2`、`8686272`（**本篇自身哈希由下一篇回填**）
- **承接**：本篇为独立议题（Agent 命令闸门放宽 + IDE 设置启动期落地），与 0024 的
  "发布路径验收 / 两处静默坏数据修复"无关，故**新开一篇**而非追加 0024；0024 的未完成项
  仍留其 §7 作为历史，不在此搬运（议题不混写）。

---

## 1. 本阶段目标

把主 Agent 的命令执行闸门从"逐类逐次弹确认"放宽为"**默认全自动、少数不可逆操作保留确认**"，
并让这套偏好在**本环境真正生效**——而非只是一份写在仓库里、运行时被平台覆盖掉的 `settings.json`。

附带两项配套：① 把 CNB"同时存在容器数上限"的核实结论记入平台事实资产（官方未公布）；
② 由架构师落盘 ADR-0032、机制文档与威胁模型 T-15，使决策、机制、残余风险三者可追。

## 2. 关键决策与理由

### 2.1 真正的难点不是"配什么"，而是"配了不生效"

第一版实现（把 `codingcopilot.*` 写进仓库设置文件）**看起来完全正确，运行时却照样弹确认**。
根因三层（详见 ADR-0032 §1.2，均已自证）：

1. **`scope = application`**：`codingcopilot.autoRun` / `safeDeleteEnabled` / `safeDeleteBulkThreshold` /
   `customBlacklistCommands` / `disabledSecurityCategories` / `autoAcceptWebSearch` 六键的 scope 均为 application
   （`autoRunMode` 未声明，见 ADR-0032 §5.3 `N-2`）⇒ **只能写在 User 级**；
2. 仓库级 `.vscode/settings.json` 与镜像 Machine 级那份对该六键**都不生效**；
3. **平台在环境启动时用自己的一份覆盖 `User/settings.json`** ⇒ 构建期 COPY 进 User 级的键**会被抹掉**。

⇒ 唯一可靠时机是**环境启动之后**、由平台事件钩子把设置**合并**进**运行中的** User 文件。
`.ide/settings.json` 单独存在不会自动生效——这正是"配置是对的、运行时不生效"的直接根因。

### 2.2 决策：默认全自动 + 红线黑名单提醒 + 启动期 stage 保证落地

- **默认全自动**：`autoRun=true`、`autoRunMode="runEverything"`；
- **红线改为黑名单提醒**：禁用 12 个内置安全类别（只留 `custom`），并设 33 条黑名单正则
  （不可逆操作 + 项目 B/D/E 类红线，按 `git-workflow.md` §4 可回退性判据取材）；
- **防删库回收站阈值收紧到 200**：`safeDeleteEnabled=true`、`safeDeleteBulkThreshold=200`；
- **启动期落地**：新增 `.cnb.yml` 的 `vscode` 事件首段 stage `agent-permissions`，
  环境启动后由 `scripts/apply_ide_settings.py` 把设置**合并**进运行中的 User 设置并**断言**生效
  （含"设置文件与启动期 stage 成对存在"不变式）。

### 2.3 并发上限：记入平台事实，但"撞上限表现"未验证

`43a57a2` 把"同时存在容器数上限"的核实结论写入平台事实资产 §9 与 V7：
官方文档与 Open API **均未公布**该上限；**实测下界 = 4**（参考实现 `compute-matrix`）；
所有者观察约 6（跨仓库、按人/组织，**未实测**）；撞上限的具体表现**未验证**，仅附探针配方。

## 3. 变更记录

**`ec8791d` · Agent 命令默认自动运行，红线命令改为黑名单提醒**

把 `codingcopilot.*` 的偏好从"逐次弹确认"改成"默认全自动 + 黑名单提醒"：`.ide/settings.json` 设
`autoRun=true`、`autoRunMode="runEverything"`、`safeDeleteEnabled=true`、`safeDeleteBulkThreshold=200`、
禁用 12 个内置安全类别（只留 `custom`）、33 条黑名单正则（不可逆操作 + B/D/E 类红线）。
配套的落地机制：新增 `scripts/apply_ide_settings.py`（把设置合并进运行中的 User 设置并断言），
`Makefile` 加 `apply-ide-settings`，`.cnb.yml` 的 `vscode` 事件首段 stage `agent-permissions` 在环境启动后执行它；
`.ide/Dockerfile` 的 COPY 注释补适用范围说明；`tests/unit/test_apply_ide_settings.py` 17 条用例
（含"设置文件与启动期 stage 成对存在"不变式）。`.cnb.yml` 同时补 25 行相关配置。

**`43a57a2` · 记录 CNB 并发上限核实结论（官方未公布）**

平台事实资产 `research/2026-09-25-cnb-platform-behavior-facts.md` 新增 §9 与 V7：
同时存在容器数上限不在官方文档与 Open API 中；实测下界 = 4（参考实现 `compute-matrix`）；
所有者观察 ≈ 6（跨仓库、按人/组织，未实测）；撞上限表现未验证；附探针配方。

**`8686272` · 架构师落盘 ADR-0032 与机制/威胁文档**

决策 [ADR-0032](../adr/0032-agent-command-gate-and-ide-settings.md)（**已接受**，F 类授权放宽主 Agent 命令闸门）、
机制与故障判据 [`engineering/agent-command-gate.md`](../engineering/agent-command-gate.md)、
威胁模型新增 **T-15（部分缓解）** 并联动 T-05；`docs/adr/README.md`、`docs/design/README.md`、
威胁模型 README 与 `untrusted-input-and-agentic.md` 同步。

**验证方式（三笔共用）**

- `uv run ruff check .` / `ruff format --check` 全绿；
- `uv run pytest tests/unit` **889 passed**；
- 黑名单**双向实测**：41 命中 / 25 日常放行；
- 合并脚本**幂等**：两次执行后 87 键不变、平台 16 键保留。

⚠️ **未端到端验证（必须如实标注）**：运行中会话的扩展日志**仍显示旧值**——
安全检查读的是面板会话态 `chatModeSettings`，需面板/页面**重载**或**新环境**才重新初始化。
⇒ 本项**只验证到"脚本合并 + 断言通过"，未验证"运行中的会话确实用了新偏好"**；
对应 §7 第①②项（下次拉起环境确认 stage 输出与运行时加载）。

**A 类推送报告（推送已发生）**

- 分支：`develop`
- 提交：`ec8791d`、`43a57a2`、`8686272`
- 文件清单（13 个，`d30e31c..8686272`）：
  `.cnb.yml`、`Makefile`、`.ide/Dockerfile`、`.ide/settings.json`、
  `scripts/apply_ide_settings.py`、`tests/unit/test_apply_ide_settings.py`、
  `docs/adr/0032-agent-command-gate-and-ide-settings.md`、`docs/adr/README.md`、
  `docs/design/README.md`、`docs/design/threat-model/README.md`、
  `docs/design/threat-model/untrusted-input-and-agentic.md`、
  `docs/engineering/agent-command-gate.md`、
  `docs/research/2026-09-25-cnb-platform-behavior-facts.md`
- 验证方式：同上方"三笔共用"验证（ruff 绿、889 passed、黑名单双向实测、脚本幂等）。

## 4. 数据与证据

| 核对项 | 判据 | 实测 |
| --- | --- | --- |
| 格式 / lint | `ruff check` / `ruff format --check` | ✅ 全绿 |
| 单测 | `uv run pytest tests/unit` | ✅ **889 passed** |
| 黑名单命中 | 不可逆/红线命令 | ✅ **41 命中** |
| 黑名单放行 | 日常可回退命令 | ✅ **25 放行** |
| 合并脚本幂等 | 两次执行键集合 | ✅ **87 键不变、平台 16 键保留** |
| 运行会话已用新偏好 | 扩展日志读 `chatModeSettings` | ⚠️ **未验证**（见 §3 / §5 第 1 条） |

## 5. 问题与卡点

1. **运行会话是否真正用了新偏好——未端到端验证**：脚本合并与断言通过，但运行中会话的
   扩展日志仍显示旧值（安全检查读面板会话态 `chatModeSettings`，需重载或新环境才重初始化）。
   这是本仓库反复强调的"**改了文件 ≠ 设置生效**"的又一直观实例（与 0024 §5 第 7 条同源）。
2. **并发上限"撞上限表现"未验证**：§9/V7 只确认了下界 = 4、所有者观察 ≈ 6，但撞上限后
   平台是排队、拒绝还是报错均未实测，仅留探针配方（§7 第③项）。
3. **参考实现 `compute-matrix` 不可直接读取**：ADR-0032 对其 `apply-workspace-settings` 与
   `docs/platform-facts.md` 的引用，一律来自本次任务口径与本仓库 `.cnb.yml` 既有注释，
   **不声称已逐字核对原文**（ADR-0032 §10 `U1`）。

## 6. 学到了什么

1. **"配了不生效"比"配错"更隐蔽**：`scope=application` 的键只能落在 User 级，而平台启动时
   会覆盖 User 设置 ⇒ 把偏好写进仓库/镜像这份"看似正确"的配置**根本不生效**。判断设置类缺陷
   的第一问应是「**这份配置是从哪里被读出来的**」（与 0024 §6 第 8 条同源）。
2. **"改了文件 ≠ 设置生效"是结构性事实，不是偶然**：平台对 User 设置的覆盖、扩展对会话态
   的缓存，两层都会让"文件已改"与"运行时已用"脱节 ⇒ 验证时必须去读**运行中的会话**，而非只检查文件。
3. **黑名单优于逐次确认**：真正不可逆的操作不靠"点确认"防住（点击者常不知在点什么），
   把范围约束写成**纪律 + 黑名单提醒**，既保效率又保留不可逆操作的可视性。

## 7. 下一步（= 当前活待办清单）

> ⚠️ **新会话从这里开始读**。本篇为独立议题（Agent 命令闸门放宽 + 启动期设置落地），
> 与 0024 的发布路径/静默坏数据议题无关；0024 的未完成项留其 §7 作为历史，不在此搬运。

- [ ] **① 下次拉起环境确认 stage `agent-permissions` 输出与运行时加载**（12 类已禁用 / 33 条黑名单已生效）；
      这是 §3「未端到端验证」项的闭环——确认运行中的会话**确实**用了新偏好（而非只脚本合并通过）。
- [ ] **② 人工触发一次黑名单命中探针**（如 `git push --dry-run origin main`），确认命中时给出提醒而非直接执行。
- [ ] **③ V7 并发上限探针**（平台事实资产 §9.3 配方）：实测撞上限时的真实表现（排队/拒绝/报错）。
- [ ] **④ T-15 保持「部分缓解」，绕过面探针未做**：ADR-0032 / 威胁模型的残余风险仍标记为部分缓解，待补绕过面探针。
- [ ] **⑤ 决策待办：是否把 `.ide/settings.json` 从 Dockerfile COPY 与 `build.by` 移除**（参考实现已移除；
      移除后改设置不再触发镜像重建——当前 COPY 仍有意义但会随设置改动重建镜像，需所有者裁决）。
