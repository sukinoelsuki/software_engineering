# 0006. 开发环境搭建与技术栈定案

- 时间范围：2026-09-15
- 状态：**已完成（待首次构建验证）**
- 关联：[ADR-0005](../adr/0005-single-primary-language.md)、
  [`.ide/Dockerfile`](../../.ide/Dockerfile)、[`.ide/settings.json`](../../.ide/settings.json)、
  [`.cnb.yml`](../../.cnb.yml)
- 提交：（本次记录）

---

## 1. 本阶段目标

1. 回答项目所有者关于技术栈的疑问（是否需要 Go / TypeScript）；
2. 为 CNB 云平台搭建一套**可复用、可复现**的开发环境；
3. 定制 WebIDE：深色主题默认生效、常用扩展预装。

---

## 2. 关键决策与共识

### 2.1 技术栈定案：Python 单一主语言

项目所有者提出：既然对标 Ollama（**Go**）与 CodeBuddy / Cursor（**TypeScript**），
是否该用 Go 或 TypeScript？经论证后**采纳 Python 单一主语言**，详见
[ADR-0005](../adr/0005-single-primary-language.md)。

| 项 | 决定 | 关键理由 |
| --- | --- | --- |
| 主语言 | **Python ≥ 3.12** | 自研增量（上下文引擎、护栏、评估、检索）**全部落在 Python 生态**；换语言等于放弃唯一的技术优势 |
| 推理后端 | llama.cpp（C++） | 通过 HTTP 调用，**不做语言绑定** |
| Node.js / pnpm | **预装，但非主语言** | 用于 MCP 官方 server、promptfoo、将来可能的 Web UI |
| Go | **不引入** | 其"单二进制分发"优势属于**产品化阶段**问题，本期不是关键需求 |
| Rust / C++ | 本期不引入 | 真正的性能瓶颈由 llama.cpp 承担，编排层不构成瓶颈 |

> **这个决策有明确的可逆性边界**：若 Demo 成功、需真正装进老人手机，
> 届时应**新增 ADR** 评估打包方案（PyInstaller vs 运行时重写为 Go）。
> 本决策不排除该演进路径，只是不在本期支付其成本。

### 2.2 开发环境采用 `.ide/Dockerfile` 定制（单容器模式）

CNB 的平台约定（已通过官方示例源码核实）：

| 文件 | 作用 |
| --- | --- |
| `.ide/Dockerfile` | 构建开发环境镜像 |
| `.ide/settings.json` | VS Code 设置，**必须由 Dockerfile 的 COPY 指令写入镜像** |
| `.cnb.yml` 的 `vscode` 事件 | 声明 `docker.build.dockerfile` 与 **`build.by`** |

**两个硬性约束**：

1. `build.by` 必须列出构建期用到的其它文件（即 `.ide/settings.json`），
   否则构建报错——这是 CNB 的强制要求。
2. 镜像内**安装了 code-server** 才会以**单容器模式**启动；否则退化为双容器模式。

### 2.3 环境内容边界

| 类别 | 内容 | 理由 |
| --- | --- | --- |
| 基础工具 | git + git-lfs、curl/wget、jq、ripgrep、fd、tree、sqlite3、vim、tmux、zsh | 日常开发与调试 |
| 构建工具链 | build-essential、cmake、ninja、pkg-config、libcurl-dev | 编译需要本地构建的 Python 包；构建 llama.cpp |
| **沙箱组件** | **bubblewrap**（主）、**firejail**（备）、nsjail 的构建依赖 | 本项目需要**无需 KVM 的用户态隔离**执行 AI 生成的命令 |
| Python | 3.12 + `uv`（通过 `pip install` 而非 `curl \| sh`，减少远程脚本执行） | 主语言工具链 |
| Node.js | Node 20 + pnpm 9 | 备用工具链（见 2.1） |
| IDE | code-server + 16 个扩展 | 见 2.4 |
| 推理 | **llama.cpp（CPU，源码构建）** | 主推理后端，预装可让 W1 验证立即开始 |

### 2.4 容器相关扩展的选择（回答项目所有者的疑问）

| 扩展 | 是否安装 | 理由 |
| --- | --- | --- |
| `ms-azuretools.vscode-docker` | ✅ **安装** | 镜像/容器/卷管理、Dockerfile 与 compose 语法支持；配合 `.cnb.yml` 中 `services: [docker]` 使用 |
| `ms-vscode-remote.remote-containers` | ❌ **不安装** | 该扩展面向"在**本机 Docker Desktop** 中开发 devcontainer"。我们的环境是**云端 WebIDE**，其自身已运行在容器内，该扩展在此不可用。安装它只会增加噪音 |

> 若后续确实需要，加一行 `code-server --install-extension` 即可。

### 2.4.1 CodeBuddy 补装（环境重建后发现遗漏）

首版镜像**漏装了 CodeBuddy**——而它在本项目中承担**主要的智能开发**工作。
漏装的影响不只是"少一个插件"，而是两处独立缺失：

| 形态 | 组件 | 漏装后果 |
| --- | --- | --- |
| CLI | `@tencent-ai/codebuddy-code`（提供 `codebuddy` 命令） | CNB 云开发入口页中的 **CodeBuddy Web 入口被静默隐藏**（不报错，直接不显示） |
| VS Code 扩展 | `Tencent-Cloud.coding-copilot` | WebIDE 内没有代码补全与 AI 对话面板 |

处理：两者都装。CLI 固定版本 `2.151.0`（≥ CNB 要求的 `2.137.0`），
扩展按 ID 从 Open VSX 安装，并在**镜像自检阶段硬校验**
（`codebuddy --version` 与 `code-server --list-extensions | grep -qix Tencent-Cloud.coding-copilot`），
确保"漏装"这类问题在构建期暴露，而不是等进入环境后才发现。

### 2.4.2 GitLens 移除（决策）

| 方案 | 结论 | 理由 |
| --- | --- | --- |
| 安装/保留 GitLens | ❌ 移除 | 核心增益（作者归属、多人协作视图、Launchpad、Workspaces）在**单人单作者**仓库中几乎无价值；体积与启动开销却是真实的；新版本还会持续推送 GitKraken 账号登录 |
| 内置 SCM + Git Graph | ✅ 采用 | 暂存/提交/分支切换/diff/文件历史由 VS Code 内置 Source Control（含内置 Source Control Graph）覆盖；`mhutchie.git-graph` 提供更顺手的提交图，纯本地、无需登录 |

补充事实（避免后续重复纠结）：**不存在名为 "GitLens Inspect" 的独立扩展**——
Open VSX 与 Marketplace 均无此包，它是 GitLens 自带的 Inspect 面板，
装了 GitLens 就有、不装就一起没有，因此**不需要单独决策**。

### 2.5 明确不安装 Android / 移动端工具链

移动端已在 [0003](0003-2026-09-15-单专家调度与上下文迁移.md) 中定为
**"架构约束 + 可行性验证"**，不做 App。且既定的验证路线是 **Termux**
（运行在手机上，不需要在开发机安装 Android SDK / NDK / Gradle）。

因此本期**不安装** Android 工具链——避免为一条尚未确定的路径付出环境成本。

---

## 3. 变更记录

### 3.1 首版环境

| 内容 | 说明 |
| --- | --- |
| 新增 `.ide/Dockerfile` | 8 个构建阶段；末尾含**自检步骤**（关键组件缺失即中断构建，避免交付坏环境） |
| 新增 `.ide/settings.json` | 默认深色主题（`Default Dark Modern`）、编辑器/终端/Python/Git 配置、**CNB 配置文件 YAML schema 校验**、关闭遥测 |
| 修改 `.cnb.yml` | 新增 `vscode` 事件：自定义镜像 + `build.by` 声明 + `runner.cpus: 8` + 启动后环境自检输出 |
| 新增 `docs/adr/0005-single-primary-language.md` | 技术栈定案 |

**关于 settings.json 的落盘路径**：官方示例写入 `Machine/settings.json`。
考虑到分作用域的限制（部分设置项在 Machine 作用域不生效），
本实现**同时写入 4 个路径**（2 个客户端 × 2 个作用域），确保无论通过
WebIDE 还是 Remote-SSH 进入都能生效。

### 3.2 环境重建后的修正

| 内容 | 说明 |
| --- | --- |
| 修改 `.ide/Dockerfile` | 新增第 5 阶段安装 CodeBuddy（CLI `@tencent-ai/codebuddy-code@2.151.0` + 扩展 `Tencent-Cloud.coding-copilot`）；扩展清单移除 `eamodio.gitlens`；自检阶段新增 `codebuddy --version` 与 CodeBuddy 扩展存在性校验 |
| 修改 `.cnb.yml` | 环境启动自检输出增加 `codebuddy` 版本一行 |
| **未改动** `.ide/settings.json` | 原计划删除失效的 `gitlens.*` 死配置，但该文件是 JSONC（含注释），与 pre-commit 的 `check-json` 钩子冲突；修改钩子配置属于门禁调整，需所有者确认，故本轮不动该文件（遗留项见 §5 V-8） |
| 修改 `docs/engineering/post-build-checklist.md` | 第 0/1 步加入 `codebuddy` 检查与扩展自检说明；回填范围扩到 V-7 |

---

## 4. 数据与证据

### 4.1 CNB 官方示例源码（已克隆核实）

来源：`https://cnb.cool/20210805/custom-vscode`（CNB 官方示例仓库，4 commits）

关键摘录（原文）：

```dockerfile
COPY .ide/settings.json /root/.vscode-server/data/Machine/settings.json
COPY .ide/settings.json /root/.local/share/code-server/Machine/settings.json
```

```yaml
$:
  vscode:
    - docker:
        build:
          dockerfile: .ide/Dockerfile
          by:
            - .ide/settings.json
      services:
        - vscode
        - docker
```

### 4.2 资源配置与免费额度核算

| 项 | 数值 |
| --- | --- |
| 云原生开发-CPU 免费额度 | **1600 核时/月**（月底清零） |
| 本次配置 | `runner.cpus: 8` → 8 核 / 16 GiB |
| 月用量估算 | 8 核 × 8 小时/天 × 22 工作日 = **1408 核时** |
| 结论 | **8 核属于可持续用量**，不会超额度 |

---

## 5. 问题与卡点

| 编号 | 问题 | 状态 |
| --- | --- | --- |
| V-1 | 首次构建耗时（llama.cpp 源码编译约 5~10 分钟）是否可接受 | **待验证** |
| V-2 | `.cnb.yml` 的 `vscode` 事件挂在 `"**"` 下是否可行 | **待验证**——官方示例用的是 `$`（兜底）。但根据触发规则文档，`$` 仅在**未被任何 glob 命中**时生效，而 `"**"` 命中所有分支，故挂在 `"**"` 下更可靠。若无效则改回 `$` |
| V-3 | `Default Dark Modern` 主题 ID 在所用 code-server 版本中是否存在 | **待验证**——不存在时 VS Code 会回退默认主题 |
| V-4 | settings.json 的 User / Machine 作用域实际生效情况 | **待验证** |
| V-5 | 容器内 `bwrap` 能否实际创建用户命名空间（沙箱方案的前置条件） | **待验证**——CNB 容器可能限制 unprivileged user namespaces |
| V-6 | llama.cpp 跟随 master 构建，缺少版本固定 | **已知问题**——待基准测试需要复现时改为固定 release 标签 |
| V-7 | `ms-python.vscode-pylance` 在 Open VSX 上查无此扩展（API 两次 404），该行可能一直"安装失败但不阻断构建" | **待验证**——在环境内执行 `code-server --list-extensions` 确认；若缺失，改用 `ms-pyright.pyright`（MIT、已上架 Open VSX） |
| V-8 | `.ide/settings.json` 是 JSONC（VS Code 允许注释），**无法通过** pre-commit 的 `check-json` 钩子——该文件在 HEAD 上即如此，说明它当初进仓库时绕过了钩子 | **待决策**——候选：① 为 `check-json` 配置该文件排除；② 改写为严格 JSON（丢失注释）；③ 换用支持 JSONC 的钩子。三者都涉及门禁调整，须所有者确认 |

---

## 6. 学到了什么

1. **文档不一定写全，官方示例源码往往更可靠。**
   文档页里完全没提 `.ide/settings.json` 的落盘路径，示例仓库的 Dockerfile 里却写得很清楚
   （而且写的是 `Machine/` 而非直觉上的 `User/`）。**遇到平台约定，直接拉示例源码。**
2. **平台约束要主动找**：`build.by` 这条"构建期依赖必须显式声明"的硬性要求，
   藏在示例 README 里而非主文档——漏了就是构建失败。
3. **"不做"也是决策**：明确写下"不装 Android 工具链""不装 remote-containers"，
   比装一堆用不上的东西更有价值——**每个未安装的组件都省下了一次未来的困惑**。

---

## 7. 下一步

- [ ] 触发一次开发环境构建，逐条验证 V-1 ~ V-5
- [ ] 在环境内执行 `make setup && make check`，确认工程门禁可用
- [ ] 验证 `bwrap` 可用性（决定沙箱方案是否需要降级）
- [ ] 执行 **W1 风险验证**：4B 模型能力边界 + 加载/prefill 实测
- [ ] 技术栈约定回写 SRS §11
