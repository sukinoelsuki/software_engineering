# 0013. 文档一致性核查、地基修复与 Agent 团队机制落地

- 时间范围：2026-09-18
- 状态：**进行中**（§7 即当前活待办）
- 关联：[0012](0012-2026-09-16-基准自动化数据流水线.md)（收篇）、
  [`engineering/doc-consistency-report.md`](../engineering/doc-consistency-report.md)（本篇的主要产出）、
  [ADR-0013](../adr/0013-branch-model-for-solo-dev.md)
- 提交：`7c39674`（基准固化分工）、`e8e0885`（五个角色 agent 定义）、`0b363d7`（模型选型笔记）、
  `d64f1ad`（一致性报告落盘）、`81282ea`（四条修复）、`c0f3d5a`（移除误入库截图）、
  `3a941b8`（agent 定义修正与 .gitignore）、`00b867a`（机制文档与 §10 修订）、
  `8dac983`（团队机制 devlog）、`8f55693`（首次推送与规则联动）

---

## 1. 本阶段目标

0012 把基准流水线跑通后，项目进入"要开始搭产品框架"的节点。但搭之前必须回答两个问题：

1. **我们的协作方式是什么？** 单人 + AI 角色，需要明确分工与成本纪律；
2. **地基平不平？** 60+ 份文档与实现之间有没有互相矛盾、误导决策的地方。

本篇记录这两件事的结论，以及由此暴露的"产品主体只有零散零件"的现状。

## 2. 关键决策与理由

### 2.1 协作方式：角色分工 + 文件域独占 + 实现与验证分离

落地为 `CODEBUDDY.md` / `AGENTS.md` 的 **§10**，并进一步落到**可执行配置**
（`.codebuddy/agents/*.md` 的 `model:` 字段）——"角色-模型绑定"不再靠人工切换。

六个角色的独占产出域见 §10.1。**核心不是"分活"，而是两条纪律**：
**实现与验证分离**（安全断言不得自证）、**文件域独占**（并行度不超过冲突控制能力）。

### 2.2 模型档位：按同口径分数 ÷ 倍率决定，不按档位名或价格

调研结论进了 [`notes/dev-agent-model-selection.md`](../notes/dev-agent-model-selection.md)：
同一批可用模型里，**倍率最低的档位在 Terminal-Bench 2.1 上分数最高**
（0.03x → 90.60，3/192），而一个中价档（0.25x）分数最低（66.0）。
⇒ 分配规则：机械档用 0 积分的档位，执行/规划档默认最便宜且够用的档位，卡住再按阶梯升级。

### 2.3 先修地基，再搭框架

一致性核查发现的问题里，有四条会**持续误导决策**，因此优先修：
状态矛盾（A-2）、合并策略与 ADR 冲突（A-3）、DoD 死项（A-7）、测试目标假失败（A-9）。

### 2.4 协作机制：主 Agent 恒为团队领导，五个角色统一 `agentic`

实测（见 §4）后明确了：`manual` 与 `agentic` **不是"保守 vs 激进"，而是两种不同的东西**
——`manual` 的语义是"人把它切成主 Agent 来用"，只有 `agentic` 能被主 Agent 派活。

⇒ 定案：**主 Agent 恒为团队领导**（派活、核对文件域、汇总、对所有者负责），
**五个角色统一 `agentic`**。设计类成员（总设计师 / 首席架构师）与执行类成员**不直接交接**，
一律经"**文件 + 领导核对**"——因为成员之间不共享上下文、且不跨会话恢复，
**未落盘的设计等于不存在**。所有者需要产品设计时，由领导把它拉成团队成员、
所有者与它直接对话，产出**先落盘**、再回流到领导。

**被放弃的替代方案**：混合模式（设计角色留 `manual`）。它的代价是"所有者要跟设计成员谈，
就得让出主对话"，等于放弃领导层，与"领导对全流程负责"直接冲突。

## 3. 变更记录

**协作机制（`7c39674` + `e8e0885`）**：两份基准文件各 +51 行新增 §10（角色与独占产出域、
七条协作规则、模型档位与成本纪律）；随后为五个角色创建项目级 subagent 定义
（`.codebuddy/agents/`，187 行），用 frontmatter 的 `model:` 字段绑定档位——
机械档绑 0 积分模型（验证、记录），执行档绑最便宜的可用档（实现、设计、架构）。
IDE 的 Agent Management 页面已确认五个 agent 被正确识别。

**模型选型调研（`0b363d7`）**：落盘为学习笔记。要点：档位名不能推断能力、厂商自报分数是
"满血口径"（带工具 + 最高思考档）、同一模型跨源分数不一致、收录站有时效错乱。
给出出处可信度分级（第三方独立复测 > 厂商自报 > 收录站汇总 > 社区实测）。

**文档一致性核查（`d64f1ad`）**：由只读探索代理全库扫描（75 份 Markdown +
`Makefile`/`scripts`/`.cnb.yml`/`src`/`tests`）产出 **14 条问题 + 完整现状盘点**，
落盘为 [`engineering/doc-consistency-report.md`](../engineering/doc-consistency-report.md)。
同时给该报告在 §10.1 补了落点条款（记录员的跨域例外）。

**四条高优先级修复（`81282ea`）**：

| 编号 | 问题 | 处置 |
| --- | --- | --- |
| A-2 | base project 状态有**四种表述**（ADR 索引"提议中"、提案索引"待决策"、基准"未冻结"、README"已完成"） | 统一为"**方向已定（组装组件 + 5 个自研模块）、复用组件清单未产出**"；待办改写成可判定状态 |
| A-3 | `CONTRIBUTING.md` 要求 squash merge，与 `ADR-0013` 冲突 | 改为 merge commit——**squash 会让 devlog/CHANGELOG 里写死的提交哈希指向不存在的对象** |
| A-7 | `docs/design/threat-model/` 不存在却被 DoD 列为必选 | 改为"写明威胁与缓解；目录建立后同步更新条目"（消除死项） |
| A-9 | `make test-security` 在无 `security` 用例时退出码 5，**必然失败** | 改为显式提示 + 返回 0；并在测试策略里标注"分层为目标态、当前只有 unit" |

顺带修了 A-4（develop 标注为工作主干）、A-5（统一基准数据归档路径）、
A-8（补 ADR-0014 与提案 0002/0003 索引）、A-10（README 仓库结构与实际不符）。

**一次流程失误（`c0f3d5a`）**：用 `git add -A` 提交时把工作区里的临时截图一并入库了
——提交前没有核对暂存清单。已移除该文件；此后提交前先跑 `git diff --cached --name-status`。

**0012 收篇**：基准自动化的核心议题（流水线建成、首夜验证、发布缺陷修复）已完成，
未完成项抄入本篇 §7；0002 / 0004 一并标注为收篇，0005 明确为"待人类逐条确认的清单"。

**团队调用机制实证与配置修正（`3a941b8`、`00b867a`）**：新会话第一件事即复验
`Task(subagent_name="verifier")`，**结果仍然失败**——由此确认本篇 §5 的旧诊断
（"需重载会话才能加载新建定义"）**不成立**。根因是 `agentMode` 被写成 `manual`：
官方文档明确 `manual` 的语义是"在 Agent 选择框中选中、**完全替代主 Agent**"，
只有 `agentic` 才由主 Agent（团队领导）调用。**只改这一个字段即跑通**。

据此落地三件事：

| # | 动作 | 落点 |
| --- | --- | --- |
| ① | 五个角色**统一 `agentic`**，并在 `description` 写明触发边界（收敛自动触发以控成本） | `.codebuddy/agents/*.md` |
| ② | 补 `.gitignore` 规则（实测该目录未被任何既有规则覆盖，含成员信箱，属运行产物） | `.gitignore` 的 `.codebuddy/teams/` |
| ③ | 新增机制**单点权威描述**，§10 只留常驻结论并指向该文 | [`engineering/agent-teams.md`](../engineering/agent-teams.md) + `CODEBUDDY.md`/`AGENTS.md` §10.4 |

③ 的理由值得记下：`CODEBUDDY.md` 自己规定"只写**必须始终生效**的内容"，
而机制细节（实测证据、硬约束清单、未验证项）**不必常驻**——所以按项目既有模式
（常驻摘要 + 完整文档，如 git-workflow 与 `.codebuddy/rules/git-workflow/`）分置，
**而不是再写第三份副本**（第三份副本只会新增一个漂移源）。
`§10.3` 同时补了两条团队成本纪律（并行是乘法开销、能串行就不并行）。

**首次推送与"规则联动修复"（`a211a44`）**：三笔提交经所有者逐文件审查后推送成功
（`0e553c9..8dac983  develop -> develop`，fast-forward）。所有者同时提出一条**机制性要求**：
**规则改了之后，必须回头更新被它覆盖的旧约束；只有写在日志里的历史记录不用改。**
据此做两件事：

1. 把这条要求写成**始终生效的约定**：`.codebuddy/rules/project-conventions/RULE.mdc`
   新增「**规则变更的联动更新**」——含"必须同步的位置"表、**反向搜索旧术语**的落地动作，
   以及"日志 / 调研笔记 / 已接受 ADR 的历史快照**不改**"的例外条款；
2. **立即执行一次反向搜索**，清除 `develop` 去保护后残留的旧约束：`git-workflow` 规则摘要
   （分支模型仍写"保护分支"、仍写"禁止直推 `develop`"、合并策略仍是 Squash merge）
   与 `CODEBUDDY.md` / `AGENTS.md` §2（仍写"合并到 `develop` 需事先批准"）。

**成员自动执行与并发写纪律（`809b354`、`40f9b0f`）**：所有者要求开启 `enabledAutoRun`
（效率优先，命令行执行不设限制）。据此把五份定义改为 `enabledAutoRun: true`，
并补齐它带来的唯一护栏缺口——**并发一致性**：`CODEBUDDY.md` / `AGENTS.md` §10.2 与
`agent-teams.md` 各增一条"**同一文件不得被两个成员同时写**"（并行只在产出域互不重叠时开、
派活提示写明产出白名单、同文件改动一律串行、冲突不自动合并）。
另澄清 `agent-teams.md` §4 的一处歧义：**"子代理不持有 `Agent` 工具"与命令行权限无关**，
它只表示"成员不能再派成员"。

**本轮一致性 / 地基修复提交链（沿 `develop` 14 笔提交链落定，均未 push、`make check` 全绿）**：

- `e41af26`（docs(requirements)）：修一致性报告 **A-12** 与 **A-6**。A-12 把 `REQ-PERF-04` 验收标准从不存在的"`bench` 命令"改为实际入口 `make bench-round`（证据 `Makefile:138` + `src/agent_sec_perf/bench/rounds.py:602`）；A-6 把 `docs/requirements/README.md` 的模块前缀以 `srs.md` §7 实际分组回写为 `MODEL/HARNESS/SEC/PERF/TOOL/UX/OBS/PLAT/OPS`，删去从不使用的 `KERNEL`。SRS 版本 v0.1.2 → v0.1.3。
- `1240806`（docs(adr)）：修 **A-14**。`ADR-0013` 追加 §9 修订记录（§1~§8 一字未动，符合"ADR 只增不改"），`docs/engineering/git-workflow.md` §2 分支表补 `bench/nightly`、`bench/data` 与例外条件（依据 `ADR-0014` §2.1），消除分支表缺口。
- `3d8edc8`（docs(adr)）：新增 `ADR-0015`（608 行，+ `docs/adr/README.md` 索引补 0015），回应 `CODEBUDDY.md` §9 两项待办（base project 选型冻结、目录结构与模块划分 ADR），并登记 **A-1**（`ADR-0003` §6~§8 历史残留、其"AIOS 源码核验门槛"作废；未改写 ADR-0003 正文）。状态为「提议中」：选定组件＝新增依赖，须所有者逐条确认。核心结论：四层纵向（UX→HARNESS→CAPABILITY→FOUNDATION）+ 两横切层（SEC/OBS）+ 零行为契约层 `contracts/`；复用组件清单按 8 类给出（已核验项注明 PyPI 出处，未核验标【待核验】，无一处凭印象写数字）；自研 5 项落到具体层并列出"不写什么"与 13 项不自研清单；`src/` 新增 8 个目录各附理由；`tests/` 四层落位（含 A-9 修复安排）；§8 分列「真决策 D1~D10」与「待核验事实 V-a~V-l」。

**验证方式**：各笔均 `make check` 全绿；提交前用 `git diff --cached --name-status` 核对暂存清单（沿用 `c0f3d5a` 后的纪律）；`ADR-0015` 的组件事实以"逐个访问 PyPI 项目页"核验、未核验项一律标【待核验】，未把推测当事实写入。本轮引入 **ADR-0015 的决策清单（D1~D10 / V-a~V-l）作为新的阻塞项**——详见 §7 优先级 B。
本轮亦把一致性报告头部的修复状态同步回本篇 §5（见 §5 更新注），避免活文档内的陈旧开放状态误导后续会话。

**链上后续提交（撰写时未知、本次回填；标注所属域）**：

- `4f7db3b`（**记录员域**）：将一致性报告头部「修复状态」同步回本篇 §5（A-3 等开放项收口注），并随领导把「`ruff` 格式化 `docs/` python 代码块」落为规则而收口 §7 对应登记。
- `3f13638`（**记录员域**）：在一致性报告头部补 A-3 引文更正（`git-workflow §3.3` → 实际 §4），并在本篇 §7 优先级 D 登记 `git-workflow.md` §2 分支图在 CJK 宽字符下右边界不齐的低优先项（来源 `d525f70`，附两种修法前提）。
- `e246e31`（**记录员域**）：本篇 §6 追加第四条过程教训——判据与结论分处两文件时先写下的会过期（架构师同会话两份产出自相矛盾，由领导复核发现），与既有「写在文档里 ≠ 已生效」同族。
- `f9b5ea6`（**架构师域**）：`ADR-0013` §9.4 引文更正（`git-workflow §3.3` → §4），按「只增不改」在修订记录中更正。
- `8f77942`（**架构师域**）：`ADR-0013` §9.3 就地把「常驻摘要只列短期功能分支」的旧表述更正，并照录原句留痕（**取代** `f9b5ea6` 的部分处理）。

其余链上提交哈希已就地登记：`7b6356f`/`39ec073`/`d525f70`/`ab599ba`（§7 优先级 D）、`72a50d9`（§7 优先级 B）；`e3690a6` 为本篇首次落盘（本次哈希由下次更新回填）。

**本轮提交链已推送（`5245ecc`）**：§3 提交链记录的 14 笔 + 末笔哈希回填 `5245ecc` 共 **15 笔提交**（`c11ea87..5245ecc`）经 `git push origin develop` 推送成功（**fast-forward**，目标远端 `origin`，2026-09-18），推送后本地与远端无差异。

**一处流程偏差（如实记录，不作美化）**：本次推送的依据是**项目所有者两次明确指示（"可以推送"）**；团队领导呈报的形式是**文件清单 + 改动规模（17 个文件、+907/−43）与关键点摘要**，**并未逐文件全文呈现**。而项目规则（`CODEBUDDY.md` §2 / `.codebuddy/rules/git-workflow/RULE.mdc`）要求推送前"**必须先提交文件内容审查……不得只给摘要**"，且须"**审查通过后**才能推送"。⇒ 本次实际是**所有者的明确指示压过了该规则的形式要求**，规则原本规定的"两阶段（全文审查 → 审查通过 → 推送）"在实际执行中被跳过。影响：规则的"全文呈现"前置条件未被执行；建议（待领导裁决）：下一轮明确"所有者可用明确指示豁免全文呈现"这一条例外，或将"清单 + 摘要"的呈报形式正式写进规则，使实际做法与书面规则一致。

**一次跨域混入事故（如实记录，不美化）**：本篇 §3 推送小结、§7 引言收口注、§7 新增两条待办（回报通道三机制、推送审查规则例外）的初稿，我先 `git add` 进索引、尚未提交；首席架构师随后提交其 `docs/engineering/agent-teams.md` 改动（`6fa010a`，`docs(engineering): 机制 3 改为"关闭前摘录可复现证据"`）时，**把共享索引里我那份 devlog 暂存内容一并带走了**——根因是对方将"核对索引"与"提交"串在一条命令里，核对没能阻断。机制根因：**工作树与 git 索引在同一仓库内全局共享**，而 `CODEBUDDY.md` §10.2 规则 8 的"文件域独占"只保护文件、**覆盖不到全局共享的索引**。处置（团队领导裁决，已生效）：**不改写历史**——理由：内容无错、未外泄（`origin/develop` 仍在 `5245ecc`）、且动 HEAD 必动共享索引、会碰到另一名正在同一索引上工作的成员（与本次事故同源）；改为**如实登记 + 固化提交纪律**。影响：域归属错位（记录员产出记在架构师域提交里），**内容完整无缺失**（8 行 / 4 个 hunk，经 `git show 6fa010a -- docs/devlog/0013-2026-09-18-一致性核查与地基修复.md` 复核）。后续固化纪律：① 提交只用 `git commit -o -m "<msg>" -- <本篇>`（`--only` 只提交点名路径，不带走他人暂存）；② `git diff --cached --name-status` 核对与 `git commit` 必须是两条独立命令；③ 发现他人已暂存内容不碰、只在回报说明。另注：本记录员曾以空提交 `809cc7a` 补"成员完成"终止声明信号，**该提交确实存在**（`git cat-file -t 809cc7a` = `commit`），但它是一笔**空提交**——`git show --stat 809cc7a` 不含任何文件。当时把它判为「提交被移除」是**误判**：devlog 末笔之所以仍显示 `6fa010a`，是因为**空提交不改变任何文件**。本段文字因此**未随任何提交落库**、一直滞留在工作区，最终由**团队领导兜底提交**（见下段）。此现象同属共享索引 hazard，进一步佐证上述纪律的必要性。

**记录员「完成信号与内容脱节」（2026-09-18 新增；含团队领导的自我更正）**：记录员 `scribe-records2` 在本篇留下的两处追加文字（本段、以及 §7 机制③的更新注）**从未被提交带走**——它按机制 1 以 `809cc7a` 发出终止声明，但那是一笔**空提交**，文字始终停留在工作区（`git status` 显示 `M`），直至它被强制终止。团队领导对它**两度判定并更正**：① 首次有界对账时只见 `6fa010a`，判为「**零产出 / 静默停滞**」；② 复核 `809cc7a`（`git cat-file -t` / `git show --stat`）后改判为「**完成信号与内容脱节**」，列为**第三类失效模式**（前两类为投递失败、延迟到达）。教训与 §6 同族：**现象是证据、归因只是假设**——领导第二次仍把「某一时点的仓库快照」当成了终态归因。机制层补强（**禁止以空提交作 `回报：` 终止声明**；对账判据须确认终止声明所在提交**含本域改动**）由首席架构师落入 `docs/engineering/agent-teams.md` §8。本篇 §3 这些追加文字**由团队领导兜底提交**：记录员域缺位（成员被强制终止），而 `CODEBUDDY.md` §2 本就把「维护开发日志」列为主 Agent 职责。

**本轮机制文档 7 笔提交已推送（`5245ecc..cac0bd4`）**：`95b25e0`（§8 回报通道可靠性设计）、`6fa010a`（机制 3 改为「关闭前摘录可复现证据」，含被扫入的记录员文字）、`08e764b`（五份 `.codebuddy/agents/*.md` 补回报纪律——机制 1 的落地前提）、`c327ca6`（两份常驻文件 §7 留痕表补「协作机制变更 → agent-teams.md」）、`8409909`（§8.1 措辞与机制 3 对齐）、`3eae714`（固化「共享暂存区」缺口：文件域独占不覆盖 git 索引）、`cac0bd4`（规则 9 括注的反向搜索修正）。目标 `origin`/`develop`，fast-forward。

**决策闭环与实现落地（2026-09-18，`a9c67d6` / `0f48e04` / `617f564` / `33cf6b8` / `bf1eda0`）**：

- **决策闭环（ADR-0015 → 已接受）**：所有者于 2026-09-18 批准 §8.1 的 **D1~D10**——D1（Typer+Rich）、D4（structlog）、D5（tree-sitter）、D6（platformdirs）、D8（8 个目录）、D9（`bench/{proc,paths,errors}.py` 提升为 `foundation/`）、D10（本期不引入清单）通过；**D2（pydantic）通过但限定用途**：仅用于"信任边界校验 + 工具参数 JSON Schema 生成"两处，配置仍 `tomllib`、内部结构仍 `dataclasses`，回退路径（手写校验）保留；**D3 由 `httpx` 改选 `urllib3` 2.x**；**D7（构建后端）暂缓**，待 `hatchling` 许可证核验（V-a）。`a9c67d6` 将 ADR-0015 状态「提议中」→「已接受」并据 D1~D10 更新选型；`0f48e04` 补「修订记录」小节（记转已接受与"接受时实质变更 = D3 改选"），并将 §5.2.4 表内行标识 `D1/D2/D3` 改为 `HTTP-1/HTTP-2/HTTP-3` 以消除与 §8.1 决策编号 `D1~D10` 的同名异义（四处交叉引用同步）。
- **实现落地（地基修复）**：`617f564` 经 `git mv` 把 `bench/{proc,paths,errors}.py` 提升为 `src/agent_sec_perf/foundation/`（**纯重构**，全仓 `src/`+`tests/` 无残留旧 import；`Makefile`/`scripts/`/`.cnb.yml`/`pyproject.toml` 零引用）；AST 唯一封装层白名单 `ENCAPSULATION_MODULE` 由文件名 `proc.py` 升级为相对路径 `agent_sec_perf/foundation/proc.py`——同名文件无法再绕过。
- **契约骨架（`33cf6b8`）**：按 ADR-0015 §5.4.1 建 8 个目录骨架；`contracts/` 落**最小契约**（纯类型 + Protocol，只依赖标准库）：`tools.ToolCallRequest/ToolResult/Tool`、`policy.PolicyDecision`、`model.ModelClient`、`audit.AuditSink`；**12 个类型按"不发明"原则留空（仅类型名、无字段）**，并列 **9 条需架构师澄清**的问题。
- **分层机器检查（`bf1eda0`）**：`tests/unit/test_architecture_layers.py` 把 ADR §7.1 的 **R1~R5 / V1~V6 做成可执行检查**（9 个用例），并做**变异探针**（向 `contracts/` 注入第三方 import、向 `foundation/` 注入上层 import、在 `bench/` 放 `proc.py`，三种违规均使对应用例失败 ⇒ 证明检查真的会拦、非恒过）。
- **依赖零新增**：`git diff -- pyproject.toml` 为空；`make check` 全绿、**76 个测试通过**、`mypy` 覆盖 26 源文件。

**验证方式**：各笔提交前用 `git diff --cached --name-status` 单独核对暂存清单（沿用 `c0f3d5a` 后的纪律，规避工作树与索引全局共享导致的串味）；`make check` 全绿；分层检查附**变异探针**证明其非恒过。上述 5 笔均落在本地 `develop`，待团队领导推送。

**接口契约与豁免登记同步（2026-09-18，`781b27d` / `d1672c8` / `f2f3c2e`）**：

- **① `docs/design/interfaces/` 已建立（`781b27d`）**：`README.md` + `model.md` + `tools.md` + `policy.md` + `audit.md` + `sandbox.md` 共 **1101 行**；每个类型给出「字段名 + 类型 + 语义 + 不变式 + 谁产生/谁消费」，并含「对 `contracts/*.py` 的改动清单」——把 `33cf6b8` 留空的 12 个类型补成了字段级契约（§7 原待办 ① 收口）。
- **② ADR-0014 安全豁免登记已同步（`d1672c8`）**：豁免表路径 `bench/proc.py` → `foundation/proc.py` 按"指针类更正"处理；文末新增「修订记录」照录原句（原 `bench/proc.py` 表述保留为历史，符合"日志/已接受 ADR 历史快照不改"的例外）。解决了实现者报告的"登记位置与实际豁免位置不一致"（§7 原待办 ② 收口）。
- **③ ADR-0015 补一条现状事实登记（`f2f3c2e`）**：把本轮地基修复后的现状事实补入 ADR-0015（不修改结论、只补事实，符合 ADR 只增不改与 V-a~V-o 的"只补事实"约束）。
- **④ 豁免登记落点口径已由团队领导统一（`0c657d3`）**：`pyproject.toml` 的 bandit 注释从"登记到 `SECURITY.md`"改为"给出规则号与理由（`# nosec B404` / `# noqa: S603`），并登记到 `ADR-0014 §2.9`"；反向搜索确认无别处如此表述（§7 原待办收口；是否进一步把豁免表从 `ADR-0014` 迁移至 `SECURITY.md` 属需新增 ADR 的决策，本次不做）。

**验证方式**：`interfaces/` 落定后 `make check` 全绿；豁免更正附"反向搜索确认无残留活引用"（见 ADR-0014 修订记录）。

## 4. 数据与证据

- **只读探索代理的实际开销**：78 次工具调用、**125 秒**、**4.02 credits**（约月额度的 0.06%）。
  产出 14 条问题 + 3 张盘点表 —— 这是"用便宜档位跑机械任务"的第一个成本样本。
- **核查暴露的两个反直觉事实**：
  ① `CONTRIBUTING.md` 与 `ADR-0013` 的合并策略冲突，而冲突方之一（squash）会**破坏证据链**
  ——这类问题只有"跨文档对照"才能发现，单看任何一份文档都是自洽的；
  ② 文档**宣称已有**但实际不存在的有四项：威胁模型（0 条）、设计文档（`design/` 下只有 README）、
  测试分层（只有 unit）、基准归档目录（`reports/bench/` 不存在）。
- **`make test-security` 的实测**：修改前 `67 deselected / 0 selected` ⇒ 退出码 5；
  修改后同一命令输出提示且 `rc=0`（`make check` 仍全绿，因为它本就不引用该目标）。
- **产品骨架的真实盘点**（读代码得出）：`src/` 下唯一有实现的是 `bench/`（12 模块 + 4 夹具），
  其中**可直接复用为产品地基的只有三处**：`proc.py`（唯一子进程封装：非特权 uid + rlimit + 最小环境）、
  `paths.py`（路径白名单）、`errors.py`（fail-secure 异常层次）。
  产品六层（模型/Harness/安全/工具/UX/可观测）中，**五层只有零散零件、没有骨架**。
- **发布链路的"三重验证"（2026-09-18，修复后的真实运行）**：数据分支现有三轮数据，
  `bench/daily/` 下 **09-16（28 文件）、09-17（28 文件）、09-18（124 文件）同时存在**：

  | 验证点 | 证据 | 结论 |
  | --- | --- | --- |
  | SIGPIPE 修复生效 | 09-17 的 push 轮成功发布（`2026-09-17-push`，1.063 核时） | ✅ 发布不再中止 |
  | **合并而非替换** | **09-16 的 28 个文件仍在** | ✅ 历史轮次未被删除 |
  | **crontab 事件能发布** | 09-18 04:00 夜轮（`git_commit=9d24ec8`）成功入库 | ✅ **crontab 令牌有 `repo-code:rw`**（关闭 0012 §5 的悬置项） |

  首个完整夜轮的数据（三档 × R=10，**6.859 核时**，隔离 `user`，签名含 CPU 型号）：

  | 档 | prefill 中位数 | gen 中位数 | 能力通过率 |
  | --- | --- | --- | --- |
  | S | 92.21 | 26.11 | 66.7% |
  | M | 50.93 | 15.49 | 66.7% |
  | L | 27.60 | 8.86 | 33.3% |

  与 09-17 那轮（发布失败、数值只存在于构建日志）同量级（S 90.90~93.78、M 51.11~51.68、
  L 27.58~28.01）⇒ **同签名下"跨夜序列"的基线已经成立**，这是本项目第一次拥有
  可比的时间序列数据。
- **团队机制实验（2026-09-18，三次调用）**：
  ① `manual` 定义下 `Task` 报 `Subagent "verifier" not found`（加 `subagent_path` 同样失败）；
  ② 改 `agentMode: agentic` 后**单次前台独立验证跑通**——15 次工具调用 / **687.93 秒**，
     **此期间主对话被完全冻结**，所有者无法介入；
  ③ 团队 `ctx-probe` 的**两个后台成员**（`verifier` + `scribe`）并行完成并回报，
     派活调用**立即返回** ⇒ **前台与后台的可用性差异是机制性的，不是数量级差异**。
- **门禁事实的二次独立确认**：前台独立验证与后台 `verifier` **两条独立路径**得到同一结论
  ——`Makefile:108` 的 `check:` 依赖为 `format-check lint typecheck test security`
  （**无 `test-security`**）、`tests/` 下**只有 `unit/`**（10 个 `.py`：`conftest.py` +
  9 个 `test_*.py`；`integration/`/`security/`/`benchmark/` 目录**均不存在**）、
  `pytest -m security` 选中 **0 个**用例 ⇒ `test-security` 的 `rc=0` 是"**无用例可跑**"，
  `make check` 对安全回归**零覆盖**。
- **`make test-security` 的前置条件缺口（新发现）**：干净工作区（无 `.venv`）直接执行报
  `Failed to spawn: pytest`，**`rc=2`**；**必须先 `make setup`**。CI 每条 `make check`
  前都先 `setup`，故不受影响 ⇒ 但 §3/§4 原先写的"`rc=0`"**缺了这条前提**，属文档复现
  条件不完整。

- **团队成员失效模式的实测三例（2026-09-18）**：
  ① **投递失败 / 延迟到达**：记录员首次任务的 3 次回报仅 1 次到达；关闭确认**延后约 20 分钟**才到（且是团队已删除之后）。
  ② **消息已读但不产出（当时的判定）**：其信箱 4 条消息全部 `read: true`——08:01:27（架构师同成员同步）、08:02:02（领导停手 + 追加任务）、08:04:06（领导到达性检查）、08:08:53（领导关闭请求）。**此判定后被更正，见 ③。**
  ③ **完成信号与内容脱节（更正后的判定 = 第三类失效模式）**：`809cc7a`（`docs(devlog): 记录员完成信号——0013 本轮三处变更已落库`）经 `git show --stat` 核实为**空提交**（不含任何文件）；记录员的**两处文字追加从未提交**、滞留工作区（`git diff --stat` = 1 file changed, 3 insertions(+), 1 deletion(-)），直至其被强制终止。⇒ **对账只看「有无终止声明」会把这种情形误判为已完成，并把内容丢在工作区。**
  **可核性**：`git cat-file -t 809cc7a` = `commit`；`git show --stat 809cc7a` 无文件；上述 diff 已由团队领导兜底提交；信箱证据在关闭团队前摘录（原始文件随团队删除消失）。
  **方法论提示**：**领导给出的证据只是某一时点的快照**——本轮领导两度误判（先「零产出」、后误信「提交被移除」），均由复核关键谓词（`git cat-file -t`、`git show --stat`）纠正。此条与 §6「现象是证据、归因只是假设」同族。
- **关闭请求的超时是 60 秒**（本轮实测）：`scribe-records2` 的关闭请求发出后 60 秒内未确认，被运行时**强制终止**（原话：did not acknowledge the shutdown_request within 60s and has been force-terminated）。⇒ 机制 3「关闭前先摘录证据」是**有截止时间的动作**（窗口约 60 秒），不是一句原则；本轮据此把「先读信箱、再发关闭请求」的顺序固定下来。
- **D3 改选的决策证据（2026-09-18 实查）**：`httpx` 最新稳定版 `0.28.1` 停留在 **2024-12-06**（稳定线约 **21 个月**未更新）；`1.0.dev1`(2025-07-02)→`1.0.dev6`(2026-08-31) 中间静默约 11 个月，且**该预发布线仅出自 `packagetrack.dev` 单一来源**（需以 PyPI 官方页二次确认）；`0.28.0` 已移除 `proxies`/`app`、弃用 `verify`/`cert` ⇒ 1.0 破坏性变更在即；`httpx`/`requests` **默认读 `HTTP(S)_PROXY`**（需 `trust_env=False` 关闭），与"出站默认拒绝 + 白名单放行"不符。对照：`urllib3` 2.x 线**活跃**（`2.7.0` 变更日志标注 2026-05-07，另有 `urllib3-lts`）、**纯 Python 无编译扩展**、代理**显式**（需 `ProxyManager`）。**来源可信度局限**：`httpx` 的 1.0 预发布线与维护状态、以及 urllib3 的日期均来自单一/非官方镜像，**须以 PyPI 官方页二次确认**（ADR-0015 §8.2 V-m / V-n / V-o）。
- **分层机器检查的有效性证据（变异探针）**：向 `contracts/` 注入第三方 import、向 `foundation/` 注入上层 import、在 `bench/` 放 `proc.py` 三例违规，**均使对应用例失败**（9 个用例中定向触发），证明 ADR §7.1 的 R1~R5 / V1~V6 检查是真实拦截、非恒过——避免"绿着但没生效"的静默失败（与 A-9 同类隐患）。
- **安全豁免的"承载性"验证（反向验证，实现者报）**：豁免注释不是摆设——`ruff check --select S603 --ignore-noqa` 精确报出 **3 条**（`foundation/proc.py` L145/187/223），`bandit --ignore-nosec` 精确报出同样的 **3 条 B603** 加 L23 的 **B404** ⇒ **3 个调用点 + 1 个 import 与豁免注释一一对应、无多无少**（证明豁免登记确实指向真实调用点，而非过期注释）。
- **随豁免核查暴露的既有缺陷（登记与实际不完全咬合）**：① `ruff S404` 是 **preview 规则、当前未启用**，而豁免登记表原把它与 `bandit B404` 并列——属"虚的规则号"（登记表写了未生效规则）；② 豁免理由原先**只覆盖第 1 个调用点**，已派架构师统一措辞、实现者逐点照抄（其余 2 个调用点的理由原缺）。两项均随 `d1672c8` 修正（**grep 实证**：`ADR-0014` §2.9.1(a) 已将表改为「bandit B404；ruff S404 处于 preview、当前未启用」，§2.9.1(b) 给出逐点理由措辞；`foundation/proc.py` 的 L23/L145/L187/L223 行内理由均已就位 ⇒ 与「进行中」假设不符，按「grep 到即记已修」记为已修，待领导确认收口）。
- **共享索引纪律"被险情真的挡住"（本轮最有价值的一条，实现者实测 + 记录员旁证）**：实现者 `git add` 自己的文件（`src/agent_sec_perf/contracts` + `tests/unit/test_contracts.py`）后跑 `git diff --cached --name-status`，发现索引里躺着**他人暂存内容 7 个文件 / 3 类**——类① ADR：`docs/adr/0014-benchmark-automation.md`（1）；类② 设计接口：`docs/design/interfaces/{README,audit,model,policy,sandbox}.md`（5）；类③ devlog：`docs/devlog/0013-2026-09-18-一致性核查与地基修复.md`（1）。它靠**两阶段核对 + `-o`/`--only` 显式路径**把它们排除：① 提交前 `git commit -o -m … -- src/agent_sec_perf/contracts tests/unit/test_contracts.py`；② 提交后 `git show --stat 20e7d49` = **6 个文件**（5 个 `contracts/*` + 1 个 `tests/unit/test_contracts.py`），`docs/` **零命中**；③ 反向确认 `-o` 没吞他人内容——再跑 `git diff --cached --name-status`，`docs/design/interfaces/*`（5）+ devlog（1）**仍在索引**；唯独 `docs/adr/0014-…` 消失，因架构师同窗提交了 `0e88816`（提交日志两笔间插着 `e954a1d`/`0e88816`）；④ 第二笔复核 `git show --stat 0a121ef` = 2 文件；⑤ 第三笔 `2f6080b`：`git diff --cached -U0 … | grep '^[+-]'` 恰好 **4 行删 / 4 行增、全为注释行**。⇒ **这是"共享索引 hazard"第一次被纪律真的挡住**（`agent-teams.md` §6 新小节 + `CODEBUDDY.md` §10.2 规则 8 的 `-o` 纪律）；**可复现边界**：窗口极短（架构师在"两条命令之间"完成提交），故"改完尽快提交"与"提交前重跑 `git diff --cached` 核对"两条都不可省；**对照组**：上一轮 `6fa010a`（跨域混入）是同一场景、纪律未生效，分野只两条——`-o` 点名自己路径 + 核对与提交分两条命令。记录员本人亦独立观测同类（提交前索引有架构师域 6 文件暂存、`--only` 提交只带走 devlog），可作旁证。

## 5. 问题与卡点

- ~~**自定义 agent 无法被 `Task` 调用**~~ ⇒ **已于 2026-09-18 结案，且原诊断是错的**。
  原文写的是"IDE 界面可用（已截图确认），但工具侧需要**重载会话**才能加载新建的 agent 定义"。
  新会话复验**依然报 `Subagent "verifier" not found`**，直接证伪"重载会话"这一归因；
  真正原因是 `agentMode: manual` 的语义与我们的用法不符（它不可被派活）。
  **这句错误诊断原文留存于此**：它是一次"看似合理的归因未经验证即写入日志"的实例——
  与 §6 记的"文档漂移"是同一类失败，只是发生在日志里。
  两条附带结论：① **界面上看得见 ≠ 能被调用**，原记录的"已截图确认"不能作为可用证据；
  ② 前台子任务会**冻结主对话**（实测 687 秒），"后台并行"必须走团队模式，而不是
  "只能用内置的只读探索代理"（这句同样不成立——自定义角色可作为团队成员并行）。
- **`make test-security` 的文档前提缺失**：干净工作区直接执行返回 `rc=2`，
  必须先 `make setup`（详见 §4）。原 §3 的"`rc=0`"结论**未声明该前提**。
- **门禁对安全回归零覆盖**：`make check` 里的 `security` 环节只是 `bandit` + `pip-audit`
  （静态扫描 + 依赖审计），且 `test-security` 未被 `check` 引用、用例数为 0
  ⇒ 属"**绿着但没生效**"的静默失败，且会给"安全已受控"的错觉（详见 §4）。
- **ADR-0013 的验证条目从未真正执行**：ADR-0013 §7 要求"ADR、`git-workflow.md`、
  `CODEBUDDY.md` / `AGENTS.md` 三处表述一致，无矛盾"。实际是 `git-workflow.md` 与 ADR
  已切换到新模型，而**规则摘要与 §2 停留在旧模型两天**，直到本次推送前由所有者指出才被发现
  ⇒ **"验证条目写进文档"不等于"被执行"**；跨文件一致性必须做成**可执行检查**（见 §7 优先级 D）。
- **报告里仍有 10 条未修**：A-1（ADR-0003 §6~§8 历史残留，需新增 ADR 登记）、A-6（需求编号规范与 SRS 不符）、
  A-11（README/SECURITY 宣称"CI 密钥扫描"但 CI 实际不查）、A-12（`REQ-PERF-04` 验收标准指向不存在的 `bench` 命令）、
  A-13（已在本篇收篇时处理）、A-14（`bench/nightly`、`bench/data` 未登记进 ADR-0013 分支表）。
  （2026-09-18 更新：本篇状态仍"进行中"，但上述开放项多数已收口——A-1 已由 `ADR-0015 §5.5` 登记为历史残留、A-6/A-12 已由 `e41af26` 修、A-14 已由 `1240806` 登记进 `ADR-0013 §9`、A-13 已于收篇时处理；**仍未修仅 A-11**，其余见 §7 优先级 D。）
- **`0.03x` 是限时活动价**，到期后执行档成本会上升；届时需要重新评估档位（依据已落盘在笔记里）。
- ~~基准流水线的夜轮验证~~：**已于 2026-09-18 完成且三重验证全部通过**（见 §4）；
  0012 §5 的"crontab 令牌是否有 `repo-code:rw`"悬置项由此关闭。

## 6. 学到了什么

一句话：**文档会漂移，而漂移的文档比没有文档更危险——它会让人基于错误前提做决策。**

`adr/0003` 的 §0/§5.1 与 §6~§8 在同一份文件里指向两个不同的项目，`CONTRIBUTING.md` 的
合并策略与 `ADR-0013` 相反，README 说 Phase 0 已完成而基准里四项待办未勾——
**每一份单独看都自洽，只有横向对照才暴露矛盾**。这正是"记录员"这个角色存在的理由，
也是"把约定做成机器检查"（A-3 那类规则本可以写成测试）的价值所在。
（2026-09-18 更新：三例中 `CONTRIBUTING.md`↔`ADR-0013` 已由 `81282ea` 经 A-3 修复（改 merge commit）、`README` 的 Phase 0 状态已由 A-2 统一为"进行中 + 待办未完"（见 README §5）；仅 `adr/0003` 双项目指向仍为真，已改由 `ADR-0015 §5.5` 登记为历史残留而非改写正文——原句保留作"漂移曾发生"的证据。）

第二条：**"宣称已有"与"实际存在"之间要定期对账**。四项内容被反复引用却从未建立
（威胁模型、设计文档、测试分层、归档目录），说明文档里的"计划结构"很容易被后续文档
当成"既有事实"引用——引用一次，就开始积累误导。

第三条：**"归因"和"现象"要分开验证**。§5 那条"需要重载会话"当初看着很合理
（新建文件 → 需要重新加载），于是就被写进日志当结论；实际原因完全不同。
教训是：**现象是证据，归因只是假设**——写进日志时必须标明哪部分是观察、哪部分是推测，
并且**下一次会话第一件事就是复验推测**。这次能纠正，靠的正是上一轮把"新会话先验证"
写成了待办，而不是靠记忆。

第四条：**判据与结论分处两个文件时，先写下的那份会过期**。本轮真实发生：架构师先在 `ADR-0013` §9.3 断言"常驻摘要只列短期功能分支、不构成矛盾"，随后在写 `git-workflow.md` §2「摘要与完整文档的分工」时才把判据落成文字——判据是"省略后按摘要默认规则推出的动作是否仍然正确"，据此 `bench/*`（不合回、不得删除）**必须**出现在摘要里，于是它补了 `.codebuddy/rules/git-workflow/RULE.mdc`。结果同一作者、同一次会话产出了两份互相矛盾的文字（一份说不列长驻分支、一份说必须列），由领导复核两人产出时发现。这与 §5/§6 已有的"把约定写成文字就以为它生效了"是**同一族失效**——都是"以为写了就等于成立"。结论：**同一议题的多份产出，要在判据落地后回头复核一遍**；这正是"实现与验证分离 + 领导核对文件域"存在的理由。该教训尚无对应笔记（待补）。

## 7. 下一步（= 当前活待办清单）

> 0012 §7 的未完成项已抄入本节（0012 §7 保留原文作为历史）。本轮（09-18）收口：A-14、ADR-0015、A-6、A-12 已落 `develop`（见 §3）。
> 本轮 `develop` 上共 **15 笔提交（`c11ea87..5245ecc`，含 §3 提交链的 14 笔 + 末笔哈希回填 `5245ecc`）已全部推送**至远端 `origin`（fast-forward，2026-09-18）。批准与呈报方式：依据所有者两次明确指示"可以推送"，呈报采用**文件清单 + 改动规模（17 文件、+907/−43）+ 关键点摘要**，**未逐文件全文呈现**——属对"推送前须全文审查、审查通过后方可推送"规则的形式豁免（详见 §3 推送小结）。

### 优先级 A：基准发布链路（已闭环）

- [x] ~~下一轮跑完后确认 `bench/daily/<日期>/` 出现，且历史轮次（09-16）没有被删~~（09-18 已验证）
- [x] ~~确认 `crontab` 事件的令牌是否有 `repo-code:rw`~~（09-18 夜轮发布成功 ⇒ 有）
- [x] ~~把 `bench/nightly` 与 `bench/data` 登记到 ADR-0013 的分支表（对应 A-14）~~（`1240806` 已修）
- [ ] 09-18 起数据已可累积：下次观察时对比同签名的 S/M/L 三档是否稳定（极差与漂移）

### 优先级 B：搭产品骨架（本阶段主线）

- [x] ~~**`ADR-0015`：分层与自研边界**（四层纵向 + SEC/OBS 横切 + `contracts/`；复用组件清单 + 自研边界，`3d8edc8` 已新增，状态「提议中」）~~
- [x] ~~**[阻塞 · 最高优先级] 所有者逐条确认 ADR-0015 §8.1 的 D1~D10**（2026-09-18 已批准：D7 暂缓待 V-a，其余通过；ADR-0015 状态「提议中」→「已接受」，依据 `a9c67d6`/`0f48e04`）~~
- [ ] **完成 ADR-0015 §8.2 的 V-a ~ V-l 核验**：把组件事实（PyPI 出处 / 版本 / 许可证 / 体积）补入该文；**只补事实、不改结论**；标【待核验】的条目不得当作已确认事实使用。
- **以下三项以 D1~D10 确认为前提，确认前不启动**：
- [ ] **CLI + agent 循环最小骨架**：`Typer` CLI + 一次"读文件 → 调模型 → 执行工具 → 回喂"的闭环
      （`REQ-HARNESS-01`/`REQ-UX-01` 的最小载体；推理复用 `bench/runner.py`，执行复用 `foundation/proc.py`）
- [ ] 模型层最小抽象：本地/云端统一接口 + 能力探测（`REQ-MODEL-03/05/06`）
- [ ] 安全层骨架：能力/权限模型 + 审计日志（`REQ-SEC-01/06`）

**本轮（09-18 决策闭环 + 地基修复）新增待办**：

- [ ] **[实现者后续] 12 个留空类型补字段**：`contracts/` 的 12 个类型当前仅类型名、无字段；待接口定义后由实现者补字段（按"不发明"原则，字段以接口定义为准）。
- [ ] **[待落地] R5（领域包禁止加载 `.py`）暂无代码可测**：`harness/domain_pack` 尚未落地，ADR §7.1 的 R5 检查暂缺被测对象，待该子系统建立后补可执行检查。
- [ ] **[待补] 威胁模型仍 0 条**：`docs/design/threat-model/` 仍为 0 条威胁条目（§5 B2 已记），属 Phase 1 前置交付物，须补初稿。
- [ ] **[约束] 依赖按需加入**：首个真正用它的增量——模型层用 `urllib3`、工具参数用 `pydantic`；写入 `pyproject.toml` 前仍受 ADR-0015 §7.4 的"可安装性与类型兼容"验证约束（D7 暂缓、V-a~V-o 未全核验完成前不得写依赖）。
- [ ] **[架构师未开始] `docs/design/architecture.md` 仍未写**：`ADR-0015` §7/§9 规划；`interfaces/` 已完成（依据 `781b27d`），architecture.md 未开始（属 Phase 1 设计前置件）。
- [ ] **[架构师自查] `CapabilityTier` 成员待定**：架构师正据 `ADR-0011` 与 SRS `REQ-MODEL-03/05/06` 自查，**仅在两处矛盾时才升级给所有者**（不主动升级）。
- [ ] **[待决 · 需新增 ADR] 豁免登记表是否移入 `SECURITY.md`**：`ADR-0014 §2.9` 现承载豁免登记（路径同步 `d1672c8`、口径统一 `0c657d3` 均已落）；但组织上豁免表置于 `SECURITY.md` 更自然——是否迁移属需新增 ADR 的决策，本次不做。

### 优先级 C：设计前置件（与 B 并行）

- [ ] 威胁模型初稿（`docs/design/threat-model/`，**前置交付物**，当前 0 条）
- [ ] `docs/design/architecture.md` + 模块接口定义
- [ ] SRS v0.2 修订（与 0002~0004 的讨论合流，见 `devlog 0004` §4）

### 优先级 D：一致性报告剩余项

- [ ] A-11：README/SECURITY 宣称"CI 密钥扫描"——要么改文档，要么在 `.cnb.yml` 真加一个 stage
- [ ] 把 A-3 那类"易漂移的约定"继续做成机器检查（能写成测试的不要只写在文档里）
- [ ] 把"**跨文件一致性**"做成机器检查：至少覆盖 ADR-0013 §7 要求的
      "ADR / `git-workflow.md` / 规则摘要 / `CODEBUDDY.md` / `AGENTS.md`"五处口径，
      以及 `Makefile` / `.pre-commit-config.yaml` / `.cnb.yml` 的门禁口径
      （`git-workflow` 摘要停在旧模型两天，正是这条缺失导致的）
- [x] ~~**[A-2 残留] `docs/proposals/0003` 头部状态栏仍为「待决策」**~~（架构师复核时发现；`7b6356f` 已同步为采纳结论）
- [x] ~~**[一致性] ADR-0003 §5.1（5 个自研模块）与提案 0003 §5.3（4 个）清单差异**~~（已随 `ADR-0015` §5.5 登记"以 §5.1 为准"，`3d8edc8`）
- [x] ~~**[隐性约束 · 已写入规则] `ruff` 会格式化 `docs/` 内的 python 围栏代码块，而 `make check` 的 `format-check` 覆盖 `docs/`** ⇒ 在文档里写 python 代码块会被门禁拦下（首席架构师实测踩到）。规则已由领导写入 `.codebuddy/rules/docs-and-adr/RULE.mdc`（`ab599ba`）的「文档通用规范」（含"示意片段用 `text` 标注、写完先跑 `uv run ruff format docs/`"两条操作办法）；该规则落在"写文档时加载的规则"里、**不常驻 `CODEBUDDY.md`**（常驻文件只写必须始终生效的内容）。记录员原只登记，现随规则落笔而收口。~~
- [x] ~~**[一致性] `.codebuddy/rules/git-workflow/RULE.mdc` 分支摘要未列 `bench/*`（亦缺 `release/*`/`hotfix/*`）**~~（摘要与完整文档的分工口径无人写；`39ec073` 已在 `git-workflow.md` 写明口径并登记两处漂移源）
- [ ] **[低优先 · 一致性] `git-workflow.md` §2 分支图（开头 ASCII 图）在 CJK 宽字符下右边界 `│` 不齐**：由首席架构师在 §2 分支表修复时发现（`d525f70`），属只读扫描的副产物。修法二选一（各有前提）：① 改为 **Mermaid**（与 `docs/design/README.md` 的"Mermaid 优先"口径一致、根治对齐，但**须先验证 CNB 与 IDE 确实渲染 Mermaid**，未验证就换可能比现状更糟）；② **手工按东亚字符宽度重算**（易算错，需逐行核）。未定方案前保持现状。

### 优先级 E：基准与安全主线的遗留

- [ ] L0 子集评测执行器（20 题 humanevalplus 接入轮次，产出通过率与方差）
- [ ] 自建阶梯任务集 L1；M 档横向对照
- [ ] 硬件探测模块 + `isolation_matrix()` 合并 + "探测值 → 决策 → 生效配置"留痕格式
- [ ] `make sandbox-run CMD='...'` 封装；探针矩阵写成可执行测试纳入 CI
- [ ] 把保留期清理挪到数据分支的工作副本上执行（在 CI 上等于不生效）
- [ ] （可选，需批准）把 `bench/data` 重建为 orphan 分支
- [ ] `devlog 0005` 的 S-1 ~ S-10 与 Q-1 ~ Q-8 **待项目所有者逐条确认**
- [ ] 30 天后回顾分支自检挂载点（ADR-0013 观察项）

### 优先级 F：协作机制（2026-09-18 新增）

- [ ] **落一份协作机制 ADR**：主 Agent 恒为团队领导、五角色统一 `agentic`、
      设计→实现以**文件为唯一载体**、团队成本纪律。属"流程变更"⇒ 按 §7 文档表须有 ADR；
      由**首席架构师**产出（`docs/adr/` 是其独占产出域）——这本身也是"设计成员走文件交接"
      的第一次实做。
- [ ] 复验 [`engineering/agent-teams.md`](../engineering/agent-teams.md) §7 的三个未验证项：
      IDE 能否**直接点名成员**对话 / `agentic` 是否仍可在 Agent 选择框中被手动选为主 Agent /
      成员之间能否直接互发消息。**复验后必须删掉或改写该表**（未验证项长期挂着＝漂移源）。
- [ ] 把"agent 定义字段合法性"做成**机器检查**：五个定义的 `agentMode` 均为 `agentic`、
      `name` 与文件名一致、`model` 在允许列表内（能写成测试的不要只写在文档里）。
- [ ] 把"**子代理输出是不可信输入**（可被注入）"登记进威胁模型
      （`docs/design/threat-model/`）；上游 harness 已对成员产出做去毒，可作为缓解措施引用。
- [ ] 补 `CODEBUDDY.md` / `AGENTS.md` §7"文档与决策留痕"表：新增一行
      "协作机制变更 → `docs/engineering/agent-teams.md`"。
- [ ] **回报通道三机制（2026-09-18 定，完整落点由首席架构师写 `docs/engineering/agent-teams.md`）**：成员回报消息实测不可靠（3 成员 9 次派活全部执行，但记录员回报到达率仅 1/3、关闭确认延后约 20 分钟、团队删除时信箱被一并删除无法复盘）；据此定三条：① 成员最后动作必须是提交、正文含 `回报：` 终止声明；② 领导在有界时点对账仓库（不轮询），"未确认 ≠ 失败、禁止据此重派"；③ 关闭团队前先归档成员信箱。（2026-09-18 更新注：③ 已据 `docs/engineering/agent-teams.md` §8.3 定案为"关闭前摘录可复现证据进仓库，原始信箱不承诺长期保存"——原"归档成员信箱"不再作为承诺；本句保留作机制演化证据，未按新结论回改。）**对记录惯例的影响**：今后"成员完成"的信号以**仓库中的终止声明为准，消息仅作加速器**——本篇 §3 推送小结与本次提交即按此执行（提交信息正文附 `回报：` 块）。
- [ ] **推送审查规则例外（待领导裁决）**：本轮推送以"所有者明确指示"豁免了"逐文件全文呈现"的形式要求（见 §3 推送小结与本篇 §7 引言收口注）。需明确：① 是否将"所有者可用明确指示豁免全文呈现"列为正式例外；② 或将"文件清单 + 规模 + 关键点摘要"的呈报形式写进 `CODEBUDDY.md` §2 / `git-workflow` 规则，使实际做法与书面一致。
- [ ] **机制 1 / 2 的补强（由首席架构师落入 `docs/engineering/agent-teams.md` §8）**：① 明确**禁止以空提交作为 `回报：` 终止声明**（`--allow-empty` 不适用于本协议）；② 对账判据不止「有无终止声明」，还需确认**该提交含本域改动**（`git show --stat` 非空、或至少触及声明中的文件）——否则会把「信号已发、内容滞留工作区」误判为已完成（本轮实例见 §3 / §4）。
- [ ] **第三类失效模式已登记**：投递失败 / 延迟到达 / **完成信号与内容脱节**——由本轮 `agent-teams.md` §8.1 证据节收录（随架构师补录提交）。
- [ ] **记录员域缺位的事实登记**：本轮 devlog 的追加内容由**团队领导兜底提交**（记录员被强制终止）；下一轮若仍需记录员，按新机制派活（回报写进提交正文、禁止空提交、核对与提交两条独立命令）。
