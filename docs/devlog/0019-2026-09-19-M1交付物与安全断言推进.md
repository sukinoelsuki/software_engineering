# 0019. Phase 1 收口：`M1` 交付物与安全断言推进

- 时间范围：2026-09-19 ~
- 状态：**进行中**（§7 即当前活待办）
- 关联：上游 [0018](0018-2026-09-19-安全行为覆盖与cli装配.md)（**已收篇**：安全行为覆盖 28 条 +
  `cli/` 三件 + 真模型端到端 `7 passed`）；判据
  [`sdlc.md`](../engineering/sdlc.md) §3（**`M1` = 需求规格 + 架构设计 + 威胁模型完成**）；
  契约 [`harness.md`](../design/interfaces/harness.md)（**第十一版**）；
  [`ADR-0015`](../adr/0015-layering-and-reuse-boundary.md) §7.2（`S1`~`S3`）、
  [`ADR-0020`](../adr/0020-argument-validator-implementation.md)（已接受）
- 提交：（建篇；**本篇自身哈希由下一篇回填**）
- **承接**：0018 的议题（安全行为覆盖 + `cli/` 装配）已随"端到端转绿"收篇。
  本篇起为**新议题：把 `M1` 的交付物收口，并把"部分缓解"的安全断言逐条推进为可验证状态**。
  0018 §7 的未完成项已抄入本篇 §7（0018 §7 保留原文作为历史）。

---

## 1. 本阶段目标

`Phase 0`/`M0` 与本期"能跑起来"都已达成：`harness/` 9 件、`cli/` 3 件、`tests/security/` 28 条对抗性用例、
真模型端到端 `7 passed`。**下一步不是继续堆功能，而是把 `M1` 的出口条件收口**：

1. **`M1` 的三件交付物**：SRS 成稿（现 v0.1.3，与 `devlog 0004`/`0005` 已分叉）、
   `docs/design/architecture.md` 成稿、**威胁模型成稿**（现 13 条，**0 已缓解并验证 / 8 部分缓解 / 5 未缓解**）；
2. **把"部分缓解"推进为"已缓解并验证"**：逐条给出**可复现证据**，而不是复述缓解措辞；
3. **还清本期欠账**：审计写入失败的可识别类型、声明缺陷与非法参数同形、`audit.md` 的 kind→outcome 无机器检查、
   `§7.1`/`§7.2` 的逐项复核、以及 `cli/` 交付后遗留的文档同步。

---

## 2. 关键决策与理由

（待记）

**本阶段开篇待决的两项（都不阻塞开工，但都有明确的决定者与触发时点）**：

| # | 待决 | 决定者 | 触发时点 |
| --- | --- | --- | --- |
| 1 | 审计落点的 **`A1`/`A2`**：① 是否允许 `ALLOWED_AUDIT_ROOTS` 含额外根（CI 挂载卷 / 演示归档）② 是否移除 `audit.directory` 键 | **所有者** | 未拍板前**按最保守取值实现**（单一根 + 保留键）；已多轮登记，见 §7 |
| 2 | **"不配领域包 ⇒ 非交互 CLI 一个工具都执行不了"**（0018 §3.11(c)，**可用性缺口，非安全缺陷**） | **架构师出提案 + 所有者拍板** | 三个候选：① 无 pack 即拒绝启动 ② CLI 侧风险声明选项 ③ 只读工具的风险分级另议（**③ 属改安全默认 ⇒ 必须 ADR**）。**当前不改产品默认** |

---

## 3. 变更记录

### 3.1 建篇（本笔）

0018 议题完整（安全行为覆盖 28 条 + `cli/` 装配 + 端到端 `7 passed`）⇒ 收篇并承接其 §7 未完成项；
本篇目标 = `M1` 交付物收口 + 安全断言推进 + 还欠账。**验证**：`make check` 全绿（本篇为文档改动）。

### 3.2 承接时的在跑事项（0018 期末，如实结转）

- `architect-wave7`（0018 期末派出，**结果待落**）：`docs/design/threat-model/*` 的"未实现"现状同步
  （硬约束：只改事实性描述，**不得**动 `T-XX` 的状态判定与计数）+ `ADR-0015` 修订记录追加
  **"模块清单依据 = §5.4.1 + `ADR-0020`"** 指针 + 逐项复核契约**第十/十一版**、
  **并补做此前未做的第八版复核**。
- `implementer-e2e-fix`（0018 期末派出）：余一笔 docstring 补写
  （`_RequestTimeoutModel` 要写明"**忽略**调用方传入的 `timeout_s`、一律用 `request_timeout_s`"）。

### 3.3 端到端收口：`slow` 两道锁 + 超时语义的可判定化（`6b011ad` / `a80e5d9`）

承接 0018 §3.11 的三条裁决，该成员补齐两笔后收尾：

- `6b011ad`：`tests/integration/` 的 docstring 按**两道锁**口径改准（门禁侧 `make test`/`test-cov`
  已排除 `slow`；用例侧 `AGENT_SEC_PERF_E2E=1` 保留），理由写明"拦的是**显式**
  `-m integration` 时误起真模型"这条门禁拦不住的路；**且不复制耗时数字**
  （指向 `docs/engineering/testing-strategy.md` §8 作**唯一真源**，避免第二份数字漂移）。
- `a80e5d9`：`_RequestTimeoutModel` 的 docstring **显式**写明"**忽略**调用方传入的 `timeout_s`、
  一律使用 `request_timeout_s`"，并给出两条理由（① `harness/loop.py` 从不传该参数；
  ② **默认形参在语言层面无法区分"没传"与"显式传了 60.0"**，要区分就得把形参改成
  `float | None = None` ⇒ 那是改 `ModelClient` 的签名语义、**属契约变更**）；
  并**加一行断言**把该语义钉住（`chat(..., timeout_s=1.0)` ⇒ 内层收到 `240.0`）
  —— 这是"把隐含语义变成可判定断言"，方向正确。

**我的独立核验（不采信自报）**：两笔的 `--stat` 与自报一致（13/9、22/7）；
`src/` 下 `384` **零命中**、`240.0` **不在** `cli/app.py`（产品默认仍是
`max_completion_tokens=None` / `model_request_timeout_s=60.0` / `model_ready_timeout_s=60.0`）
⇒ 裁决③"取值只留测试侧"**成立**。
它执行的**裁决②**：未动产品默认（`DEFAULT_TOOL_RISK` 保持 `HIGH`），
测试侧只读领域包保留为**产品正规通路**（不是绕过）。

### 3.4 `architect-wave7` **未确认、零产出**（第三次同形）⇒ 重派 `architect-wave8`

**事实**：`send_message` ⇒ `Recipient "architect-wave7" not found. Known members: implementer-e2e-fix`；
**工作树干净**（`git status --short` 为空）；其目标文档**一处未改**——
`threat-model/*` 的多处"未实现"登记、`harness.md` §5.1 第 8 行、以及 `ADR-0015`
修订记录尾部的「**`ADR-0020` 尚为「提议中」**」**全部保持原样** ⇒ 判定**未确认、零产出**
（按 `agent-teams.md` §8.2/§8.9 **不判失败、不追责**）。

**这是本阶段的第三次同形**（波 4 的两名成员、`implementer-integration`、现在的 `architect-wave7`）
⇒ 观察：**"spawn 成功返回"与"成员真的在跑"是两件事**。现有判据（通道可达性 + 仓库产出）够用，
但**任何依赖"成员会自行提交"的期待，都必须配"领导能接手"的兜底**（本阶段靠这条兜底救回过三份产出）。

**重派 `architect-wave8`**，任务写细（逐条列出**已核实为过时**的位置）+ 硬约束：
只改**事实性描述**、**不得**动 `T-XX` 的结论与计数（**0 / 8 / 5**）、
**不得**把"机制已实现"写成"威胁已缓解"（本项目明令区分这两件事）；
**逐条先核实再改**（宁可保留原样并说明，也不要凭印象改）；
另**补做此前从未做的第八版复核**（`I8`/`V4` 的键名澄清，`7721658`）。

⚠️ **一处明确要求"可能保留原样"**：`threat-model/README.md` 的 `T-09`/`T-10` 两行里也有
"机制未实现"字样 —— 那两条**可能仍然成立**（权重/摘要校验、未授权出站的**执行机制**
确实尚无载体）⇒ 已要求它**先核实，不确定就保留**并在 `回报：` 里说明。

### 3.5 `architect-wave8`：文档现状同步 6 笔 + 契约第十二/十三版；其自报有一处不准

**A 类四项**：分支 `develop`；`0564d48..1e993bf`（**6 笔**）；
4 个文件全在 `docs/design/` + `docs/adr/`（`harness.md`、`adr/0015-…md`、
`threat-model/README.md`、`threat-model/assets.md`）；每笔推送前 `make check` 全绿（**922 passed**）。

**我的独立核验（不采信自报）**：逐笔 `--stat` ⇒ **零 `src/`/`tests/` 改动**；
末笔正文含 `回报：` 块；`threat-model/README.md` 第 3 行与汇总表**仍是
「0 已缓解并验证 / 8 部分缓解 / 5 未缓解」**、`T-09`/`T-10` 仍为「**无**」/「未缓解」
⇒ **"不得动 `T-XX` 结论与计数"这条硬约束守住了**；全 `threat-model/` 无
"机制已实现 ⇒ 威胁已缓解"的混淆（`grep 已缓解并验证` 只命中 0 计数处与规则条文本身）。

**落地的同步**：`harness.md` §3.4 / §5.1 第 8 行 / §7 / §7.2 的校验器状态（→ **已实现**）；
`ADR-0015` 修订记录**追加「续四」**（更正"续三"末段过期的「`ADR-0020` 提议中 / 批准前不得开工」，
**只追加、正文与全部决策不改**），并按既有裁决写明「模块清单依据 = §5.4.1 **+** `ADR-0020`」；
`threat-model/*` 的实现状态与 §2.1 / §3 的 mermaid 同步。

**契约第十二版**（实现状态同步）与**第十三版**（**复核第八 / 十 / 十一版 + `T6`：四项均确认、无需更正**）
—— 其中一条有用的联动结论：第八版末句"未声明键名这一面尚无用例"**已解除**
（`tests/security/test_harness_s1c_argument_leak.py` 的 undeclared-key 用例已入库）。

**🔴 一处自报不准（如实记，不追责）**：它声称"超出清单的 3 处相邻同步**已单独成笔**、
可单独 revert"，实际**折在 `b7e3ee9`/`178c265` 之内** ⇒ revert 会连同主同步一起丢。
改动本身**我认可**（"避免文档自相矛盾"且"该项**未关闭**"这个限定词加得对），
但口径要记：**"可单独 revert"只有在真的单独成笔时才可以说**
——与"**声称的能力必须能被实测**"同源。

**追加指令（发给同一成员）**：遗留 ③（`assets.md` 的 `B-3`/`B-5`/`B-7` + §2.1 汇总）、
④（`docs/design/architecture.md` 的 `cli/`"未开工"）⇒ **授权逐处核实后同步**；
**遗留 ②（`T-11` 与 §5 计数）只准出提案** —— `README.md` §4 明写
「**状态变更必须有验证证据**」，故要求它给出**证据映射**（`S1` 的哪几条用例覆盖 `T-11` 缓解的哪一半、
属"已缓解并验证"还是"部分缓解"）与**建议计数**，**裁决前一个数字都不动**；
并明确"**若依据不足，照实说'维持原状'**"。

### 3.6 `architect-wave8` 第二轮（4 笔）与 `P-1` 提案：我的裁决是**暂不动计数**

**核验（不采信自报）**：4 笔全在 `docs/design/`（零 `src/`/`tests/`）；末笔含 `回报：` 块；
**`threat-model/README.md` §4.1 汇总表仍是 0 / 8 / 5**（`T-11` 仍列"未缓解"）、**§5 计数仍是 10**、
`G-7` 与 §0 的 `S1`/`S3` 表述原样 ⇒ **"裁决前一个数字都不动"守住了**；
`P-1` 已作为**新 §8.1** 登记（约第 451 行）；**`B-7` 保留"未实现"**
（它逐行核实后判"**看着像过时但其实是对的**"——这正是我要的动作，比机械同步有价值）。

**我的裁决：`P-1` 保持提案状态，计数一个不动。** 三条理由：

1. `threat-model/README.md` §4 明写「**状态变更必须有验证证据**」；
2. ⚠️ **`P-1` 自陈覆盖面不全**——`T-11` 的**名义范围含"工具滥用"**（已授权工具的载荷层），
   其载体 `test_g8_tool_defense.py` **本提案未评估**。在覆盖面不全时把 §5 计数由 `10` 减到 `9`，
   与"**把'机制已实现'写成'威胁已缓解'**"属**同一族风险**
   （本项目已因"同形 / 夸大"吃过多次教训，见 0016 起各篇）；
3. **安全断言不得由主张者自证**（`CODEBUDDY.md` §10.2 规则 4）——提案是**架构师**写的，
   证据必须由**独立角色从用例源码重新推导**，不得采信提案里的转述。

**处置**：派 `verifier-independent-s1`（独立验证）——
① 从**用例源码**重新推导 `S1`/`S1-b`/`S1-c`/`test_s1_replayable_audit_link.py` 对 `T-11` 的证据映射，
逐条写"**断言了什么 / 没断言什么**"；② **补评估** `T-11` 的"工具滥用"半、`T-12`/`S3`、
`T-02` 的「且留审计」半；③ 明确告知"**依据不足、建议维持原状是合格结论，不丢分**"；
④ 把 `tests/security/test_s1_replayable_audit_link.py` 的过期 docstring
（"`harness/` 尚未开工"、"`S1` 完整语义仍属阻塞"）并入其域（**先核实解除再改**）。
⇒ 等独立结论回来**一次性裁决**计数。

**一条正面样板（值得带走）**：架构师在 `86239f6` 里主动写明
「`cli/app.py` 已落地 ⇒ **串接存在 ≠ 端到端已跑通**」，
**没有让"已落地"顺手变成"已验证"** —— 这是本项目最容易犯的那个混淆。

### 3.7 独立验证回来：`T-11`/`T-12` 可升「部分缓解」，`T-02` **依据不足**（负向发现）

**提交**：`20da806`（`tests/security/test_s1_replayable_audit_link.py` 的 docstring；24/12 行
**全在注释内**，断言与跳过条件**一字未动**——我逐行核过）。该角色**未动任何数字**
（`§4.1` 仍 0/8/5、`§5` 仍 10）⇒ **裁决权与执行权分离**这条守住了。

**三条裁决级结论**（由它从**用例源码**独立推导，**不采信 `P-1` 的转述**）——**我全部采信**：

| 条目 | 它的裁决 | 关键理由（它自己去读出来的） |
| --- | --- | --- |
| `T-11` | **依据充分**（方向正确）⇒ 升**部分缓解** | `S1` 的 5 例 + `test_s1_replayable_audit_link` 的 2 例用**真实 `Session.run`** 断言"deny 时 `Tool.invoke` 未调用、`TOOL_RESULT.result is None`、审计含 `TOOL_CALL/DENY`+`POLICY_DECISION`、`audit_id` 对应 DENY `event_id`"，并有**对照用例**证明拒绝**非恒过** |
| `T-12` | **依据充分** ⇒ 升**部分缓解** | `S3` 的 4 例是**首条行为级证据**（此前只有**结构性**静态守卫）：包内 `.py` 带副作用 ⇒ 抛 `DomainPackError`、**副作用标志文件不存在**、模块**不在 `sys.modules`**；摘掉 `_reject_python_content` 后**原拒绝翻红** |
| `T-02` | **依据不足** ⇒ **维持原状** | `test_audit_landing_whitelist.py` **不构成**该半的证据——它断言的是**审计落点目录白名单**（**另一攻击面**）；`S2` 主体用例（18 例）**自身 docstring 即写明**"拒绝被正确审计记录"一半**无法验证** |

**🟢 本轮最有价值的是那条负向发现**：`T-02` 的"且留审计"半**看起来**有证据、**实际没有**。
文档原本就写"审计半无载体"，但我原先**不知道**存在一个"**看起来像证据、实际是另一攻击面**"
的用例 ⇒ 这正是**"机制已实现"与"威胁已缓解"之间那道最容易被跨过的缝**，
与 `86239f6` 那句"串接存在 ≠ 端到端已跑通"是**同一族**。⇒ 已要求把它**写进 `T-02` 行做防误引登记**
（防的是未来有人把相近用例当证据引用）。

**我的裁决与执行**（派 `architect-wave9` 落盘）：
- `T-11`/`T-12` 升**部分缓解**（**只能**是部分缓解）；数字：`§4.1` → **0 / 10 / 3**
  （未缓解 = `T-03`/`T-04`/`T-10`）、`§5` → **8 条**（"缺行为层验证"）；
  **"已缓解并验证"仍为 0**（不得顺手升到 1）；**历史数字只追加不覆写**。
- **缺口必须写进条目**（不得省略）：`T-11` 的真实 `PolicyEngine` **未被锻炼**（S1 用 `FakePolicyEngine`）、
  "工具滥用"半**零证据**、能力层 deny 的真实 sink **仅经 `RecordingSink`**；
  `T-12` 的 `.pth`/`.so` 等向量未断言、仅覆盖"领域包"这一载体。
- **新开待裁决问题（只框选项、不改设计）**：`T-02` 的"且留审计"半若要闭环，
  **"`resolve_within` 拒绝时由谁 emit 审计记录"**属设计问题
  （`foundation.paths` 持有 sink？调用方 emit？新增一层？），且必须回答
  "**`foundation` 是否该知道 `observability`**"这条分层约束（`ADR-0015`）。

⚠️ **一处操作事实（值得记）**：`architect-wave8` 的 shutdown **已批准**，
但 `task` 工具报 `Member "architect-wave8" already exists in team`
⇒ **`shutdown_response` 的"批准" ≠ 成员已从名册移除**，重派须换名（本轮改用 `architect-wave9`）。

### 3.8 `architect-wave9` 执行裁决：头条数字落为 **0 / 10 / 3**，并纠正类我的一处不准口径

**核验（不采信自报）**：3 笔全在 `docs/design/threat-model/`（`README.md` / `untrusted-input-and-agentic.md` /
`execution-and-isolation.md`）；末笔 `d945b0c` 正文含 `回报：` 块；
`§4.1` 汇总表 = **0 已缓解并验证 / 10 部分缓解（含 `T-11`/`T-12`）/ 3 未缓解（`T-03`/`T-04`/`T-10`）**；
`§5` = **8 条** ⇒ 与我下达的裁决**逐项一致**；"已缓解并验证"**仍为 0**（未被顺手升级）。

**它提的两处事实校正 —— 我独立复核后全部采纳，其中第一处是纠正我**：

1. **我的派活口径过宽**：我写的缺口是"`T-11`/攻击路径 3『策略求值出错却返回 allow』**未断言**"。
   实际 `tests/security/test_policy_eval_failure.py`（4727 B）**存在**，`from …security.policy import PolicyEngine`
   （**真实**引擎，非 fake），断言 `requires_confirmation is True` ⇒ 准确口径是
   "**单元层已断言，仅未经 `Session.run` 端到端断言**"。⇒ ✅ **它没有照抄我的口径**，
   而是先核实再落笔。**这是本项目"派活提示不是权威、仓库才是"这条纪律的一次正向兑现**，
   也是我个人的一次转述不准（记下来：**派活时不要替成员描述它没核过的证据现状**）。
2. **配置面落点白名单的落地现状过期**：`W1`/`W2`/`W4`~`W8` **用例已入库**（`91ea9d5`），
   仅 `W3`（根内符号链接指向根外）与 UNC（第 5 类）仍缺 ⇒ 更正值**未动任何计数**（我核过）。
   ⚠️ **但这两个是真实的安全用例缺口，不是措辞问题**⇒ 已另派验证角色补 `W3`。

**`Q-1` 的框定与我的裁决方向**：它答对了关键一条 —— **`foundation` 不得知道 `observability`，且由机器强制**
（`tests/unit/test_architecture_layers.py` 的 `ALLOWED_DEPENDENCIES["foundation"] == {"contracts"}`，
我核过），并给出三候选 + 各自代价（含"合并拒绝与审计 ⇒ **sink 故障会把路径拒绝升级为会话失败**"、
"`foundation/config.py` 与 `observability/audit.py` 两个调用点**没有会话级 sink**"）。

**我的方向（先取证再定稿）**：**把"设计缺口"与"测试缺口"分开**——
- 若**运行时**路径穿越被拒**已经**产生审计记录（可能 `loop` 的工具失败路径已在 emit）
  ⇒ T-02 的"且留审计"半**只是缺用例**，**不需要新模块、不需要 ADR**；
- 若确实未留痕 ⇒ 在 (ii) 调用方 emit 与 (iii) 新增 `security/path_guard.py` 之间选，**优先 (ii)**（零 ADR）；
- **配置期/装配期**拒绝：倾向**明确划出范围**——那时**会话与 sink 都不存在**，
  要在"尚未校验落点白名单"的阶段决定往哪写审计，会与**攻击面 6**（落点白名单）**循环**；
  ⇒ 倾向"**fail-closed、无审计，且理由写明**"，而非当成一个洞。**但这是范围决策，我会带上三候选交所有者。**
⇒ 已派 `verifier-t02-audit` 取证（读失败路径 + **真实 `JsonlAuditSink`** 端到端跑一次，
并明确"**只认真实 sink**，只有 `RecordingSink` 能观察到**不算**已留痕"）；同时补 `W3` 用例。

### 3.9 🔴 一次**声明失真**被推翻：`T-02` 的"审计半无载体"是错的 —— 而且它已经造成了一次错误裁决

**推翻它的证据（我独立复核后确认）**：

- **载体一直都在**：`tools/files.py:121-129` 的 `_fail` → `tools/registry.py:179-211` 的 `audit_tool_call`
  → `:211` 的 `sink.emit`；`observability/audit.py` 真实落盘；`harness/loop.py` 不重复 emit，
  只把 `result.audit_id` 回填进 `TOOL_RESULT`。
- **证据早已存在**：`tests/security/test_s1_replayable_audit_link.py:85`
  的 `test_denied_tool_call_is_audited_and_replayable`（**真实 `JsonlAuditSink` + 真实 `ReadFileTool`**，
  断言落盘后 `query_by_id` 可还原）。
- **本轮新增**：`test_t02_path_traversal_audit.py`（`4a35c61`，含变异探针：摘掉 `emit` 主用例必红）、
  `W3` 用例（`9b37e99`，配置期 + 装配期两层 + 专属变异探针）。

**错误的传播链（如实记，这是本阶段最值得带走的一条）**：

1. **源头**：`tests/security/test_path_traversal_rejected.py` 的 docstring 写
   「`AuditSink` 尚未落地、无法验证」⇒ **假声明**（已由验证角色在 `b79f383` 修正）。
2. **第一次独立验证**：把上面那条**既有**用例判为
   "工具层路径越权、**非能力层 deny ⇒ 不算该半证据**"——该判据对 `T-11`（能力授权）**成立**，
   但 **`T-02` 本身就是工具层路径穿越** ⇒ **口径被跨条目错误套用**。
3. **我**采信后裁决"`T-02` 依据不足、维持原状"，**架构师**据此把"审计半无载体"
   写进 `§4.1` 行与条目 ⇒ **一句假声明最终升格为正式文档**（`ed28085` / `d945b0c`）。

⇒ **两条纪律（本阶段第二次犯同族，见 §3.8 校正①）**：
① **"依据不足 / 依据充分"这类结论必须写明它是在哪个口径下判的**，
   否则跨条目复用必然失真（本轮失真点：T-11 的"能力层 deny"口径套到了 T-02 的"工具层路径"上）；
② **派活提示里不要替成员描述它没核过的证据现状**——提示是"任务与边界"，**不是事实源**。
⚠️ **这两条的价值在于：本轮是"成员**没有顺从领导的错误口径**"才纠回来的**（`verifier-t02-audit`
把任务背景里那句错误前提直接推翻）。⇒ 与既有口径一致：**完成判定与决策依据都只认仓库**。

**处置**：已派 `architect-wave10` ① 更正 `T-02` 的"审计半"表述（载体与证据都写明；
**保留并精化**那条件防误引登记——`test_audit_landing_whitelist.py` 仍**不是**该半证据，
但**必须补写"该半的证据在别处"**，否则读者会以为仍无证据，**这正是本轮犯错的形状**）；
② 把这次**假声明事故**登记进威胁模型的变更记录（写明它**实际造成了什么**，
**不得**写成"措辞不当"）；③ 产出**提案 `P-2`**：`T-02` 是否可升「**已缓解并验证**」（**将是项目首次**）；
④ 新开 `Q-2`：拒绝事件的 `outcome` 是 `ERROR`（靠 `detail.reason` 区分）还是应有专用 `DENY`。

⚠️ **数字一律不动**（`§4.1` 保持 **0 / 10 / 3**、`§5` 保持 **8**、"已缓解并验证"保持 **0**）
—— 把项目最保守的那个核心指标从 0 变成 1，**交所有者裁决**，不由我或验证角色单方面决定。

**另记一处"不为凑数写弱断言"的正面处置**：`UNC`（落点白名单第 5 类）经实测在 Linux 下
`pathlib` 把前导 `//` 折叠为单 `/` ⇒ **不构成独立绕过面** ⇒ 验证角色**登记为缺口、拒绝补弱用例**
（而非写一条恒过用例充数）。这与本项目"**恒过的断言比没有断言更坏**"的取向一致。

### 3.10 `architect-wave10`：更正落地 + 提案 `P-2`；`Q-2` 由我裁决并关闭

**核验（不采信自报）**：5 笔（`382be98..fc94581`）全在 `docs/design/threat-model/`；
计数仍 **0 / 10 / 3**、`§5` 仍 **8**、"已缓解并验证"仍 **0**；末笔正文含 `回报：` 块。
`T-02` 行已改写为「**载体已实现 + 已有两处行为层证据**」，且**同一行保留了
"原『审计半无载体』表述有误"的更正说明**——**做的是"更正"而不是"抹掉"**（读者能看出这里曾经错过，
这点比改对本身更重要）；精化后的防误引登记**补写了"该半的证据在别处"**（正是本轮犯错的形状）；
`§7` 新增**假声明事故登记**；`§8.2` 新增**提案 `P-2`** 与**问题 `Q-2`**。

**`Q-2` 由我裁决并关闭（不需要改契约）：维持 `outcome=ERROR` + `detail.reason="path_not_allowed"`。**
三条理由：① 改专用 `DENY` 要动 `audit.md` §2.2 的 `D3`（工具层只产 `OK`/`ERROR`）
+ 下游消费者 + 既有用例；② **更要紧的是语义同形**——`DENY` 在契约里绑定
"**策略/审批**导致未执行"，把"**工具层自身拒绝**"并进去，会让**原因完全不同**的两类拒绝在审计里
**同形**（本项目明令避免的老坑）；③ 该区分**已被机器钉住**
（`test_t02_path_traversal_audit.py` 断言 `detail.reason == "path_not_allowed"`）⇒ 不再靠纪律。

**`Q-1` 与 `P-2` 的逻辑依赖（我整理的裁决顺序，两项一起交所有者）**：
`P-2` 的缺口 (c) 已随 `Q-2` 关闭、(a) 随 `W1`~`W8` 全落地（`W3` 本轮补、UNC 判不适用）
⇒ **`P-2` 的成立与否实际取决于 `Q-1`**：

- 若 `Q-1` 判「**配置期 / 装配期不纳入审计范围**」⇒ (a)~(c) 均不阻塞
  ⇒ `T-02` **可升「已缓解并验证」**（**项目首次**）；
- 若判「应纳入」⇒ `T-02` **维持部分缓解**，但理由换成"**非工具层调用点未进审计**"。

⇒ **这两项必须一起交所有者**（先定 `Q-1`，`P-2` 才有答案）。
⚠️ 我不单方面把项目最保守的核心指标从 0 变 1；且按 `§8.2` 的自述——
「**建议 ≠ 结论**，不得据 `P-2` 的建议改状态或计数」——执行。

**一条方法上的正面记录**：`Q-2` 我能**直接裁**，是因为架构师把
"现口径（`D1`/`D2`/`D3`）+ 两选项各自代价 + 影响面"**列全了**；
如果它只写"请示是否改成 DENY"，我就只能反问一轮。
⇒ **"只框定选项与代价"的写法，价值就在于让裁决方能一次裁掉。**

---

## 4. 数据与证据

（待记）

---

## 5. 问题与卡点

- 🔴 **威胁模型 13 条里 0 条"已缓解并验证"** ⇒ 任何对外表述**不得**出现"安全已到位"。
  本阶段的核心工作就是把这个数字往下推（**用证据**，不是用措辞）。
- ⚠️ **`M1` 的 SRS 与设计文档存在分叉**：SRS v0.1.3 与 `devlog 0004`/`0005` 的讨论结果不一致
  （`S-1`~`S-10` / `Q-1`~`Q-8` **待所有者确认**）⇒ 在确认前，`REQ-*` 与
  `docs/design/interfaces/` 的一致性核查只能做到"**能核的先核**"。
- ⚠️ **可用性缺口的措辞纪律**：`HIGH` + `R1` 拒绝是**刻意的 fail-secure**，
  它的后果（非交互 + 无 pack ⇒ 工具不可执行）是**可用性问题**——登记与提案时**必须这么表述**，
  不得写成"安全缺陷"（否则会被误读成需要下调默认风险等级）。

---

## 6. 学到了什么

（待记）

---

## 7. 下一步（= 当前活待办清单）

> ⚠️ **新会话从这里开始读**（「重拾语境」第 4 步）。承接自 [0018](0018-2026-09-19-安全行为覆盖与cli装配.md)
> §7 的**未完成项**（旧篇保留原文，此处是**唯一有效**的清单）。

### 🔴 优先级 H：本阶段主线

**对账第一件事（不要凭本文件下结论，看仓库）**：

```text
git log --oneline -15 && git status --short
uv run pytest -n auto -m "not benchmark and not slow" -q   # 建篇时基线：922 passed（slow 已排除）
# 对每名成员的最后一笔：
#   git show --stat <hash>
#   git show -s --format=%B <hash> | tail -20     ⇒ 末尾有没有 `回报：` 块
#   没有 ⇒ 状态只能记「未确认」（**不得判失败、不得据此重派**）
```

- [x] **结转项 1：`implementer-e2e-fix` 已完成并 shutdown**（三笔 `2d011ad` / `b1fc35a` / `6b011ad` /
      `a80e5d9`；真模型端到端 **7 passed / 273.47 s**；见 §3.3）
- [ ] **结转项 2：`architect-wave7` 未确认、零产出** ⇒ 已重派 **`architect-wave8`**
      （与 `architect-wave7` 同任务，写得更细：逐条列出过时位置 + 只改事实描述 + 不动 `T-XX`
      结论与计数 + **补做第八版复核**）—— 见 §3.4。⚠️ 它交出的东西**只认仓库**，
      收到 `回报：` 块前**不得**认为已完成
- [x] **`architect-wave8` 第一轮 6 笔已核验**（`0564d48..1e993bf`；见 §3.5）：契约**第十二/十三版**落地、
      `ADR-0015` 追加「续四」、`threat-model/*` 实现状态同步 —— **`T-XX` 计数 0/8/5 与 `T-09`/`T-10` 原样**
- [x] **`architect-wave8` 第二轮 4 笔已核验**（`1e993bf..5ae3628`；见 §3.6）：遗留 ③（`assets.md` 的
      `B-3`/`B-5` → 已实现、**`B-7` 保留"未实现"**）、④（`architecture.md` 的 `cli/` 与其余实现状态）；
      **计数一个未动**、**`P-1` 已登记为新 §8.1** ⇒ 该成员已 shutdown
- [x] **`P-1` 已裁决**（独立验证 `20da806` + 我的决定；见 §3.7）：`T-11`/`T-12` 升**部分缓解**、
      `§4.1` → `0 / 10 / 3`、`§5` → **8 条**；**`T-02` 维持原状**（负向发现：
      `test_audit_landing_whitelist.py` **不构成**其"且留审计"半的证据）⇒ 已派 `architect-wave9` 落盘
- [x] **`architect-wave9` 3 笔已核验**（`ed28085..d945b0c`；见 §3.8）：头条数字落为 **0 / 10 / 3**、
      `§5` **8 条**、缺口逐字写入条目、`T-02` 防误引登记、`§8` 的 `P-1` 标"已裁决" ⇒ 该成员已 shutdown
- [x] **`tests/security/test_s1_replayable_audit_link.py` 的过期 docstring** 已同步（`20da806`：
      两条阻塞已解除，并写清本文件与 `S1` 授权文件**互补**；断言与跳过条件一字未动）
- [x] **`verifier-t02-audit` 3 笔已核验**（`c6b32f7..b79f383`）：取证结论是"**运行时已被留痕**"
      ⇒ **推翻**了"`T-02` 审计半无载体"这一**已写进仓库**的说法（见 §3.9）；
      并补 `W3` 用例、`UNC` 判为不构成独立绕过面（**登记为缺口、不写弱断言**）⇒ 该成员已 shutdown
- [x] **`architect-wave10` 5 笔已核验**（`382be98..fc94581`；见 §3.10）：`T-02` 更正为
      "载体已实现 + 两处行为层证据"（**保留**更正说明，未静默改口）、`§7` 登记假声明事故、
      `§8.2` 新增 `P-2`/`Q-2`；**计数仍 0/10/3、§5 仍 8** ⇒ 该成员已 shutdown
- [x] **`Q-2` 已裁决并关闭**（我）：**维持 `outcome=ERROR` + `detail.reason="path_not_allowed"`**
      —— 改 `DENY` 会与"策略/审批导致未执行"**同形**（原因不同的两类拒绝在审计里不可分）；
      该区分已由 `test_t02_path_traversal_audit.py` 机器钉住。详见 §3.10
- [ ] **待所有者裁决（两项必须一起定，先 `Q-1` 后 `P-2`）**：
      ① **`Q-1` 的范围面**：审计是否覆盖**配置期/装配期**拒绝 —— 我倾向"**fail-closed、无审计、
      理由写明**"（那时会话与 sink 都不存在，要在未校验落点白名单的阶段决定往哪写 ⇒ 与**攻击面 6 循环**）；
      ② **`P-2`**：`T-02` 是否升「**已缓解并验证**」（**项目首次** ⇒ 核心指标 0 → 1）。
      ⇒ 若 `Q-1` 判"不纳入"则 (a)~(c) 不阻塞 ⇒ **可升**；若判"应纳入"⇒ **维持部分缓解**（理由换成
      "非工具层调用点未进审计"）。⚠️ 按 `§8.2` 自述"**建议 ≠ 结论**"，我不单方面改数字
- [ ] **`T-02` 的"且留审计"半若确需改动分层**（`Q-1` 的 (iii) 新增 `security/path_guard.py`）
      ⇒ **须 ADR**，不得由实现者自行开工；**优先 (ii) 调用方 emit**（零 ADR）
      —— ⚠️ 但 §3.9 已证明**运行时路径根本不需要改分层**（载体已在工具层）
- [x] **`T-09`/`T-10` 保留原样**（已核实仍成立：权重/二进制摘要校验、未授权出站的**执行机制**均无载体）
- [ ] **`cli/` 交付后遗留的文档同步**（0018 §3.6(e) 登记，**不得抢跑**）：
      `docs/design/architecture.md` 的 §0"未开工"清单、§1.1 的 cli mermaid 节点、§4.3 的 `cli/` 行
      —— 现在 `cli/` 已交付 ⇒ **可以改**（改完 `cli/` 就不在"未开工"里了）
- [ ] **【交架构师出提案】"不配领域包 ⇒ 非交互 CLI 一个工具都执行不了"**（0018 §3.11(c)）：
      三候选见 §2 第 2 行；**③ 属改安全默认 ⇒ 必须 ADR**
- [ ] **【架构师可裁】模型请求超时的落点**（0018 §3.11(e) 第 1 条）：现为 CLI 侧注入层；
      替代 = 给 `LocalLlamaClient` 加 `request_timeout_s`，或给 `SessionConfig` 加字段（**属契约变更**）
- [ ] **`§7.1`（7 项）与 `§7.2`（4 项）逐项复核**：均有落点，但**复核 ≠ 看它"看起来落地了"**
- [x] **收尾**：`shutdown_request` 给全部成员（**6/6 均已批准**）→ `team_delete`（**用完即删**，不回头删旧团队）
      ⇒ 团队 `phase1-harness-wave5` 已删除；收尾时核对 `HEAD == origin/develop == bd05859`、工作树干净、
      **927 passed / 7 skipped**、本波共 **29 笔**提交
      ⚠️ **一条操作事实**：`shutdown_response` 的"批准"**不等于**成员已从名册移除
      （本轮实测：`architect-wave8` 已批准关闭后重派仍报 `already exists`，只能换名）

### 🔴 优先级 A：把安全断言推进为"已缓解并验证"

- [ ] **威胁模型 `T-01`~`T-13` 的独立复核**：逐条给出**可复现证据**，把"部分缓解 / 未缓解"
      推进为**可验证状态**（现状 **0 / 8 / 5**）；**不得**用"已实现某机制"替代"该机制被对抗性用例证明有效"
- [ ] **【下轮修】审计写入失败缺"可识别类型"**（0018 §3.8(d)）：审批路径上它被 `loop` 的 `R3`
      收敛为 `ERROR(INTERNAL)`+`FAILED`，与 `audit.md` §2.4"必须冒泡"不完全一致；
      缺口边界 = "**退出码正确、事件流响亮，但 `text`/`error_kind` 不可分**"
      ⇒ 修法：`AuditWriteError`（带 `__cause__`）+ `loop` 在 `R3` 前 `except …: raise`；
      **属契约变更 ⇒ 须架构师 + 验证者参与**
- [ ] **【新登记】"工具参数声明缺陷"与"模型给的参数不合法"在审计里同形**：契约 §2.7 的
      `denied_reason` 是**闭集**、没有"声明缺陷"一档 ⇒ 候选：加第六档（如 `schema_defect`）
      或只把"声明缺陷"写进审计 `detail` 而不动 `denied_reason`；**属契约变更，须架构师/领导裁决**
- [ ] **`audit.md` §2.2 的 kind→outcome 约束表没有机器检查**（已证实全仓无此类断言）
      ⇒ 会被静默违反。候选处置：`observability/audit.py` 的 `emit` 前校验，或加一条扫描该表的单测
- [ ] **`S1-c` 的日志面当前"空通过"**（0018 §3.8(a) 的遗留）：`caplog` 检查通过是因为 harness
      目前**不记录参数原文**——**不得**把它读成"日志面已验证"；将来若新增参数日志须补强

### 优先级 B：`M1` 的交付物（`Phase 1` 出口）

- [ ] **SRS v0.2 收敛**：与 `devlog 0004`/`0005` 的分叉（`S-1`~`S-10` / `Q-1`~`Q-8`）**待所有者确认**；
      `REQ-*` 与 `docs/design/interfaces/` 的一致性一并核
- [ ] **威胁模型成稿**（含"部分缓解 ⇒ 已缓解并验证"的推进路径与证据）
- [ ] **`architecture.md` §11 的 `G-1`/`G-2` 决策时点**：`G-1`（硬件探测无落点）随
      `REQ-PERF-05/06` 实现时定模块名；`G-2`（`D2` 的 pydantic 两处用途）**已由
      `ADR-0020` 处置为"两处均不落地"** ⇒ 只剩把它同步进 `architecture.md` 的 `G-2` 行
- [ ] **测试计划复核**（`testing-strategy.md` 的四层是否与实际落位一致；`slow` 口径已于
      `0b5def7` 同步）

### 优先级 C：欠债与收尾

- [ ] **`REQ-UX-03` / `REQ-OPS-04` 的实现与验收**：`cli/` 与 `[project.scripts]` 入口已落地
      ⇒ **可以开始核验收标准**（`D7` 的构建后端阻塞早已解除）
- [ ] **【需所有者拍板】审计落点的 `A1` / `A2`**（`architecture.md` §11 的 `G-5`）：
      ① 是否允许 `ALLOWED_AUDIT_ROOTS` 含额外根；② 是否移除 `audit.directory` 键。
      **未拍板前按最保守取值实现**（单一根 + 保留键），且 `P1`~`P7` 与 `W1`~`W8` **不随这两项变化**
- [ ] **`git-workflow.md` §4 的措辞量纲统一**（"5 处" vs "三者"，不影响分级集合）
- [ ] 镜像重建后核 `V-15`/`U11`、`U7`~`U12` 继续核验；
      `T3`（令牌轮换）是**所有者动作，代理不得代办**
- [ ] `agent-teams.md` §7 的三个未验证项、`S-8` 系列的"写了但没跑"扫描
- [ ] PR #6 的合并后复核（可选，低优先）：核对 `main` 上的 CI 结果与 PR 页面 `merged` 状态

### 已关闭（供回溯，不再维护）

- [x] **安全行为覆盖**（`tests/security/` **28 条**，验证角色独立完成，含 `S1`/`S1-b`/`S1-c`/`S3`/`S-new-1`~`6`）
- [x] **`cli/` 三件齐备**（`render` / `approval` / `app` + `[project.scripts]` 入口，`REQ-UX-03`）
- [x] **真模型端到端 `7 passed`**（273.47 s；`AGENT_SEC_PERF_E2E=1` 才跑）与 **`slow` 并入默认排除集**（F 类，所有者批准）
- [x] `ADR-0020` 获批准并落地（手写 JSON-Schema 子集校验器）+ `T6` 裁决（`specs()` = 全量）
- [x] harness 基线 9 件 + 契约收敛到第十一版 —— 见 0017 §3.7~§3.10、0018 §3.1~§3.11
