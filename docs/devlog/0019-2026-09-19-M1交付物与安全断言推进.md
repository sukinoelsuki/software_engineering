# 0019. Phase 1 收口：`M1` 交付物与安全断言推进

- 时间范围：2026-09-19 ~
- 状态：**进行中**（§7 即当前活待办）
- 关联：上游 [0018](0018-2026-09-19-安全行为覆盖与cli装配.md)（**已收篇**：安全行为覆盖 28 条 +
  `cli/` 三件 + 真模型端到端 `7 passed`）；判据
  [`sdlc.md`](../engineering/sdlc.md) §3（**`M1` = 需求规格 + 架构设计 + 威胁模型完成**）；
  契约 [`harness.md`](../design/interfaces/harness.md)（**第十一版**）；
  [`ADR-0015`](../adr/0015-layering-and-reuse-boundary.md) §7.2（`S1`~`S3`）、
  [`ADR-0020`](../adr/0020-argument-validator-implementation.md)（已接受）
- 提交：`bd05859`（§3.10 记录）、`6620772`（收尾团队并核对最终状态）；
  （**本篇自身哈希由下一篇回填**）
- **承接**：0018 的议题（安全行为覆盖 + `cli/` 装配）已随"端到端转绿"收篇。
  本篇起为**新议题：把 `M1` 的交付物收口，并把"部分缓解"的安全断言逐条推进为可验证状态**。
  0018 §7 的未完成项已抄入本篇 §7（0018 §7 保留原文作为历史）。

---

## 1. 本阶段目标

`Phase 0`/`M0` 与本期"能跑起来"都已达成：`harness/` 9 件、`cli/` 3 件、`tests/security/` 28 条对抗性用例、
真模型端到端 `7 passed`。**下一步不是继续堆功能，而是把 `M1` 的出口条件收口**：

1. **`M1` 的三件交付物**：SRS 成稿（现 v0.1.3，与 `devlog 0004`/`0005` 已分叉）、
   `docs/design/architecture.md` 成稿、**威胁模型成稿**（现 13 条，**0 已缓解并验证 / 8 部分缓解 / 5 未缓解**）；
2. **把"部分缓解"推进为"已缓解并验证"**：逐条给出**可复现证据**，而不是复述缓解措辞；
3. **还清本期欠账**：审计写入失败的可识别类型、声明缺陷与非法参数同形、`audit.md` 的 kind→outcome 无机器检查、
   `§7.1`/`§7.2` 的逐项复核、以及 `cli/` 交付后遗留的文档同步。

---

## 2. 关键决策与理由

（待记）

**本阶段开篇待决的两项（都不阻塞开工，但都有明确的决定者与触发时点）**：

| # | 待决 | 决定者 | 触发时点 |
| --- | --- | --- | --- |
| 1 | 审计落点的 **`A1`/`A2`**：① 是否允许 `ALLOWED_AUDIT_ROOTS` 含额外根（CI 挂载卷 / 演示归档）② 是否移除 `audit.directory` 键 | **所有者** | 未拍板前**按最保守取值实现**（单一根 + 保留键）；已多轮登记，见 §7 |
| 2 | **"不配领域包 ⇒ 非交互 CLI 一个工具都执行不了"**（0018 §3.11(c)，**可用性缺口，非安全缺陷**） | **架构师出提案 + 所有者拍板** | 三个候选：① 无 pack 即拒绝启动 ② CLI 侧风险声明选项 ③ 只读工具的风险分级另议（**③ 属改安全默认 ⇒ 必须 ADR**）。**当前不改产品默认** |

---

## 3. 变更记录

### 3.1 建篇（本笔）

0018 议题完整（安全行为覆盖 28 条 + `cli/` 装配 + 端到端 `7 passed`）⇒ 收篇并承接其 §7 未完成项；
本篇目标 = `M1` 交付物收口 + 安全断言推进 + 还欠账。**验证**：`make check` 全绿（本篇为文档改动）。

### 3.2 承接时的在跑事项（0018 期末，如实结转）

- `architect-wave7`（0018 期末派出，**结果待落**）：`docs/design/threat-model/*` 的"未实现"现状同步
  （硬约束：只改事实性描述，**不得**动 `T-XX` 的状态判定与计数）+ `ADR-0015` 修订记录追加
  **"模块清单依据 = §5.4.1 + `ADR-0020`"** 指针 + 逐项复核契约**第十/十一版**、
  **并补做此前未做的第八版复核**。
- `implementer-e2e-fix`（0018 期末派出）：余一笔 docstring 补写
  （`_RequestTimeoutModel` 要写明"**忽略**调用方传入的 `timeout_s`、一律用 `request_timeout_s`"）。

### 3.3 端到端收口：`slow` 两道锁 + 超时语义的可判定化（`6b011ad` / `a80e5d9`）

承接 0018 §3.11 的三条裁决，该成员补齐两笔后收尾：

- `6b011ad`：`tests/integration/` 的 docstring 按**两道锁**口径改准（门禁侧 `make test`/`test-cov`
  已排除 `slow`；用例侧 `AGENT_SEC_PERF_E2E=1` 保留），理由写明"拦的是**显式**
  `-m integration` 时误起真模型"这条门禁拦不住的路；**且不复制耗时数字**
  （指向 `docs/engineering/testing-strategy.md` §8 作**唯一真源**，避免第二份数字漂移）。
- `a80e5d9`：`_RequestTimeoutModel` 的 docstring **显式**写明"**忽略**调用方传入的 `timeout_s`、
  一律使用 `request_timeout_s`"，并给出两条理由（① `harness/loop.py` 从不传该参数；
  ② **默认形参在语言层面无法区分"没传"与"显式传了 60.0"**，要区分就得把形参改成
  `float | None = None` ⇒ 那是改 `ModelClient` 的签名语义、**属契约变更**）；
  并**加一行断言**把该语义钉住（`chat(..., timeout_s=1.0)` ⇒ 内层收到 `240.0`）
  —— 这是"把隐含语义变成可判定断言"，方向正确。

**我的独立核验（不采信自报）**：两笔的 `--stat` 与自报一致（13/9、22/7）；
`src/` 下 `384` **零命中**、`240.0` **不在** `cli/app.py`（产品默认仍是
`max_completion_tokens=None` / `model_request_timeout_s=60.0` / `model_ready_timeout_s=60.0`）
⇒ 裁决③"取值只留测试侧"**成立**。
它执行的**裁决②**：未动产品默认（`DEFAULT_TOOL_RISK` 保持 `HIGH`），
测试侧只读领域包保留为**产品正规通路**（不是绕过）。

### 3.4 `architect-wave7` **未确认、零产出**（第三次同形）⇒ 重派 `architect-wave8`

**事实**：`send_message` ⇒ `Recipient "architect-wave7" not found. Known members: implementer-e2e-fix`；
**工作树干净**（`git status --short` 为空）；其目标文档**一处未改**——
`threat-model/*` 的多处"未实现"登记、`harness.md` §5.1 第 8 行、以及 `ADR-0015`
修订记录尾部的「**`ADR-0020` 尚为「提议中」**」**全部保持原样** ⇒ 判定**未确认、零产出**
（按 `agent-teams.md` §8.2/§8.9 **不判失败、不追责**）。

**这是本阶段的第三次同形**（波 4 的两名成员、`implementer-integration`、现在的 `architect-wave7`）
⇒ 观察：**"spawn 成功返回"与"成员真的在跑"是两件事**。现有判据（通道可达性 + 仓库产出）够用，
但**任何依赖"成员会自行提交"的期待，都必须配"领导能接手"的兜底**（本阶段靠这条兜底救回过三份产出）。

**重派 `architect-wave8`**，任务写细（逐条列出**已核实为过时**的位置）+ 硬约束：
只改**事实性描述**、**不得**动 `T-XX` 的结论与计数（**0 / 8 / 5**）、
**不得**把"机制已实现"写成"威胁已缓解"（本项目明令区分这两件事）；
**逐条先核实再改**（宁可保留原样并说明，也不要凭印象改）；
另**补做此前从未做的第八版复核**（`I8`/`V4` 的键名澄清，`7721658`）。

⚠️ **一处明确要求"可能保留原样"**：`threat-model/README.md` 的 `T-09`/`T-10` 两行里也有
"机制未实现"字样 —— 那两条**可能仍然成立**（权重/摘要校验、未授权出站的**执行机制**
确实尚无载体）⇒ 已要求它**先核实，不确定就保留**并在 `回报：` 里说明。

### 3.5 `architect-wave8`：文档现状同步 6 笔 + 契约第十二/十三版；其自报有一处不准

**A 类四项**：分支 `develop`；`0564d48..1e993bf`（**6 笔**）；
4 个文件全在 `docs/design/` + `docs/adr/`（`harness.md`、`adr/0015-…md`、
`threat-model/README.md`、`threat-model/assets.md`）；每笔推送前 `make check` 全绿（**922 passed**）。

**我的独立核验（不采信自报）**：逐笔 `--stat` ⇒ **零 `src/`/`tests/` 改动**；
末笔正文含 `回报：` 块；`threat-model/README.md` 第 3 行与汇总表**仍是
「0 已缓解并验证 / 8 部分缓解 / 5 未缓解」**、`T-09`/`T-10` 仍为「**无**」/「未缓解」
⇒ **"不得动 `T-XX` 结论与计数"这条硬约束守住了**；全 `threat-model/` 无
"机制已实现 ⇒ 威胁已缓解"的混淆（`grep 已缓解并验证` 只命中 0 计数处与规则条文本身）。

**落地的同步**：`harness.md` §3.4 / §5.1 第 8 行 / §7 / §7.2 的校验器状态（→ **已实现**）；
`ADR-0015` 修订记录**追加「续四」**（更正"续三"末段过期的「`ADR-0020` 提议中 / 批准前不得开工」，
**只追加、正文与全部决策不改**），并按既有裁决写明「模块清单依据 = §5.4.1 **+** `ADR-0020`」；
`threat-model/*` 的实现状态与 §2.1 / §3 的 mermaid 同步。

**契约第十二版**（实现状态同步）与**第十三版**（**复核第八 / 十 / 十一版 + `T6`：四项均确认、无需更正**）
—— 其中一条有用的联动结论：第八版末句"未声明键名这一面尚无用例"**已解除**
（`tests/security/test_harness_s1c_argument_leak.py` 的 undeclared-key 用例已入库）。

**🔴 一处自报不准（如实记，不追责）**：它声称"超出清单的 3 处相邻同步**已单独成笔**、
可单独 revert"，实际**折在 `b7e3ee9`/`178c265` 之内** ⇒ revert 会连同主同步一起丢。
改动本身**我认可**（"避免文档自相矛盾"且"该项**未关闭**"这个限定词加得对），
但口径要记：**"可单独 revert"只有在真的单独成笔时才可以说**
——与"**声称的能力必须能被实测**"同源。

**追加指令（发给同一成员）**：遗留 ③（`assets.md` 的 `B-3`/`B-5`/`B-7` + §2.1 汇总）、
④（`docs/design/architecture.md` 的 `cli/`"未开工"）⇒ **授权逐处核实后同步**；
**遗留 ②（`T-11` 与 §5 计数）只准出提案** —— `README.md` §4 明写
「**状态变更必须有验证证据**」，故要求它给出**证据映射**（`S1` 的哪几条用例覆盖 `T-11` 缓解的哪一半、
属"已缓解并验证"还是"部分缓解"）与**建议计数**，**裁决前一个数字都不动**；
并明确"**若依据不足，照实说'维持原状'**"。

### 3.6 `architect-wave8` 第二轮（4 笔）与 `P-1` 提案：我的裁决是**暂不动计数**

**核验（不采信自报）**：4 笔全在 `docs/design/`（零 `src/`/`tests/`）；末笔含 `回报：` 块；
**`threat-model/README.md` §4.1 汇总表仍是 0 / 8 / 5**（`T-11` 仍列"未缓解"）、**§5 计数仍是 10**、
`G-7` 与 §0 的 `S1`/`S3` 表述原样 ⇒ **"裁决前一个数字都不动"守住了**；
`P-1` 已作为**新 §8.1** 登记（约第 451 行）；**`B-7` 保留"未实现"**
（它逐行核实后判"**看着像过时但其实是对的**"——这正是我要的动作，比机械同步有价值）。

**我的裁决：`P-1` 保持提案状态，计数一个不动。** 三条理由：

1. `threat-model/README.md` §4 明写「**状态变更必须有验证证据**」；
2. ⚠️ **`P-1` 自陈覆盖面不全**——`T-11` 的**名义范围含"工具滥用"**（已授权工具的载荷层），
   其载体 `test_g8_tool_defense.py` **本提案未评估**。在覆盖面不全时把 §5 计数由 `10` 减到 `9`，
   与"**把'机制已实现'写成'威胁已缓解'**"属**同一族风险**
   （本项目已因"同形 / 夸大"吃过多次教训，见 0016 起各篇）；
3. **安全断言不得由主张者自证**（`CODEBUDDY.md` §10.2 规则 4）——提案是**架构师**写的，
   证据必须由**独立角色从用例源码重新推导**，不得采信提案里的转述。

**处置**：派 `verifier-independent-s1`（独立验证）——
① 从**用例源码**重新推导 `S1`/`S1-b`/`S1-c`/`test_s1_replayable_audit_link.py` 对 `T-11` 的证据映射，
逐条写"**断言了什么 / 没断言什么**"；② **补评估** `T-11` 的"工具滥用"半、`T-12`/`S3`、
`T-02` 的「且留审计」半；③ 明确告知"**依据不足、建议维持原状是合格结论，不丢分**"；
④ 把 `tests/security/test_s1_replayable_audit_link.py` 的过期 docstring
（"`harness/` 尚未开工"、"`S1` 完整语义仍属阻塞"）并入其域（**先核实解除再改**）。
⇒ 等独立结论回来**一次性裁决**计数。

**一条正面样板（值得带走）**：架构师在 `86239f6` 里主动写明
「`cli/app.py` 已落地 ⇒ **串接存在 ≠ 端到端已跑通**」，
**没有让"已落地"顺手变成"已验证"** —— 这是本项目最容易犯的那个混淆。

### 3.7 独立验证回来：`T-11`/`T-12` 可升「部分缓解」，`T-02` **依据不足**（负向发现）

**提交**：`20da806`（`tests/security/test_s1_replayable_audit_link.py` 的 docstring；24/12 行
**全在注释内**，断言与跳过条件**一字未动**——我逐行核过）。该角色**未动任何数字**
（`§4.1` 仍 0/8/5、`§5` 仍 10）⇒ **裁决权与执行权分离**这条守住了。

**三条裁决级结论**（由它从**用例源码**独立推导，**不采信 `P-1` 的转述**）——**我全部采信**：

| 条目 | 它的裁决 | 关键理由（它自己去读出来的） |
| --- | --- | --- |
| `T-11` | **依据充分**（方向正确）⇒ 升**部分缓解** | `S1` 的 5 例 + `test_s1_replayable_audit_link` 的 2 例用**真实 `Session.run`** 断言"deny 时 `Tool.invoke` 未调用、`TOOL_RESULT.result is None`、审计含 `TOOL_CALL/DENY`+`POLICY_DECISION`、`audit_id` 对应 DENY `event_id`"，并有**对照用例**证明拒绝**非恒过** |
| `T-12` | **依据充分** ⇒ 升**部分缓解** | `S3` 的 4 例是**首条行为级证据**（此前只有**结构性**静态守卫）：包内 `.py` 带副作用 ⇒ 抛 `DomainPackError`、**副作用标志文件不存在**、模块**不在 `sys.modules`**；摘掉 `_reject_python_content` 后**原拒绝翻红** |
| `T-02` | **依据不足** ⇒ **维持原状** | `test_audit_landing_whitelist.py` **不构成**该半的证据——它断言的是**审计落点目录白名单**（**另一攻击面**）；`S2` 主体用例（18 例）**自身 docstring 即写明**"拒绝被正确审计记录"一半**无法验证** |

**🟢 本轮最有价值的是那条负向发现**：`T-02` 的"且留审计"半**看起来**有证据、**实际没有**。
文档原本就写"审计半无载体"，但我原先**不知道**存在一个"**看起来像证据、实际是另一攻击面**"
的用例 ⇒ 这正是**"机制已实现"与"威胁已缓解"之间那道最容易被跨过的缝**，
与 `86239f6` 那句"串接存在 ≠ 端到端已跑通"是**同一族**。⇒ 已要求把它**写进 `T-02` 行做防误引登记**
（防的是未来有人把相近用例当证据引用）。

**我的裁决与执行**（派 `architect-wave9` 落盘）：
- `T-11`/`T-12` 升**部分缓解**（**只能**是部分缓解）；数字：`§4.1` → **0 / 10 / 3**
  （未缓解 = `T-03`/`T-04`/`T-10`）、`§5` → **8 条**（"缺行为层验证"）；
  **"已缓解并验证"仍为 0**（不得顺手升到 1）；**历史数字只追加不覆写**。
- **缺口必须写进条目**（不得省略）：`T-11` 的真实 `PolicyEngine` **未被锻炼**（S1 用 `FakePolicyEngine`）、
  "工具滥用"半**零证据**、能力层 deny 的真实 sink **仅经 `RecordingSink`**；
  `T-12` 的 `.pth`/`.so` 等向量未断言、仅覆盖"领域包"这一载体。
- **新开待裁决问题（只框选项、不改设计）**：`T-02` 的"且留审计"半若要闭环，
  **"`resolve_within` 拒绝时由谁 emit 审计记录"**属设计问题
  （`foundation.paths` 持有 sink？调用方 emit？新增一层？），且必须回答
  "**`foundation` 是否该知道 `observability`**"这条分层约束（`ADR-0015`）。

⚠️ **一处操作事实（值得记）**：`architect-wave8` 的 shutdown **已批准**，
但 `task` 工具报 `Member "architect-wave8" already exists in team`
⇒ **`shutdown_response` 的"批准" ≠ 成员已从名册移除**，重派须换名（本轮改用 `architect-wave9`）。

### 3.8 `architect-wave9` 执行裁决：头条数字落为 **0 / 10 / 3**，并纠正类我的一处不准口径

**核验（不采信自报）**：3 笔全在 `docs/design/threat-model/`（`README.md` / `untrusted-input-and-agentic.md` /
`execution-and-isolation.md`）；末笔 `d945b0c` 正文含 `回报：` 块；
`§4.1` 汇总表 = **0 已缓解并验证 / 10 部分缓解（含 `T-11`/`T-12`）/ 3 未缓解（`T-03`/`T-04`/`T-10`）**；
`§5` = **8 条** ⇒ 与我下达的裁决**逐项一致**；"已缓解并验证"**仍为 0**（未被顺手升级）。

**它提的两处事实校正 —— 我独立复核后全部采纳，其中第一处是纠正我**：

1. **我的派活口径过宽**：我写的缺口是"`T-11`/攻击路径 3『策略求值出错却返回 allow』**未断言**"。
   实际 `tests/security/test_policy_eval_failure.py`（4727 B）**存在**，`from …security.policy import PolicyEngine`
   （**真实**引擎，非 fake），断言 `requires_confirmation is True` ⇒ 准确口径是
   "**单元层已断言，仅未经 `Session.run` 端到端断言**"。⇒ ✅ **它没有照抄我的口径**，
   而是先核实再落笔。**这是本项目"派活提示不是权威、仓库才是"这条纪律的一次正向兑现**，
   也是我个人的一次转述不准（记下来：**派活时不要替成员描述它没核过的证据现状**）。
2. **配置面落点白名单的落地现状过期**：`W1`/`W2`/`W4`~`W8` **用例已入库**（`91ea9d5`），
   仅 `W3`（根内符号链接指向根外）与 UNC（第 5 类）仍缺 ⇒ 更正值**未动任何计数**（我核过）。
   ⚠️ **但这两个是真实的安全用例缺口，不是措辞问题**⇒ 已另派验证角色补 `W3`。

**`Q-1` 的框定与我的裁决方向**：它答对了关键一条 —— **`foundation` 不得知道 `observability`，且由机器强制**
（`tests/unit/test_architecture_layers.py` 的 `ALLOWED_DEPENDENCIES["foundation"] == {"contracts"}`，
我核过），并给出三候选 + 各自代价（含"合并拒绝与审计 ⇒ **sink 故障会把路径拒绝升级为会话失败**"、
"`foundation/config.py` 与 `observability/audit.py` 两个调用点**没有会话级 sink**"）。

**我的方向（先取证再定稿）**：**把"设计缺口"与"测试缺口"分开**——
- 若**运行时**路径穿越被拒**已经**产生审计记录（可能 `loop` 的工具失败路径已在 emit）
  ⇒ T-02 的"且留审计"半**只是缺用例**，**不需要新模块、不需要 ADR**；
- 若确实未留痕 ⇒ 在 (ii) 调用方 emit 与 (iii) 新增 `security/path_guard.py` 之间选，**优先 (ii)**（零 ADR）；
- **配置期/装配期**拒绝：倾向**明确划出范围**——那时**会话与 sink 都不存在**，
  要在"尚未校验落点白名单"的阶段决定往哪写审计，会与**攻击面 6**（落点白名单）**循环**；
  ⇒ 倾向"**fail-closed、无审计，且理由写明**"，而非当成一个洞。**但这是范围决策，我会带上三候选交所有者。**
⇒ 已派 `verifier-t02-audit` 取证（读失败路径 + **真实 `JsonlAuditSink`** 端到端跑一次，
并明确"**只认真实 sink**，只有 `RecordingSink` 能观察到**不算**已留痕"）；同时补 `W3` 用例。

### 3.9 🔴 一次**声明失真**被推翻：`T-02` 的"审计半无载体"是错的 —— 而且它已经造成了一次错误裁决

**推翻它的证据（我独立复核后确认）**：

- **载体一直都在**：`tools/files.py:121-129` 的 `_fail` → `tools/registry.py:179-211` 的 `audit_tool_call`
  → `:211` 的 `sink.emit`；`observability/audit.py` 真实落盘；`harness/loop.py` 不重复 emit，
  只把 `result.audit_id` 回填进 `TOOL_RESULT`。
- **证据早已存在**：`tests/security/test_s1_replayable_audit_link.py:85`
  的 `test_denied_tool_call_is_audited_and_replayable`（**真实 `JsonlAuditSink` + 真实 `ReadFileTool`**，
  断言落盘后 `query_by_id` 可还原）。
- **本轮新增**：`test_t02_path_traversal_audit.py`（`4a35c61`，含变异探针：摘掉 `emit` 主用例必红）、
  `W3` 用例（`9b37e99`，配置期 + 装配期两层 + 专属变异探针）。

**错误的传播链（如实记，这是本阶段最值得带走的一条）**：

1. **源头**：`tests/security/test_path_traversal_rejected.py` 的 docstring 写
   「`AuditSink` 尚未落地、无法验证」⇒ **假声明**（已由验证角色在 `b79f383` 修正）。
2. **第一次独立验证**：把上面那条**既有**用例判为
   "工具层路径越权、**非能力层 deny ⇒ 不算该半证据**"——该判据对 `T-11`（能力授权）**成立**，
   但 **`T-02` 本身就是工具层路径穿越** ⇒ **口径被跨条目错误套用**。
3. **我**采信后裁决"`T-02` 依据不足、维持原状"，**架构师**据此把"审计半无载体"
   写进 `§4.1` 行与条目 ⇒ **一句假声明最终升格为正式文档**（`ed28085` / `d945b0c`）。

⇒ **两条纪律（本阶段第二次犯同族，见 §3.8 校正①）**：
① **"依据不足 / 依据充分"这类结论必须写明它是在哪个口径下判的**，
   否则跨条目复用必然失真（本轮失真点：T-11 的"能力层 deny"口径套到了 T-02 的"工具层路径"上）；
② **派活提示里不要替成员描述它没核过的证据现状**——提示是"任务与边界"，**不是事实源**。
⚠️ **这两条的价值在于：本轮是"成员**没有顺从领导的错误口径**"才纠回来的**（`verifier-t02-audit`
把任务背景里那句错误前提直接推翻）。⇒ 与既有口径一致：**完成判定与决策依据都只认仓库**。

**处置**：已派 `architect-wave10` ① 更正 `T-02` 的"审计半"表述（载体与证据都写明；
**保留并精化**那条件防误引登记——`test_audit_landing_whitelist.py` 仍**不是**该半证据，
但**必须补写"该半的证据在别处"**，否则读者会以为仍无证据，**这正是本轮犯错的形状**）；
② 把这次**假声明事故**登记进威胁模型的变更记录（写明它**实际造成了什么**，
**不得**写成"措辞不当"）；③ 产出**提案 `P-2`**：`T-02` 是否可升「**已缓解并验证**」（**将是项目首次**）；
④ 新开 `Q-2`：拒绝事件的 `outcome` 是 `ERROR`（靠 `detail.reason` 区分）还是应有专用 `DENY`。

⚠️ **数字一律不动**（`§4.1` 保持 **0 / 10 / 3**、`§5` 保持 **8**、"已缓解并验证"保持 **0**）
—— 把项目最保守的那个核心指标从 0 变成 1，**交所有者裁决**，不由我或验证角色单方面决定。

**另记一处"不为凑数写弱断言"的正面处置**：`UNC`（落点白名单第 5 类）经实测在 Linux 下
`pathlib` 把前导 `//` 折叠为单 `/` ⇒ **不构成独立绕过面** ⇒ 验证角色**登记为缺口、拒绝补弱用例**
（而非写一条恒过用例充数）。这与本项目"**恒过的断言比没有断言更坏**"的取向一致。

### 3.10 `architect-wave10`：更正落地 + 提案 `P-2`；`Q-2` 由我裁决并关闭

**核验（不采信自报）**：5 笔（`382be98..fc94581`）全在 `docs/design/threat-model/`；
计数仍 **0 / 10 / 3**、`§5` 仍 **8**、"已缓解并验证"仍 **0**；末笔正文含 `回报：` 块。
`T-02` 行已改写为「**载体已实现 + 已有两处行为层证据**」，且**同一行保留了
"原『审计半无载体』表述有误"的更正说明**——**做的是"更正"而不是"抹掉"**（读者能看出这里曾经错过，
这点比改对本身更重要）；精化后的防误引登记**补写了"该半的证据在别处"**（正是本轮犯错的形状）；
`§7` 新增**假声明事故登记**；`§8.2` 新增**提案 `P-2`** 与**问题 `Q-2`**。

**`Q-2` 由我裁决并关闭（不需要改契约）：维持 `outcome=ERROR` + `detail.reason="path_not_allowed"`。**
三条理由：① 改专用 `DENY` 要动 `audit.md` §2.2 的 `D3`（工具层只产 `OK`/`ERROR`）
+ 下游消费者 + 既有用例；② **更要紧的是语义同形**——`DENY` 在契约里绑定
"**策略/审批**导致未执行"，把"**工具层自身拒绝**"并进去，会让**原因完全不同**的两类拒绝在审计里
**同形**（本项目明令避免的老坑）；③ 该区分**已被机器钉住**
（`test_t02_path_traversal_audit.py` 断言 `detail.reason == "path_not_allowed"`）⇒ 不再靠纪律。

**`Q-1` 与 `P-2` 的逻辑依赖（我整理的裁决顺序，两项一起交所有者）**：
`P-2` 的缺口 (c) 已随 `Q-2` 关闭、(a) 随 `W1`~`W8` 全落地（`W3` 本轮补、UNC 判不适用）
⇒ **`P-2` 的成立与否实际取决于 `Q-1`**：

- 若 `Q-1` 判「**配置期 / 装配期不纳入审计范围**」⇒ (a)~(c) 均不阻塞
  ⇒ `T-02` **可升「已缓解并验证」**（**项目首次**）；
- 若判「应纳入」⇒ `T-02` **维持部分缓解**，但理由换成"**非工具层调用点未进审计**"。

⇒ **这两项必须一起交所有者**（先定 `Q-1`，`P-2` 才有答案）。
⚠️ 我不单方面把项目最保守的核心指标从 0 变 1；且按 `§8.2` 的自述——
「**建议 ≠ 结论**，不得据 `P-2` 的建议改状态或计数」——执行。

**一条方法上的正面记录**：`Q-2` 我能**直接裁**，是因为架构师把
"现口径（`D1`/`D2`/`D3`）+ 两选项各自代价 + 影响面"**列全了**；
如果它只写"请示是否改成 DENY"，我就只能反问一轮。
⇒ **"只框定选项与代价"的写法，价值就在于让裁决方能一次裁掉。**

### 3.11 新会话对账（2026-09-20）：两个数字都正常，但**发现一批"已裁决未联动"的旧表述**

**对账结果（看仓库，不凭本文件）**：工作树干净；`HEAD == origin/develop == 6620772`；
`.codebuddy/teams/` 已不存在 ⇒ 无在跑成员（与 §7「收尾」一致）；
`uv sync --extra dev --extra security` 后 **927 passed**（`slow`/`benchmark` 已排除）。
⇒ §7 的结转项**全部结清**（`architect-wave7` 未确认 → `wave8`/`wave9`/`wave10` 逐笔核验、
`P-1` 已裁决、`Q-2` 已关闭、团队已删除）。

**🔴 对账新发现（欠账，且是"规则要求过、但当时没做"的那一类）**：`T-11`/`T-12` 的升级
（`0/8/5 → 0/10/3`）与"`S1`/`S3` 已落地"**只落在了 `docs/design/threat-model/` 内部**，
六个**现行文档**仍是被它们推翻的旧表述 —— 即 `CODEBUDDY.md` 的
「**规则变更的联动更新**」要求的**反向搜索该做而未做**：

| 文件 | 残留 |
| --- | --- |
| `SECURITY.md:104` | 状态分布 `0 / 8 / 5` |
| `CODEBUDDY.md:214,219` | `S1`/`S3` 对抗性用例**仍未落地**；`0 / 8 / 5` |
| `AGENTS.md:193,198` | 同上（两文件内容等价，须逐字同步） |
| `docs/engineering/definition-of-done.md:62` | `0 / 8 / 5` |
| `docs/engineering/sdlc.md:77` | `S1` 与 `S3` **仍未落地** |
| `docs/design/architecture.md:42,560,569,570,573,634` | `0 / 8 / 5`；`T-11`/`T-12` 未缓解；`T-02` 审计半未落地；`G-7` 未解除 |

⚠️ `docs/devlog/`、`docs/research/` 与 ADR 中的**历史快照不在**范围内 —— 按规则它们记录
"当时怎么想"，**改了即失真**（这是本条与前一条"未联动"的界线，不能混）。

**所有者裁决（2026-09-20）**（`0019` §7 顶部两项，**必须一起定、先 `Q-1` 后 `P-2`**）：

| 项 | 裁决 | 后果 |
| --- | --- | --- |
| `Q-1`（范围） | **不纳入**：配置期 / 装配期拒绝 = **fail-closed、无审计、理由写明** | **零 ADR、零新模块**（原倾向的 (ii)/(iii) 均不再需要）；理由要写进契约 |
| `P-2`（升级） | **升级** ⇒ `T-02` = **已缓解并验证** | 状态分布 **0 / 10 / 3 → 1 / 9 / 3**，**项目首条「已缓解并验证」** |

**派活（文件域互不重叠，两名成员并行）**：`architect-wave11` —— 威胁模型落盘
（§4.1 计数 / `T-02` 条目含三条残余风险 / `Q-1`·`P-2` 标已裁决 / §5 的**复算**
〔⚠️ 已提示它：`T-02` 本来就在"已有行为级用例"之列 ⇒ §5 **很可能不变 8**，
不得为"看起来联动了"而改数字〕+ `interfaces/audit.md` 的 `Q-1` 范围声明）；
`architect-wave12` —— 上述六个下游文件的**事实同步**。

⚠️ **结果待落，只认仓库**：收到 `回报：` 块前**不得**认为已完成（本阶段已有 3 次
"spawn 成功 ≠ 成员在跑"的实测，见 §3.4）。

**结果：7 笔，全部独立核验**（不采信自报）。

| 成员 | 提交 | 文件 | 核验要点 |
| --- | --- | --- | --- |
| `architect-wave12` | `6785ac6` / `d1d63c9` / `7aeaaa7` / `71fa9a0` | 6 个下游文件 | 文件范围精确、零 `src/`/`tests/`；计数确为 **1 / 9 / 3**；**除 `T-02` 外无任何 `T-XX` 状态被改**；`G-7` 改「已解除」时**保留**了原判据与"落地前不得视为已覆盖"的历史口径（做的是**更正**，不是抹掉） |
| `architect-wave11` | `83630c8` / `3963775` / `8d0315d` | `threat-model/{README,execution-and-isolation}.md` + `interfaces/audit.md` | §7 的旧 `0 / 8 / 5`（4 处）与 `0 / 10 / 3`（12 处）**原样保留**（只追加不覆写）；`audit.md` 那笔**纯追加（+32/−0）** ⇒ §2.2/§2.4/§2.5 一字未动；`Q-2` 的裁决者标为**团队领导（2026-09-19）**而非所有者 —— **主体与日期分得清** |

**两条"成员没有顺从我的口径"的正面记录**（本阶段第二次，第一次见 §3.8）：

1. `architect-wave12` 在追加指令里**没有照我说的"只改末句"**：它先核实 `devlog 0016` §5 的原文，
   发现**只把出处标注为"当时"会让读者隐含"唯一缺口就是 `S1`/`S3`、现已补齐" ⇒ 反而弱化限定**，
   于是改成"明写当时依据 + 单列落地依据 + 声明**本限定不因二者落地而解除**"。
   ⇒ **改对，而不是改得像对**（与 §3.8 校正①同族）。
2. `architect-wave11` **没有把 `T-02` 的升级顺手牵动 §5 计数**：它先读 §5 口径再复算，判定
   "`T-02` 早在 `f436b0b` 就已移出该清单 ⇒ **升级不产生新的移出项**"，于是**不改数字（仍 8）**、
   只**追加计数沿革**说明 —— 我派活时给的只是提示，**结论是它自己算出来的**。

**我的裁决与追加指令**（均在核验之后下达，不预先放行）：

- 认可 `architect-wave12` 的两处"超范围"更正（`execution-and-isolation.md` 里两处过期的
  "仍缺 `W3`"）：它们与本轮 `P-2` 的 (a)「`W1`~`W8` 全部落地」**直接冲突**，
  **留矛盾比"超范围"代价大**，且属**事实更正**、不改状态与计数（`9b37e99` 已核实为 W3 用例）。
  §12 表头修正**不单列一行**（属同一同步范围，单列属噪声）。
- `Q-2` 正文仍写"现状：未决" ⇒ 追加一笔 `8d0315d`：按与 `Q-1`/`P-2` **同构**的格式在**正文末尾**
  追加【裁决后状态】，写明**裁决者是团队领导（2026-09-19）**与三条理由（取自 §3.10），
  **原「现状：未决」原文保留**（只增不改）。

⚠️ **一处并发事故（本阶段第 4 种同族失效）**：我第一次提交 devlog 被 `commitizen` 钩子拦下，
报 `Failed / files were modified by this hook`。**取证结论：门禁本身是好的** ——
单独复现 `cz check --commit-msg-file` 得 `exit=0` 且文件未变；真因是
**`.git/COMMIT_EDITMSG` 被成员的并发提交覆盖**（我读到的里面是成员那笔的提交信息）。
⇒ 既有的"**文件域独占不覆盖 git 索引**"必须扩写为：**`.git/COMMIT_EDITMSG` 同样是全局共享的**，
领导与成员**同时**提交会互相打断（本次是成员的提交覆盖了领导的信息文件 ⇒ 领导的钩子误判）。
处置：**领导不与成员并发提交**（等成员落完再提交），并登记为待办（见 §7）。

### 3.12 第二波（同日）：8 条缺验证条目的独立复核 + `kind→outcome` 机器检查

**派活（团队 `m1-threat-review`，文件域互不重叠）**：`verifier-threat-coverage`（产出域 `docs/research/`）、
`implementer-audit-contract-check`（产出域 `src/observability/`、`tests/unit/`）。

**(1) `kind→outcome` 机器检查落地（`7520787` / `51bc4db`，2 笔）**：新增
`tests/unit/test_audit_kind_outcome_contract.py`（**16 例**），四层：
(a) 解析 `audit.md` §2.2 的约束表 ↔ 测试侧声明**双向**比对（表有声明无 / 声明有表无 均判红）；
(b) 四个 `emit` 调用点**实际产出**的组合（断言**恰为**预期集合，而非仅 ⊆ —— 后者会让"不再产出"也算过）；
(c) 三条变异探针；(d) **调用点结构化覆盖**：用 **AST** 判定 producer（体内构造 `AuditEvent`）与
透传（不构造 + 裸 `raise` 重抛），并做**登记不实**与**登记漂移**两个反向断言。

**我的独立核验**：1 文件、`src/` 一字未改、16 例实跑通过。⚠️ **我另 grep 出 `src/` 里共 5 处 `.emit(`，
而成员首轮只列了 4 处**（漏的是 `cli/app.py` 的透传包装）⇒ 它的第 (d) 层补的正是这一格。
关于"声明的强度必须能被实测"：我原打算**往 `src/` 插一个假调用点**做变异验证，被审批超时拦下
⇒ 改为**读探针实现**：探针④用**合成模块**证明扫描精度（定义 / docstring / 注释一律排除），
探针⑤在**同一份扫描结果下只换登记表**（证明既非恒过也非恒红）—— **两种方式都达到目的，后者不改仓库**。

**我的裁决（否决了它提的候选 B）**：**不在 `emit` 里做运行期校验**。理由：**B 与 `REQ-SEC-06`（不丢证据）
直接冲突** —— 拒绝写入 = **主动丢掉那条证据** ⇒ 把"生产者违反契约"这个**数据完整性缺陷**转换成
**证据丢失**，方向与本项目的审计目标相反；且 B 必然引入**第二个真源常量**（若上移到 `contracts/`
又与零行为契约层冲突）。⇒ 改用 (d) 的**结构性检查**收窄 A 的残余风险（新增调用点即翻红）。

**(2) 8 条缺验证条目的独立复核（`476c5b9`：`docs/research/2026-09-20-threat-evidence-coverage.md`）**：
结论**逐条从源码 / 用例源码重新推导**（不采信威胁模型文档的转述、也不采信 devlog）。
**两处「声明失真」，方向是"低估"**（少见但同样危险）：

| # | 条目 | 文档声称 | 仓库事实 |
| --- | --- | --- | --- |
| 乙-1 | `T-03` | "现有缓解**全部是设计与契约，无实现载体**"、"`harness/` **尚未建立**"、验证方式"**无**" | `harness/` 已落地；指令-数据分离**有实现**（`prompts.py` 三个 builder · `context/assemble` 角色守卫 · `loop.py` 唯一解析点），且有 `@pytest.mark.security` 的 `S-new-6`（3 例 + `monkeypatch` **变异探针**） |
| 乙-2 | `T-09` | "防 rug-pull 的能力当前为零（**既无实现、也无被测对象**）" | `description_digest` **已实现**且启动期 **fail-secure 拒绝**；`test_tools_registry.py` 有 **4 条**对抗性单测 ⇒ 真正为零的是"**防护对象**"（MCP 未接入 ⇒ 此刻没有外部工具），**不是"无实现"** |

**三处防误引登记（甲）**：`S-new-2` 不得充作 `T-10` 证据（见下）、`test_remote_write_authorization_consistency.py`
不得充作 `T-07` 的提交域检查、`S-new-6` 归属 `T-03` 而**不得**充作 `T-04` 证据。

⚠️ **我对验证角色的一处转述更正**：报告把 `S-new-2` 说成"只断言硬编码常量"，我核实后是
"断言**工具实际收到的** `ExecutionContext.network_allowed is False`，经**真实执行路径**" ⇒ **比转述略强**
（**结论不变**：它不是"出站被拦截并审计"的证据 —— `network_allowed=False` 由 `loop.py` **硬编码**）。
⇒ 这是同一族纪律的**第三次记录**：**转述的证据现状不是权威**（前两次见 §3.8 校正①、§3.9 教训②）。

**我的裁决（派 `architect-wave13` 落盘）**：

- **A（更正）**：改两处失真陈述，**做"更正"不做"抹掉"**（保留"当时为什么这么判"，同 `G-7` 的既有做法）。
- **B（§5 计数）**：按 §5 **既有字面口径**（"有无可执行的行为级对抗性用例"）**`T-03`/`T-09` 移出 ⇒ 8 → 6**；
  **必须**同处写明"**移出 ≠ 覆盖**"（`T-03` 核心三条攻击路径仍无用例；`T-09` 权重/二进制/Skill/CLI 四类载体仍无）。
- **C**：三处防误引登记。
- **D（新开 `Q-3`，只框定不改数）**：`T-03` 的「未缓解」**唯一依据**（残余风险 1"`harness/` 尚未建立"）
  已被推翻、且缓解**有实现载体** ⇒ 按 §4.1 定义**可能应为「部分缓解」**；`T-04`（其"现有缓解"列的
  `harness/context/` **也已实现**）**可能同病** ⇒ 要架构师出**判据对照表**（提案），
  **分布 `1 / 9 / 3` 一个数字都不动**，我据此一次性裁决。
- **E**：登记 `EXECUTION_DEGRADATION` / `REFUSAL` **无 producer** 的覆盖边界
  （producer 落地时**必须**补触发用例，否则 §2.2 该两行仍无可执行证据）。

⚠️ **一条留给下一轮的口径问题（不得顺手改）**：§5 的**字面口径**会在"只落了**一条边界用例**"时
就把条目移出清单 ⇒ **数字变小 ≠ 缺口变小**。本轮选择**按既有字面口径重算**（与 `T-11`/`T-12`
那次同构、可逐次复算），并把"移出 ≠ 覆盖"写进同一处；**若下轮认为该口径本身需精化**
（例如改为"是否覆盖该条的**核心**攻击路径"），那属于**改口径 ⇒ 须 ADR 或所有者裁决**。

---

### 3.13 `Q-3` 收口：`T-03` 升「部分缓解」+「决定性机制」口径澄清（含团队清理）

**第一段：`architect-wave13`（`0821b74` / `f7e83ef` / `5d165f2` / `d315b3a`，4 笔）**
落盘 §3.12 的 A~E 五项。**我的独立核验**：5 文件全在白名单内（零 `src/`/`tests/`）；
`§4.1` 的 `1 / 9 / 3` **一字未动**；`§5` = **6** 且第 328 行写明"**移出 ≠ 覆盖**"；
`T-03` 的原判依据以**删除线保留**而未抹掉（`untrusted-input-and-agentic.md:81-85`，并写明
"**只更正陈述、不改状态**"）；三处防误引齐备（`execution-and-isolation.md:306` 的 `S-new-2`、
`supply-chain-and-process.md:125` 的 `remote_write_authorization`、`:120` 的 `S-new-6` 归属）；
`audit.md` §2.2 的覆盖边界（`:95-108`）写得准；审计契约机器检查仍 **16 passed**。

**第二段：它的两处自主动作 —— 一认可、一否决**（这条本身值得记：**不是所有"超范围"都该拦**）
- ✅ **认可**：它在 §4 表 `T-03`/`T-09` 两行**加了更正指针**（不在我的逐项清单里）——不加以免会留下
  **新的**声明失真。判据：**超范围的"事实更正"若不做会制造矛盾，就该做**。
- ❌ **否决并要求改**：它把头条"缺行为层验证 **8**"**保留旧值 + 追加"仅增不改"注**。
  ⇒ **头条是"当前状态摘要"**（与 2026-09-19 那次**直接覆写**状态分布同一惯例）；
  "仅增不改"适用于**历史记载**（§7 变更记录、旧复核段）。**同一行既写 8 又写 6 属自相矛盾**。
- 另裁：**`T-09` 的移出不回滚** —— §5 的原文口径是"可通过 `make check` / `make test-security` 执行"，
  `@pytest.mark.unit` 用例在 `make check` 下执行 ⇒ **合格**；**额外要求 `security` 标记等于改口径**（须 ADR/所有者）。

**第三段：`Q-3` 所有者裁决（2026-09-20）**

| 子问题 | 裁决 | 后果 |
| --- | --- | --- |
| `T-03` 档位 | **升「部分缓解」** | §4.1 → **`1 / 10 / 2`**（未缓解只剩 `T-04`/`T-10`） |
| §4.1「未缓解」口径 | **加适用澄清** | 「未缓解」= **决定性缓解机制**未建立（**≠ 完全没有载体**）；**决定性 = 能阻断该条攻击路径**的机制，仅"**降低暴露面**"不算 |

**为什么这条澄清是必须的（不是粉饰）**：`T-03` 一升，机械套用"有载体即部分缓解"会把
`T-04`（工具裁剪 / 上下文压缩**只是降面**，输出侧防护 0、检查点完整性无设计）与
`T-10`（`network_allowed=False` **只是标志位**，实际拦截机制不存在）**一起推上去**
⇒ **「未缓解」会被稀释到 1 条甚至 0 条**。⇒ 澄清的作用是**把"什么才算决定性"写死**、
**不降低判据强度**（它同时给 `T-04`/`T-10` 写出了**为何仍不够**）。

**第四段：执行与清理**
- ⚠️ `architect-wave13` 完成即**从名册消失**（**第 4 次**"成员不可再寻址"实测：`send_message` 报
  `Recipient "architect-wave13" not found. Known members: none`）⇒ 收尾改用**前台子任务**完成
  （`d20671e` / `2b63a5e`，2 笔）：§4.1 → `1 / 10 / 2`、头条 `8 → 6` 的矛盾修掉、
  `T-04` 的"现有缓解（设计层，无实现）"更正（`harness/context/`、`harness/trimming.py` 均已核实现存）
  且**状态维持未缓解**（用新判据说明理由）、`Q-3` 标已裁决、§7 追加变更记录。
- **历史数字一律保留**：`0 / 8 / 5`（5 处）、`1 / 9 / 3`（12 处）**一处未覆写**。
- 所有者指出**已完成任务的团队成员会造成明显卡顿与 API 响应延迟** ⇒ 已 `team_delete`，
  `.codebuddy/teams/` 现为空；**本波收尾不再开团队**（改前台子任务 + 领导核验）。
  ⇒ 与既有纪律一致：**成员"完成即关闭"要真的执行到"从名册移除"这一层**，不能只停在
  `shutdown_response` 的"批准"（§7 已记过该操作事实）。

⚠️ **本阶段的状态分布一天内变了三次**：`0 / 8 / 5` → `0 / 10 / 3`（`T-11`/`T-12`）→
`1 / 9 / 3`（`T-02`）→ **`1 / 10 / 2`**（`T-03`）。**每一次都有可核依据、每一次都只动一格**，
且**每次都同时写明"这不是安全已到位"** —— 这正是本阶段想立住的东西：
**数字可以动，但必须动得有据、动得可复算、且不许被读成结论。**

### 3.14 把 `0.1.0`「首个可用版本」落定，并把 `M1` 出口准则补成可核对形态

本波（11 笔，`7fa1ba4..a8a30e8`）把 `0.1.0`「首个可用版本」（台账判据：端到端可跑 + 调用工具 + 审计可回放）真正落定，并顺带把 `M1` 的出口准则补成可核对形态（`sdlc.md` §3.2 的「`M1` 出口准则」草案与示例领域包 ADR 同批产出）。

**A 类报告四项**（团队领导要求的四要素，逐项写实）：
- **分支名**：`develop`。
- **提交列表**（`7fa1ba4..a8a30e8`，11 笔，旧→新）：`42f36a3 feat(packs): 内置两份只读示例领域包并加边界用例` / `cb0dfc5 test(packs): 把示例包用例的 roots 收窄到 examples/packs` / `effc843 docs(adr): 新增示例领域包 ADR、M1 出口准则与测试计划事实同步` / `3d6d977 docs(design): 更正测试分层过期计数并登记 0.1.0 与 M1 的交叉引用` / `4c20f5d docs(engineering): 更正测试分层数字并统一取证口径` / `1441b3e docs(research): 新增 0.1.0 端到端实跑取证报告` / `c625a7f docs(research): 落盘威胁证据独立复核报告（T-01/T-02/T-08/T-11/T-12）` / `b7752db docs(readme): 补齐产品用法与仓库结构，并修正端到端配方` / `46e162e fix(scripts): 更正 release.sh 末段提示与现行发布授权口径相反` / `b9adaea fix(tests): 发布计划用例支持「当前即最高一级」的合法状态` / `a8a30e8 chore(release): 0.1.0`。
- **文件清单**（按目录归类，`git diff --stat 7fa1ba4..a8a30e8` 真实结果；18 files / +1392 / −43）：
  - 仓库根配置：`CHANGELOG.md`、`README.md`(+91)、`pyproject.toml`、`uv.lock`、`scripts/release.sh`(+9)；
  - `docs/adr/`：`0021-example-domain-packs-and-first-usable-release.md`(+276)、`README.md`(+1)；
  - `docs/design/`：`architecture.md`(+7)；
  - `docs/engineering/`：`sdlc.md`(+37)、`testing-strategy.md`(+41)；
  - `docs/research/`：`2026-09-20-threat-evidence-coverage-2.md`(+232)、`2026-09-20-v0.1.0-e2e-evidence.md`(+278)；
  - `examples/`：`README.md`(+75)、`packs/coding-readonly/pack.toml`(+36)、`packs/tech-manual-qna/pack.toml`(+36)；
  - `src/`：`agent_sec_perf/__init__.py`（+−2，仅版本号随 `a8a30e8` 机械变更）；
  - `tests/unit/`：`test_example_packs.py`(+230)、`test_release_policy.py`(+76)。
- **验证方式**：`make check` 全绿（**本人复跑：954 passed / 1 skipped**，slow/benchmark 已排除；本波内实现/记录侧曾报 953 / 955，以本次实测为准）；端到端实跑证据见 `docs/research/2026-09-20-v0.1.0-e2e-evidence.md`。

**逐笔独立核验结论**（来源 = 团队领导核验，非自报）：三名成员的产出均在各自白名单内；据领导核验，`src/`（除 `a8a30e8` 的版本号机械变更外）与 `docs/design/threat-model/` 在本波成员产出中零改动——威胁分布仍 **1 / 10 / 2**、§5 仍 **6**、「已缓解并验证」仍为 **0**；三名成员末笔正文均含 `回报：` 块。

值得记的两条"成员没有顺从领导口径"（正面记录）：`implementer-relfix` 在被要求"支持当前即最高一级"时，**新增**了"当前版本必须在台账里"的恒真不变式，并实测了它的反向（伪造成 `0.9.9` ⇒ 断言失败，不会被 skip 绕过）；`verifier-v01` **直接推翻**了领导派活配方里的 `--max-completion-tokens 384`——Qwen3-4B 是思考模型，384 预算被推理耗尽 ⇒ `content`/`tool_calls` 皆空 ⇒ `ModelProtocolError` ⇒ 任务 `FAILED`；改 `1536` + `600 s` 后实测 `exit 0` / 6 条事件 / `seq` 连续 / 工具真读到哨兵 / 审计 `query_by_id` 可回放 / 197 s。

领导自己的转述不准（如实记，不追责）：派活提示里那句"该配方已验证可用"在**配了领域包的 CLI 路径上不成立**；同一取值 `384` 在 `tests/integration/` 的更窄上下文里确实成立（领导冒烟 **7 passed / 351.93 s**）⇒ **窄上下文成立不能外推**。

两次被门禁拦下并按设计回滚（2026-09-19 一次、2026-09-20 一次），两次都证明"**门禁真的拦得住、回滚真的生效**"——本次即 `make release` 那次：空序列取 `min()` 卡住 `0.1.0` 发布，按设计回滚后由 `b9adaea` 修复。

**领导的裁决**（均为本波裁定，逐条）：
- `ADR-0021` 仍是**提议中，待所有者批准**；`examples/packs/` 已同批落地属**刻意的例外**（目录是数据、可整体 revert）——按规则"新目录先有 ADR"，本处如实登记这处**顺序偏差**，不掩饰。
- `ADR-0021` §7 第 4 条（`src/` 不得 `import examples/` 的机器检查）**裁决暂不落地**：理由 = `examples/` 无可导入模块（无 `.py`、无 `__init__.py`），而 `src/` 的动态导入已被 `tests/security/test_no_dynamic_code_from_src`（以实际用例名为准）静态守卫覆盖；改为"以核验证据（`grep` 零命中）代替 + 需求变化时复核"。
- `ADR-0021` §7 第 5 条（wheel 分发口径）**已实测**：wheel **不含** `examples/`（与 ADR 预测一致）⇒ 只装 wheel 的用户看不到示例包，如实记为**已知边界**。
- 新登记待办（见 §7）：`src/agent_sec_perf/__init__.py` 的 docstring 仍写"当前 `0.0.0`"（**陈旧**；`src/` 不在本轮任何角色的白名单内，未顺手改，属**刻意不越域**）。

**哈希引用规则**：本篇只引用产生它之前的提交；本波 11 笔均已存在，可以引用；**本人本次提交自身的哈希不写**（下次回填）。

### 3.15 收尾：仓库改名的联动更新（修 2 处）与 `develop` 推送

`1ecdc72` 之后，团队领导在**推送回显**里发现仓库已改名：远端为
`cnb.cool/Mybase_Le0n3rd/Native-Intelligent-Systems`，而仓库内仍有多处引用旧名
`software_engineering`。`docs/devlog/0016`/`0017` 早已用新名引用 PR ⇒ 改名在 09-19 之前就发生了
⇒ 这属 [「规则变更的联动更新」](../../CODEBUDDY.md) **该做而未做**的那一类（与 2026-09-18 那次同一形状：
**改了一处，没有反向搜索**）。

**先取证再动手（关键一步）**：旧路径**并未失效** ——
`git ls-remote https://cnb.cool/Mybase_Le0n3rd/software_engineering.git` **成功返回 refs**
⇒ 旧 URL 会被重定向，**不是坏链** ⇒ 本轮按"**事实陈旧**"而非"故障"处置，**不做全仓替换**。

**本轮修 2 处**：`README.md`（仓库链接 + `git clone`/`cd`）、`.cnb/ISSUE_TEMPLATE/config.yml`
（两条 `SECURITY.md`/`CONTRIBUTING.md` 链接）。**`CHANGELOG.md` 第 236 行的 `[Unreleased]` 比较链接
仍指旧名** —— 它属记录员独占产出域，而记录员已收工 ⇒ **登记为 §7 待办，不由领导越域改**
（口径与全文一致：**不是不能改，是不越域**）。

**A 类报告（本节末尾这几笔）**：分支 `develop`；提交与文件清单见 `git diff --stat 1ecdc72..<本节末笔>`
（含 `092e92f` 的 `CHANGELOG.md`、`1ecdc72` 的 `docs/devlog/`，以及本节新增的 `README.md` +
`.cnb/ISSUE_TEMPLATE/config.yml`）；**验证方式**：`make check` 全绿
（**954 passed / 1 skipped** —— 那个 skip 是"`0.1.0` 已是台账最高一级"时
`test_release_plan_is_consistent_and_does_not_touch_the_ladder` 的**预期分支**，
台账补上下一级即自动恢复，见 §3.14 与 §7）；`git push origin develop` 已推送（`1441b3e..1ecdc72`，本节随后再推）。

### 3.16 所有者批准落地：`ADR-0021` 转已接受、`M1` 出口准则生效、台账补 `0.2.0`

**授权来源（不得含糊）**：所有者 2026-09-20 明确批准团队领导提出的三项，**原话「批准方案」**；
对仓库改名另给口径：「**可替换为当前的名字，如若仅仅是记录用的日志，可以保留历史**」。

**A. 三项批准已落盘**（`5648bde`，5 文件）：

1. **`ADR-0021` 转「已接受」**（`docs/adr/0021-*.md` 状态行 + `docs/adr/README.md` 索引 + §8 动作 1 打勾 +
   §9 追加一行）；**保留**「`examples/packs/` 同批落地属刻意的顺序偏差」这一如实登记 —— 获批**不改变**
   「当时确实先建了目录」这个事实，改了即失真。
2. **`docs/engineering/sdlc.md` §3.2「`M1` 出口准则」转「已批准」**（并回填现状列）：
   ⚠️ **SRS 一项如实保持"未满足"**（仍是 v0.1.3 且与 `devlog 0004`/`0005` 分叉）；
   §3.2 对"成稿是否要求零未决项"刻意**留白**并写明"不替所有者拍板"——本波一处**没有越权**的处置。
3. **版本台账补 `0.2.0`**（`pyproject.toml` + `docs/engineering/git-workflow.md` §5.2 的"引用副本"表，
   **两处同步**）：`name` = `M2` 的「端到端主流程可运行（Alpha）」；`criteria` **明确写出**它由新增的
   **`sdlc.md` §3.3（`M2` 出口准则 `M2-1`~`M2-4`）** 定义，且**该节当前仍是草案** ⇒ 写明
   "批准前不得据此落定本版本"。

**🟢 一处直接可测的联动效果（本波收尾价值所在）**：台账补上下一级后，
`tests/unit/test_release_policy.py::test_release_plan_is_consistent_and_does_not_touch_the_ladder`
**从 `skip` 自动恢复为 `passed`** ⇒ `make check` 由 `954 passed / 1 skipped` 变为
**`955 passed`（skip 归零）**。⇒ 这印证 §3.14 那句"那个 skip 是预期分支"**不是辩解**：它确实随台账补级
自愈；反过来说，**若无人补级，这半条机器检查会长期休眠** —— 正是把它列进 §7 的理由。

**B. 仓库改名的口径执行**（按所有者给的界线，**不是**"全仓替换"）：
**现行 / 功能性引用 ⇒ 换成 `Native-Intelligent-Systems`**；**纯历史记录 ⇒ 保留旧名**。
本轮共 3 处：`README.md` 与 `.cnb/ISSUE_TEMPLATE/config.yml`（§3.15 由领导修）、
**`CHANGELOG.md` 的 `[Unreleased]` 比较链接（`96c0404`，由记录员修，只改 1 行）**。
`docs/devlog/`、`docs/research/`、已接受 ADR 的历史正文**一律未动**（项目规则明列的例外）。
⚠️ 一处"**先取证再动手**"值得带走：旧路径 `git ls-remote` **仍成功** ⇒ 判"事实陈旧"而非"坏链"
⇒ **不做全仓替换、不做额外清理**。

**A 类报告**：分支 `develop`；本节新增 3 笔 —— `96c0404`（`CHANGELOG.md`，1 行）、
`5648bde`（`docs/adr/0021-*.md` / `docs/adr/README.md` / `docs/engineering/sdlc.md` /
`docs/engineering/git-workflow.md` / `pyproject.toml`，5 文件 `+77/-12`）、以及本笔（`docs/devlog/`）；
**独立核验（不采信自报）**：`git show --stat` 逐笔核过；**威胁模型、`src/`、`tests/` 零改动**
（`git diff 5ac4721..HEAD -- docs/design/ src/ tests/` 为空 ⇒ 分布仍 **1 / 10 / 2**）；末笔正文含 `回报：` 块。
**验证方式**：`make check` 全绿；`uv run pytest -n auto -m "not benchmark and not slow" -q` ⇒ **955 passed**；
`uv run pytest tests/unit/test_release_policy.py -q` ⇒ **18 passed**。

**遗留（写进 §7，不在此处模糊处理）**：`sdlc.md` §3.3（`M2` 出口准则）**仍待所有者批准**，
`0.2.0` 的落定**前置**即该节获批；`M3`/`M4` 与版本号的映射未定（进入 `1.0.0` 需先改机器检查本身）。

### 3.17 🔴 一处「声称有机器检查、实际不存在」的失真声明被成员推翻并更正

**怎么被发现的**：上一位架构师在核对台账时，**没有照抄我在派活提示里转述的一句话**，而是去跑了用例。
我转述的是 `docs/engineering/git-workflow.md` §5.2 的原话——「两处不一致时以台账为准，
并由 `tests/unit/test_release_policy.py` 的断言拦下」。

**我的独立复核（不采信自报）**：
`grep -n "git-workflow\|engineering\|docs/" tests/unit/test_release_policy.py` **只命中第 1 行的 docstring**；
该用例只读 `pyproject.toml` / `__init__.py` / `uv.lock` / `CHANGELOG.md`（并加载 `scripts/release_edit.py`），
**不解析 `git-workflow.md`**；全仓**没有任何**断言比较 §5.2 的表与 `[tool.lowspec.releases]` 台账
（读 §4 的是 `test_remote_write_authorization_consistency.py`，与本节无关）
⇒ **该句话声称的机器检查不存在**。这正是本项目最忌的失效形态（`SECURITY.md` 的 `S-8`
「声称已生效的缓解必须能被实测」；`ADR-0019` §5.4 亦明令不得表述为机制）。

**更正（`1e976df` / `16e4a88`，2 笔）**：
- `git-workflow.md` §5.2 改为**如实三点**：① 上表是**人工转写**的引用、不是副本真源；
  ② **当前没有任何机器检查比较它与台账**（并列出用例实际读哪四个文件）；
  ③ "**本表漏改、写错都不会报错**" ⇒ 改台账**必须人工同步本表**；
  **若要把这处同步落成机器检查 ⇒ 属 `F` 类（改门禁强度）⇒ 须事先确认，不得顺手新增**。
- `pyproject.toml` 上方注释与 `src/agent_sec_perf/__init__.py` 的 docstring：两处都写死了陈旧版本号
  （"当前 `0.0.0`"）⇒ 一律改为**不复制具体值、指向台账真源**的耐用写法（写死必然再次漂移，
  这是本项目反复吃过的教训）。
- `ADR-0021` §9 **追加一行**：登记其 §5.2 正文那句「当前 `[project].version = "0.0.1"`」是**撰写当日快照**
  （**ADR 只增不改**，正文保留），读时以台账为准。

**两条纪律（本波第二次同类，价值高于修好本身）**：
① **领导转述任何"有机器检查 / 有机制拦下"的说法时，必须自己先跑一遍** —— 我这次是把文档里的一句
**未核实**的说法当成事实写进了派活提示（§3.14 已记过一次"派活配方"被推翻，这是第二次）；
② 反过来，**成员不去顺从领导的口径、而是先核再落笔**，是本项目唯一能纠回这类错误的方式
—— 两次都是这么纠回来的（`verifier-v01` 推翻 `384`、`architect-approve` 推翻本句）。

**我对上一位架构师 4 项遗留的裁决**：
1. **`sdlc.md` §3.2 的"窄口径"确认有效**（`M1-3`/`M1-4` 判"成稿（窄口径）"）：理由三条 ——
   ① `sdlc.md` §3 对 `M1` 的定义**不含**"零未决项"；② 该节把口径**写在明面上**且逐行给了依据；
   ③ 它同时写明"**成稿 ≠ 安全已到位**"、明令不得把窄口径读成"未决项已解决"、
   且**威胁计数与 §5/§6 一个都没动**（仍 `1 / 10 / 2` / `6` / 5 项）。⇒ 这不是放宽断言，是**把口径写明**；
   若所有者要更严（零未决项），须走新增/修订该节的显式决策。
2. §5.2 的失真声明 ⇒ **本轮已更正**（上）。
3. `ADR-0021` §5.2 的版本快照 ⇒ **本轮已按"只增不改"登记**。
4. 两处写死版本号的注释 ⇒ **本轮已改为耐用写法**。

**A 类报告**：分支 `develop`；本节新增 3 笔 —— `16e4a88`（`src/agent_sec_perf/__init__.py`，`+2/-1`）、
`1e976df`（`docs/engineering/git-workflow.md` / `pyproject.toml` / `docs/adr/0021-*.md`，3 文件 `+12/-3`）、
本笔（`docs/devlog/`）；**独立核验**：逐笔 `--stat` 核过、**威胁模型与 `tests/` 零改动**、
`__version__` **取值未变**（仍 `0.1.0`）；**验证方式**：`make check` 全绿（955 passed）。

### 3.18 迟到回报暴露的三条遗留：两处陈旧表述 + 一条**新的实测失效模式**

两名成员的回报**在我关闭并删除团队之后才送达**（又一次印证"回报可能丢失/延迟"，完成判定只认仓库）。
它们的报告里有三条**尚未处理**的遗留，逐条核实后处置：

**① 包内**模块 docstring**陈旧**（`2697d62`）：`src/agent_sec_perf/__init__.py` 的模块 docstring 仍写
「本包当前仅提供工程基线 Phase 0。模块划分…将在 base project 选型冻结后确定」——而包下已有
**九个模块目录**、分层已由 `ADR-0015` §5.1/§5.4.1 定案且由 `test_architecture_layers.py` 机器检查。
改法采用**耐用写法**：**不枚举模块清单**（目录会增删，复制即漂移）+ 指向权威源 +
明写「**已实现不等于已验证**」。
⚠️ **我第三次定位不准**：我在派活提示里把陈旧文本说成"docstring 里的"，成员**先核实后发现它在
`__version__` 上方的 `#:` 注释**（`16e4a88` 已改），真正的模块 docstring 是**另一处**——
是它把这一处报回来我才知道（见本节末的纪律升级）。

**② 一致性报告登记两条**（`bb5a4fc`，记录员域）：
- `A-17` 条目末尾那句「`0.1.0` 属**尚未到达**的里程碑」只在 2026-09-19 当时成立 ⇒
  **只追加**【2026-09-20 进展】（写明 `0.1.0` 已由 `a8a30e8` 落定、`0.2.0` 已由 `5648bde` 补入台账），
  **不改写**原结论（报告是历史记录，改了即失真）。
- **新增 `A-18`**：登记 §5.2 那句"虚假的机器检查声明"（含实测依据、更正提交 `1e976df`、
  以及一句关键的限定——「**这是"更正了错误声明"，不是"问题已解决"**：台账↔文档表的一致性**目前仍靠人工同步**，
  落成检查属 `F` 类待裁决」），并在 §D 处置表补一行。

**③ 🟢 一条新的实测失效模式（`0839b4e`，`agent-teams.md` §6，+61 行）**：
**"门禁的红可能来自共享工作树 —— 别人的写入会让我的钩子假红"**。事实：一名成员在**另一名成员正在写
`src/agent_sec_perf/__init__.py` 的时间窗内**跑 `make check`，`security-secrets` 报
`detect-private-key … files were modified by this hook`；待对方提交（`16e4a88`）后单独复跑该 hook ⇒
Passed、整体复跑 ⇒ 全绿。
**它比我的要求做得更细、并且纠正了我的前提**：我猜机制是 `git stash` / `restore`，它**读了
`pre-commit` 源码**后发现 —— 本仓库这条命令带 `--all-files`，而 `run.py:344` 的
`stash = not args.all_files and not args.files` ⇒ **根本不 stash**；真实机制是
「**工作树 diff 是钩子的隐式输入**」（钩子前后各采一次 `git diff`，不相等即判"文件被改动"）。
它由此给出**可执行的归属判别式**（这才是本条的价值）：
**行内无 `- exit code:` ⇒ 假红**（钩子自身是通过的，红只能来自外部改动）；
**有 `- exit code: 1` ⇒ 真红**（真扫到密钥特征串，按安全事件处置）。
并写明：窗口宽度 = 该钩子自身运行时长（**咬中带概率**）、`git diff` 不含未跟踪文件（**新建文件不会触发**）、
本仓库 `make check` 里**只有 `security-secrets` 走 pre-commit** ⇒ 该模式目前只命中这一处，
但它长得像"**密钥泄漏**"，是**最不能误判**的一类红。处置按序写死：**先判归属 → 再单独复跑 → 留证 →
仍红则停下上报**；**禁止** `--no-verify` / `SKIP=` / 删改钩子。
⚠️ 按项目口径，这**不得**被写成"机制"（不可由 CI 强制 ⇒ 只能记为**部分缓解**）。

**🔴 一条纪律升级（本波最该带走的东西）**：本波"**成员先核实、再落笔**"共**纠正了我三次**——
① `verifier-v01` 推翻我给的 `384` 参数；② `architect-approve` 推翻我转述的 §5.2"有机器检查"；
③ `architect-agents` 推翻我猜的 `git stash` 归因（并指出 `--all-worktrees` 那条路径与 stash 无关）。
⇒ 纪律由 §3.14/§3.17 的"**不要替成员描述它没核过的证据现状**"升级为：
**派活提示里不得替成员描述它没核过的三类东西 —— 事实现状、文本位置、以及机制归因**；
提示只能给**任务与边界**。三次都由成员核对纠回，说明"**仓库与实测才是权威**"这条在设计上是对的。

**A 类报告**：分支 `develop`；本节新增 4 笔 —— `2697d62`（`src/agent_sec_perf/__init__.py`）、
`bb5a4fc`（`docs/engineering/doc-consistency-report.md`）、`0839b4e`（`docs/engineering/agent-teams.md`，+61）、
本笔（`docs/devlog/`）；**独立核验（不采信自报）**：逐笔 `--stat` 核过、**`docs/design/` 与 `tests/` 零改动**
（威胁分布仍 `1 / 10 / 2`、§5 仍 `6`）、`__version__` 取值未变；**验证方式**：`make check` 全绿（955 passed）。

### 3.19 所有者第二批批准落地（含一次跨域接管与如实登记）

**授权**：所有者 2026-09-20「**批准你的请求**」⇒ ① `sdlc.md` §3.3（`M2` 出口准则）转**已批准**；
② `A1`/`A2` 采纳领导建议——**维持最保守取值**并关闭该项；③ `F` 类：`CODEBUDDY.md` / `AGENTS.md` §10
留一句常驻指针（门禁假红判别式）。⚠️ `F-1`（把 §5.2 表 ↔ 台账的同步落成机器检查）**未获批准**，
本轮**未实现**，仍留 §7。

**落地（6 文件）**：① `sdlc.md` §3.3 标题与状态块 → 已批准（照 §3.1/§3.2 的写法）；
② **批准后的联动更正**（本项目最常漏的一类，逐处反向搜索后才落笔）：`pyproject.toml` 的 `0.2.0`
`criteria` 与 `git-workflow.md` §5.2 的**人工转写表**——两处原写"该节当前为草案、批准前不得据此落定"，现已过期；
③ `architecture.md` §11 的 `G-5` → **已拍板**；④ `interfaces/audit.md` §5 **只追加**一条裁决登记
（契约的字段 / 语义 / `P1`~`P7` **一字未动**）；⑤ `CODEBUDDY.md` + `AGENTS.md` §10 各加一条**逐字等价**的常驻指针。

**🔴 一处必须如实登记的流程偏差（本波唯一一次跨域）**：本轮原派 `architect-approve2` 承担上述文档改动，
但**网络中断使其零产出**（仓库里最后一笔仍是 `bf62eaa`、工作树干净、无 `回报：` 块）。
按协议这只能记"**未确认**"、**不得据此重派**；此处存在一项**可物证的例外**：**所有者当场告知
"网络波动导致暂停、子 agent 应已停止"** ⇒ 该成员不可能在基于提示工作（与 §8.13 的例外同构，
且**不是**据 §4.2 那条已被证伪的"空信箱"判据）。⇒ 团队领导**直接接管**了这 6 处改动。
**边界（不得外推）**：这是"**有所有者明确信息 + 零产出**"下的接管，**不构成**"领导可随手跨域"的先例；
常规路径仍是**派活 + 只从仓库核对**。记此一笔，供后人判断。

**A 类报告**：分支 `develop`；本轮 2 笔（`docs(...)` 落 6 个文件 + 本笔落 `docs/devlog/`）；
**独立核验**：`grep` 反向搜索确认**无**"§3.3 仍为草案"的同源残留（`docs/devlog/` 与 `docs/adr/0021` 的
历史记录**按规矩未动**）；**威胁模型与 `src/`、`tests/` 零改动**（分布仍 `1 / 10 / 2`、§5 仍 `6`）；
**验证方式**：`make check` 全绿（955 passed）。

**`M2` 现状（提醒，不得读成结论）**：§3.3 的四项判据 `M2-1`~`M2-4` 现状**均未满足** ——
最值得先做的是 `M2-1`：目前只有**单步**工具调用的实跑证据，"**多步**（同一会话内 ≥2 次工具调用）"尚无。

### 3.20 补做发布流程：开 PR #8（owner 提醒的一步，此前被我漏掉）

**流程被我漏了一步（如实登记）**：本项目现行的发布流程是
「**代理开 PR → 所有者在仓库网页审查并合并 → 代理打 tag → 代理把 `main` 回合 `develop`**」
（口径见 `docs/engineering/git-workflow.md` §5.3 与 `ADR-0019` §5.7；**合并到 `main` 恒为所有者动作**）。
本波我只把产出推到 `develop` 就宣告"发布线在你的手上"，**漏了"由代理开 PR"这一步** —— 由所有者在
2026-09-20 当场指出 ⇒ 本次补齐，并把它写进 §7 作为**新会话的第一件事**。

**已开出 PR #8**：
- 目标：`develop` → `main`；状态 `open`；标题「`0.1.0`「首个可用版本」：端到端可跑（含独立实跑证据）与 `M1` 交付物收口」；
- **正文按 `.cnb/pull_request_template.md` 的 11 节逐节填写**（基本信息 / 动机 / 内容 / 方案取舍 /
  验收标准对照 / 验证方式 / **安全影响评估** / 性能影响 / 文档留痕 / 自评审 / 风险与回滚），
  正文文件落在 `/tmp`（**不污染仓库**），经 `cnb pulls post-pull --body-file` 提交；
- 规模：相对 `main` 领先 **176** 笔提交、**102** 个文件、`+23130 / -247`；
- 验收对照里**如实标出唯一未满足项**：`M1-1`（SRS 成稿）；风险节标出 `M2-1` 尚无证据与
  「`examples/` 不随 wheel 分发」这一已知边界。

**后续（顺序不得颠倒）**：① **所有者**在网页审查并合并（建议 **Merge commit**，保留发布节点）；
② 合并后**由代理**在 `main` 上打**带注释标签** `v0.1.0`；③ **由代理**把 `main` 回合 `develop`。
⚠️ 三处口径一致：**代理不得自行合并到 `main`**。

**A 类报告（本节）**：分支 `develop`；操作 = `cnb pulls post-pull`（**远端写：开 PR**，属已获所有者明确指示的动作）；
本轮另有 3 笔本地提交（批准项落地 + 常驻指针 + 本记录）已推送 `develop`（`bf62eaa..7406c72` 及其后本笔）。

### 3.21 发布闭环：所有者合并 PR #8 后，打 `v0.1.0` 标签并把 `main` 回合 `develop`

**触发**：所有者 2026-09-20 告知「**已经合并了 pr**」。按纪律**先核对仓库、不凭消息**。

**① 核对合并事实（不采信消息）**：
```
git fetch origin main develop
git log -1 --format="%H%n%P%n%s" origin/main
  14000b3…            ← 顶端
  7138007… 9e314a0…   ← 两个父：旧 main ⊕ develop 顶端 ⇒ **真正的 Merge commit**
  合并来自 develop 的合并请求 #8
git show origin/main:pyproject.toml | grep '^version = '   ⇒ 0.1.0（四处副本一致）
```
⇒ 合并方式符合要求（**Merge commit，保留发布节点**），且没有夹带未经审查的提交。

**② 打标签（带注释，落在合并提交上）**：
`git tag -a v0.1.0 14000b3 -m "Release v0.1.0"` + 三段正文 —— 判据（台账原文）／合并提交与**实跑证据**／
**口径**（本版本**不构成任何安全缓解**、威胁分布仍 `1 / 10 / 2`、`0.1.0` **不等于** `M1` 完成）。
推送用**全限定引用名**：`git push origin refs/tags/v0.1.0`（沿用基准数据分支那次的教训 ——
短写法在同类引用已存在时会被拒）。核对：`git cat-file -t v0.1.0` = `tag`（**带注释**，非轻量标签）、
`git rev-parse 'v0.1.0^{commit}'` = `14000b3`。

**③ 回合 `develop`**：`git switch develop && git merge --ff-only origin/main`
⇒ **fast-forward**（`develop` 是合并提交的父之一，可 ff）⇒ `git push origin develop`（`9e314a0..14000b3`）。

**④ 三方一致性核对（发布闭环的判据）**：`develop` = `origin/main` = `v0.1.0` = **`14000b3`**。
仓库现有标签：`v0.0.1`（M0）、**`v0.1.0`（本次）**。

**A 类报告**：分支 `develop`；本轮**远端写两处**（推送 `v0.1.0` 标签、推送 `develop` 的回合结果 ——
均属所有者已明确指示/授权的动作，**未触碰 `main`**）；本地一笔 = 本记录；
**验证方式**：上述 ①~④ 的核对命令与输出；`main` 上的版本四处副本 = `0.1.0`。

⚠️ **一条留在 §7 的提醒**：本波我**漏开 PR** 这一步（§3.20 已登记）——流程的三段是
「**代理开 PR → 所有者合并 → 代理打 tag / 回合**」，**代理不得自行合并到 `main`**。

### 3.22 `M2` 判据达成：多步会话断言落地 + 两轮独立取证（含三次**同一族**失真更正）

**本波分两波、串行**（两波都要起真实 `llama-server`，8 核同机并行会互相抢 CPU 并把对方拖进请求超时
⇒ 按「能串行就不并行」）：
`implementer-m2step`（波 1：多步断言落地）→ `verifier-m2evidence`（波 2：独立取证）。**A 类报告四项**：

- **分支名**：`develop`。
- **提交列表**（旧→新）：`df8d245`（`test(integration): 新增多步工具调用会话的端到端与审计回放断言`）、
  `922ffdc`（`docs(research): add M2 exit-criteria independent evidence`）、
  `b9604d9`（`docs(research): correct fabricated exit-code claims in M2 evidence report`）、
  `26f89d4`（`docs(research): correct fabricated deny-path claims in M2 evidence report`）。
- **文件清单**：`tests/integration/test_end_to_end.py`（`+346/-8`）与
  `docs/research/2026-09-20-m2-exit-criteria-evidence.md`（321 行；后两笔更正经 `+66/-6`、`+22/-4`）。
- **验证方式**：① 产品 CLI 真实多步会话（非交互、`--output-format json`）⇒ 退出码 `0` / 墙钟 184 s /
  事件流 `seq` `0..9` 连续 / 恰一条 `TASK_FINISHED(completed)` 在末尾 / 审计 **2 条 `TOOL_CALL` 且 `OK`**
  （报告 §A）；② 真模型集成套件复跑 ⇒ 退出码 `0`、**`10 passed` / `0 skipped`**、`466.55 s`（§B）；
  ③ `make check` 全绿（**`955 passed`**，skip 归零）。

**我的独立核验（不采信自报）**：逐笔 `git show --stat` ⇒ 四笔**均只动各自白名单内的文件**
（`df8d245` 只动 `tests/integration/`，其余三笔只动同一份报告）；`src/`、`docs/engineering/`、CI、
威胁模型**零改动**；四笔正文**均含 `回报：` 块**且与本域改动**同提交**（非空提交）；
`HEAD == origin/develop == 26f89d4`。`implementer-m2step` 的三条新断言我**逐行读过**：不是恒过写法
（"审计 `OK` 条数 == 事件流里已执行且成功数"是**双向**对齐；回放读侧**另起** `JsonlAuditSink`
从落盘文件读，不复用会话期写侧对象）。派活前我**自己核过**多步能力：`harness/loop.py` 的循环在模型
继续返回 `tool_calls` 时继续走（`max_steps` 默认 12，BASIC 档预算含 `READ_FILE` 且 `list_dir` 声明的
也是 `READ_FILE`）⇒ 本轮**不需要改 `src/`**，事实也确为零改动。

**本波最该带走的东西：同一族失真出现三次，且都发生在"报告"这件事里。** 第一次是我读出来的：
`922ffdc` 把 CLI 的退出码机制写成 `_EXIT_STATUS` / `EXHAUSTED` / `DENIED` / `MODEL_UNAVAILABLE` /
`INTERRUPTED→130` —— 这些常量与状态**在源码里全都不存在**（`grep` 零命中）。这不是措辞不准，
是**编造 API 行为**，与 `SECURITY.md` `S-8`（声称已生效的缓解必须能被实测）同族。追加指令后它改对了
（`b9604d9`：删除线 + "该表述失实" + 真实常量与行号）—— 但**同一族还剩第二处**（"被拒绝的工具调用
落到 `tool_result.ok=false`"，而 `harness/loop.py` 的 `_deny` 产出的是 `result=None`，契约明令
"**拒绝 ≠ 失败**、两者在事件与审计里不同形"），第三轮才清干净（`26f89d4`，并补出"连续被拒到上限
会 `STALLED`→`FAILED`"这条它自己核出来的收敛路径）。⇒ **两条纪律**：

1. **"逐条核出处"要一次性要求，不能逐轮追问**：我第一次只点了退出码一处，第二轮才发现拒绝路径同病
   ⇒ 正确的问法从一开始就是"把报告里**所有**声称某符号 / 某行为存在的陈述逐条回源码指出处"
   （第二轮才这么问，它据此产出了"二次复核清单"）。**改对一处 ≠ 清干净一族** —— 这与本项目
   「规则变更的联动更新」那条**反向搜索**教训完全同形，只是这次发生在一段文字里。
2. **领导给线索、成员给结论**：我**没有**把"正确的退出码表"塞进提示（那正是"派活提示不得替成员描述
   它没核过的事实现状 / 文本位置 / 机制归因"），只给了**可复现的 grep 依据**与"请自己读源码复核"。
   三次更正**都是它自己从源码读出来的**。

**两条正面记录**：① 它的核验脚本把字符串与枚举用 `is` 比较，产出 `match_outcome=False`，
它**主动在报告里标注"这是我的脚本 bug，不是产品缺陷"**（脚本留 `/tmp`、不进仓库）—— 与"成员不顺从
领导口径、先核再落笔"同一取向；② 报告 §C.2 是本轮最有价值的**负向发现**：判据与测试措辞写
"覆盖 > 1 个不同工具**或不同参数**"，而实现里**调用参数根本进不了事件与审计**（`I8` / `D2`）
⇒ "不同参数"只能以"**不同读取内容**"为代理观察，**不是**对参数的直接断言。它**没有把这个近似
写成"已验证"**。

**我的两项裁决**：

| 待裁决项 | 裁决 | 理由 |
| --- | --- | --- |
| 补 `sdlc.md` §3.3 的 `M2-1` 示例命令前提（pack 须在受信根内 / 须显式授予能力 / token 与超时属模型相关） | **采纳**，且**只补前提、判据文字一字不动** | 机制由我独立核实：`foundation/config.py` 的 `granted_capabilities` 默认为空（默认拒绝）；`harness/domain_pack.py` 的 `load_pack` 走 `resolve_within(directory, roots)`、`roots` 省略时取工作目录 ⇒ 指向仓库内 `examples/packs/...` 会 `PathNotAllowedError`。并一并写明退出码语义（**`2` = 步数用尽；超时 / 模型失败是 `1`**），避免读者按错误信号排查 |
| 是否把"第 2 步的输入来自第 1 步的输出"（数据依赖）纳入自动断言 | **本轮不纳入**，登记 §7 | ① 判据原文**未要求**；② 参数不可观察 ⇒ 只能写成"读到的文件名出现在 `list_dir` 输出里"这类**弱代理**，而"看起来验证了依赖、其实没有"**比没有断言更坏**；③ 真钉死它需改契约（让参数可观察）⇒ 属架构师 + ADR 的事，**不顺手做** |

**`M2` 判定与回填**：`M2-1`~`M2-4` **全部满足** ⇒ **`M2` 出口已达成**；`docs/engineering/sdlc.md` §3.3
的现状列已由**团队领导**回填（与 §3.19 同一先例：落的是**已核验的结论**，不是新决策），并写明
**三条不得放大**（不构成安全已到位 / 未测性能 / `0.2.0` 落定是**另一件事**、须由所有者按 `ADR-0019` 决定）；
`M1-1`（SRS 成稿）**不因 `M2` 通过而自动满足**。

**一处操作事实（与 §3.13 同族）**：波 1 成员的 `shutdown_response` **迟到**在 `team_delete` 之后才到达
⇒ 又一次印证"**回报可能延迟、完成判定只认仓库**"；两次收尾都执行到**从名册移除**（`team_delete`），
未留常驻成员。

**A 类报告（本节末笔）**：分支 `develop`；**提交列表**：`de240d1`（`docs(sdlc): M2 出口判据四条现状回填与示例命令前提补注`）
与 `bce9a55`（`docs(devlog): 0019 追加 §3.22（M2 判据达成与两轮独立取证）并更新 §7`）；**文件清单**：
`docs/engineering/sdlc.md`（§3.3 现状回填 + 前提补注）、`docs/devlog/0019-…md`（本记录）；
**验证方式**：`make check` 全绿（**`955 passed`**、`skip` 归零；bandit / detect-private-key / pip-audit 全过）；
**独立核验**：`git diff --stat` 确认威胁模型 / `src/` / `tests/` 零改动（分布仍 `1 / 10 / 2`、§5 仍 `6`）。
（本记录自身的最新一笔哈希由下一次更新回填 —— 见 [`docs/devlog/README.md`](README.md) 的哈希引用规则。）

---

## 4. 数据与证据

（待记）

---

## 5. 问题与卡点

- 🔴 **威胁模型 13 条里 0 条"已缓解并验证"** ⇒ 任何对外表述**不得**出现"安全已到位"。
  本阶段的核心工作就是把这个数字往下推（**用证据**，不是用措辞）。
- ⚠️ **`M1` 的 SRS 与设计文档存在分叉**：SRS v0.1.3 与 `devlog 0004`/`0005` 的讨论结果不一致
  （`S-1`~`S-10` / `Q-1`~`Q-8` **待所有者确认**）⇒ 在确认前，`REQ-*` 与
  `docs/design/interfaces/` 的一致性核查只能做到"**能核的先核**"。
- ⚠️ **可用性缺口的措辞纪律**：`HIGH` + `R1` 拒绝是**刻意的 fail-secure**，
  它的后果（非交互 + 无 pack ⇒ 工具不可执行）是**可用性问题**——登记与提案时**必须这么表述**，
  不得写成"安全缺陷"（否则会被误读成需要下调默认风险等级）。

---

## 6. 学到了什么

（待记）

---

## 7. 下一步（= 当前活待办清单）

> ⚠️ **新会话从这里开始读**（「重拾语境」第 4 步）。承接自 [0018](0018-2026-09-19-安全行为覆盖与cli装配.md)
> §7 的**未完成项**（旧篇保留原文，此处是**唯一有效**的清单）。

### 🔴 优先级 H：本阶段主线

- [x] **发布流程已走完（2026-09-20）**：**PR #8 已由所有者合并**（合并提交 `14000b3`，真正的 Merge commit，
  父为 `7138007` ⊕ `9e314a0`）⇒ 代理已在该提交上打**带注释标签 `v0.1.0`** 并推送；
  `main` 已**回合** `develop`（fast-forward）。**三方一致**：`develop` = `origin/main` = `v0.1.0` = `14000b3`（见 §3.21）。
- [x] **`M2` 判据已达成（2026-09-20）**：`M2-1`~`M2-4` **全部满足**（判据 `docs/engineering/sdlc.md` §3.3，
      现状列已回填；证据 = 验证角色独立实跑/复跑 `docs/research/2026-09-20-m2-exit-criteria-evidence.md`
      + 机器检查载体 `tests/integration/test_end_to_end.py` 的 `multi_step_session`，`df8d245`）——
      判定结论与三条限定见 §3.22。⚠️ **达成 ≠ 安全已到位**；性能未测。
- [ ] **【待所有者决定】`0.2.0`「端到端主流程可运行（Alpha）」是否落定**：台账判据已满足
      （`pyproject.toml` 的 `[tool.lowspec.releases]` 指向 `sdlc.md` §3.3）；
      落定流程 = `make release VERSION=0.2.0` → **代理**开 PR → **所有者**合并到 `main` →
      **代理**打带注释标签 + 回合 `develop`（`git-workflow.md` §5.3 / `ADR-0019` §5.7）。
      ⚠️ **代理不自行决定发布**（放开的是"谁执行已获准的发布动作"），等所有者裁决。

**对账第一件事（不要凭本文件下结论，看仓库）**：

```text
git log --oneline -15 && git status --short
uv run pytest -n auto -m "not benchmark and not slow" -q   # 建篇时基线：922 passed（slow 已排除）
# 对每名成员的最后一笔：
#   git show --stat <hash>
#   git show -s --format=%B <hash> | tail -20     ⇒ 末尾有没有 `回报：` 块
#   没有 ⇒ 状态只能记「未确认」（**不得判失败、不得据此重派**）
```

- [x] **结转项 1：`implementer-e2e-fix` 已完成并 shutdown**（三笔 `2d011ad` / `b1fc35a` / `6b011ad` /
      `a80e5d9`；真模型端到端 **7 passed / 273.47 s**；见 §3.3）
- [ ] **结转项 2：`architect-wave7` 未确认、零产出** ⇒ 已重派 **`architect-wave8`**
      （与 `architect-wave7` 同任务，写得更细：逐条列出过时位置 + 只改事实描述 + 不动 `T-XX`
      结论与计数 + **补做第八版复核**）—— 见 §3.4。⚠️ 它交出的东西**只认仓库**，
      收到 `回报：` 块前**不得**认为已完成
- [x] **`architect-wave8` 第一轮 6 笔已核验**（`0564d48..1e993bf`；见 §3.5）：契约**第十二/十三版**落地、
      `ADR-0015` 追加「续四」、`threat-model/*` 实现状态同步 —— **`T-XX` 计数 0/8/5 与 `T-09`/`T-10` 原样**
- [x] **`architect-wave8` 第二轮 4 笔已核验**（`1e993bf..5ae3628`；见 §3.6）：遗留 ③（`assets.md` 的
      `B-3`/`B-5` → 已实现、**`B-7` 保留"未实现"**）、④（`architecture.md` 的 `cli/` 与其余实现状态）；
      **计数一个未动**、**`P-1` 已登记为新 §8.1** ⇒ 该成员已 shutdown
- [x] **`P-1` 已裁决**（独立验证 `20da806` + 我的决定；见 §3.7）：`T-11`/`T-12` 升**部分缓解**、
      `§4.1` → `0 / 10 / 3`、`§5` → **8 条**；**`T-02` 维持原状**（负向发现：
      `test_audit_landing_whitelist.py` **不构成**其"且留审计"半的证据）⇒ 已派 `architect-wave9` 落盘
- [x] **`architect-wave9` 3 笔已核验**（`ed28085..d945b0c`；见 §3.8）：头条数字落为 **0 / 10 / 3**、
      `§5` **8 条**、缺口逐字写入条目、`T-02` 防误引登记、`§8` 的 `P-1` 标"已裁决" ⇒ 该成员已 shutdown
- [x] **`tests/security/test_s1_replayable_audit_link.py` 的过期 docstring** 已同步（`20da806`：
      两条阻塞已解除，并写清本文件与 `S1` 授权文件**互补**；断言与跳过条件一字未动）
- [x] **`verifier-t02-audit` 3 笔已核验**（`c6b32f7..b79f383`）：取证结论是"**运行时已被留痕**"
      ⇒ **推翻**了"`T-02` 审计半无载体"这一**已写进仓库**的说法（见 §3.9）；
      并补 `W3` 用例、`UNC` 判为不构成独立绕过面（**登记为缺口、不写弱断言**）⇒ 该成员已 shutdown
- [x] **`architect-wave10` 5 笔已核验**（`382be98..fc94581`；见 §3.10）：`T-02` 更正为
      "载体已实现 + 两处行为层证据"（**保留**更正说明，未静默改口）、`§7` 登记假声明事故、
      `§8.2` 新增 `P-2`/`Q-2`；**计数仍 0/10/3、§5 仍 8** ⇒ 该成员已 shutdown
- [x] **`Q-2` 已裁决并关闭**（我）：**维持 `outcome=ERROR` + `detail.reason="path_not_allowed"`**
      —— 改 `DENY` 会与"策略/审批导致未执行"**同形**（原因不同的两类拒绝在审计里不可分）；
      该区分已由 `test_t02_path_traversal_audit.py` 机器钉住。详见 §3.10
- [x] **`Q-1` / `P-2` 已由所有者裁决（2026-09-20）**：`Q-1` = **不纳入**
      （配置期/装配期拒绝 = **fail-closed、无审计、理由写明** ⇒ **零 ADR、零新模块**）；
      `P-2` = **升级** ⇒ `T-02` 升「**已缓解并验证**」（**项目首次**）⇒ 分布 **0 / 10 / 3 → 1 / 9 / 3**。
      （原"`T-02` 若确需改动分层 ⇒ 须 ADR"一项随之**关闭**：`Q-1` 不纳入 ⇒ 不动分层。）
      落盘与下游同步见 §3.11
- [x] **`Q-1` 的范围声明已写进契约**（`3963775`）：`interfaces/audit.md` 新增 **§2.6「审计覆盖范围」**
      （**`COV1`~`COV5`**，代号刻意避开 `S1~S3`/`P1~P7`/`W1~W8`）：覆盖 = **会话内**工具层 / 能力层；
      配置期 / 装配期拒绝不进审计，**fail-closed 且理由随口径写明**（会话与 sink 都不存在 +
      落点白名单尚未校验 ⇒ 与**攻击面 6 循环**）；并写明**不新增模块 / 不改 `R1` / 不得不外推**
- [x] **六个"现行文档"的安全状态同步已完成**（`6785ac6..71fa9a0`，4 笔）：`0 / 8 / 5` 与
      "`S1`/`S3` 未落地" ⇒ **1 / 9 / 3** 与**已落地**；`architecture.md` 的 §7.2 表头日期、
      `sdlc.md` 的出处自洽性随后校准（`71fa9a0`）—— 核验与裁决见 §3.11
- [x] **威胁模型本体已落盘 `1 / 9 / 3`**（`83630c8`）：`T-02` 三条残余风险与"证明了什么 /
      不证明什么"、§5 **复算后仍 8**（附计数沿革）、`Q-1`/`P-2` 的【裁决后状态】、
      历史数字**只追加不覆写** —— 见 §3.11
- [x] **`Q-2` 正文补【裁决后状态】块**（`8d0315d`）：写明裁决主体是**团队领导（2026-09-19）**、
      三条理由与后果；原"现状：未决"原文保留（只增不改）
- [ ] **把两条实测失效模式登记进 `docs/engineering/agent-teams.md` §6**（与既有"文件域独占
      **不覆盖 git 索引**"同族）：
      ① **`.git/COMMIT_EDITMSG` 同样是全局共享**（见 §3.11）⇒ **领导与成员同时提交会互相打断**；
      ② **成员"完成"后会从名册消失**（§3.13 第 4 次实测：`Known members: none`）⇒
      **"完成即关闭"不能只停在 `shutdown_response` 的"批准"**，且**追加指令可能投不出去**
      —— 收尾应改走**前台子任务**（不开团队，无残留实例，也避免闲置成员造成的卡顿）
- [x] **`T-09`/`T-10` 保留原样**（已核实仍成立：权重/二进制摘要校验、未授权出站的**执行机制**均无载体）
- [x] **`cli/` 交付后遗留的文档同步**（0018 §3.6(e) 登记）：已由 `architect-wave8` 第二轮
      （`1e993bf..5ae3628`）完成，本次对账逐处复核确认 —— `architecture.md` §0 的"未开工"清单
      **已不含** `cli/`、§1.1 的 `UX` 节点为"3 件已实现"、§4.3 的 `cli/` 行为"已实现"、
      §5.1 的时序图参与者为"已实现"
- [ ] **【架构师可裁】模型请求超时的落点**（0018 §3.11(e) 第 1 条）：现为 CLI 侧注入层；
      替代 = 给 `LocalLlamaClient` 加 `request_timeout_s`，或给 `SessionConfig` 加字段（**属契约变更**）
- [ ] **`§7.1`（7 项）与 `§7.2`（4 项）逐项复核**：均有落点，但**复核 ≠ 看它"看起来落地了"**
- [x] **收尾**：`shutdown_request` 给全部成员（**6/6 均已批准**）→ `team_delete`（**用完即删**，不回头删旧团队）
      ⇒ 团队 `phase1-harness-wave5` 已删除；收尾时核对 `HEAD == origin/develop == bd05859`、工作树干净、
      **927 passed / 7 skipped**、本波共 **29 笔**提交
      ⚠️ **一条操作事实**：`shutdown_response` 的"批准"**不等于**成员已从名册移除
      （本轮实测：`architect-wave8` 已批准关闭后重派仍报 `already exists`，只能换名）

### 🔴 优先级 A：把安全断言推进为"已缓解并验证"

- [x] **8 条「缺行为层验证」条目的独立复核**（`476c5b9`；见 §3.12）：`T-03`/`T-04`/`T-05`/`T-06`/`T-07`/
      `T-09`/`T-10`/`T-13` 逐条从源码重推「断言了什么 / 没断言什么」⇒ 发现 **2 处声明失真（方向为低估）**、
      **3 处误引风险**、**1 处清单与仓库漂移**；**未发现"绿着但没生效"的恒过用例**
- [ ] **复核剩余 5 条**（`T-01`/`T-02`/`T-08`/`T-11`/`T-12`）：它们"已有行为级用例" ⇒ 复核重点是
      **证据是否名副其实**（例：`T-11` 的"工具滥用"半**零证据**、`T-02` 的三条残余风险）
      —— **不得**因"已有用例"就跳过（现状分布 **1 / 9 / 3**）
- [x] **`Q-3` 已裁决并落盘**（所有者 2026-09-20；`d20671e` / `2b63a5e`；见 §3.13）：
      `T-03` 升「**部分缓解**」⇒ **`1 / 10 / 2`**（未缓解只剩 `T-04`/`T-10`）；
      并加**适用澄清**（「未缓解」= **决定性缓解机制**未建立 ≠ 完全没有载体；决定性 = 能**阻断**攻击路径）
      ⇒ 该澄清同时给 `T-04`/`T-10` 写出了"为何仍不够"，**不降低判据强度**
- [ ] **【待议，改口径须 ADR / 所有者】** §5 的**字面口径**（"有无行为级用例"）会在只有**一条边界用例**时
      就把条目移出 ⇒ **数字变小 ≠ 缺口变小**；候选改为"是否覆盖该条**核心**攻击路径"（见 §3.12 末段）
- [ ] **【下轮修】审计写入失败缺"可识别类型"**（0018 §3.8(d)）：审批路径上它被 `loop` 的 `R3`
      收敛为 `ERROR(INTERNAL)`+`FAILED`，与 `audit.md` §2.4"必须冒泡"不完全一致；
      缺口边界 = "**退出码正确、事件流响亮，但 `text`/`error_kind` 不可分**"
      ⇒ 修法：`AuditWriteError`（带 `__cause__`）+ `loop` 在 `R3` 前 `except …: raise`；
      **属契约变更 ⇒ 须架构师 + 验证者参与**
- [ ] **【新登记】"工具参数声明缺陷"与"模型给的参数不合法"在审计里同形**：契约 §2.7 的
      `denied_reason` 是**闭集**、没有"声明缺陷"一档 ⇒ 候选：加第六档（如 `schema_defect`）
      或只把"声明缺陷"写进审计 `detail` 而不动 `denied_reason`；**属契约变更，须架构师/领导裁决**
- [x] **`audit.md` §2.2 的 kind→outcome 约束表已补机器检查**（`7520787` / `51bc4db`；见 §3.12）：
      文档↔声明双向比对 + 四个 `emit` 调用点实际组合断言 + 变异探针 + **调用点结构性覆盖**（AST 判定
      producer/透传，含"登记不实"与"登记漂移"双向断言）；**`src/` 未改**（**运行期 `emit` 校验被我否决**：
      与 `REQ-SEC-06`「不丢证据」冲突）
- [ ] **`EXECUTION_DEGRADATION`/`REFUSAL` 无 producer 的覆盖边界**（已派 `architect-wave13` 登记）：
      这两行约束当前**没有实现侧证据**（`security/sandbox/`、`model/router.py`、`security/refusal.py` 均未实现）
      ⇒ producer 落地时**必须**补触发用例
- [ ] **`S1-c` 的日志面当前"空通过"**（0018 §3.8(a) 的遗留）：`caplog` 检查通过是因为 harness
      目前**不记录参数原文**——**不得**把它读成"日志面已验证"；将来若新增参数日志须补强

### 优先级 B：`M1` 的交付物（`Phase 1` 出口）

- [ ] **SRS v0.2 收敛**：与 `devlog 0004`/`0005` 的分叉（`S-1`~`S-10` / `Q-1`~`Q-8`）**待所有者确认**；
      `REQ-*` 与 `docs/design/interfaces/` 的一致性一并核
- [ ] **威胁模型成稿**（含"部分缓解 ⇒ 已缓解并验证"的推进路径与证据）
- [ ] **`sdlc.md` §3.3 已批准（2026-09-20，所有者）** ⇒ 台账 `0.2.0` 的"批准前不得据此落定"**已随之更正**；`M2` 当前四项判据（`M2-1` 多步会话 / `M2-2` 真模型用例可复跑 / `M2-3` `make check` 全绿 / `M2-4` 多步审计可回放）**均未满足**，属下一阶段主线（见 §3.19）
- [ ] **`M3`/`M4` 与版本号的映射未定**：`git-workflow.md` §5.2 的台账只列到 `0.2.0`；进入 `1.0.0` **需先改机器检查本身**（`tests/unit/test_release_policy.py` 当前拒绝 `1.0.0`），属一次显式决策
- [ ] **`ADR-0021` §7 第 2 条的"复核时点"**：`0.1.0` 发布时或 `examples/` 首次因 `pack.toml` schema 变更而失效时，复核第 1/2/5 条
- [ ] **README 的端到端配方数值（`1536` / `600`）属模型相关**：换用其它模型必须重新实测 `--max-completion-tokens` 与超时（本次 `384` 在 Qwen3-4B 上 `ModelProtocolError` 失败）

### 优先级 C：欠债与收尾

- [ ] **【规则矛盾，待所有者确认后修正（改 Skill = 改"规则本身" ⇒ 属 `F` 类）】团队 Skill 第 3 步与权威源冲突**：
      `.codebuddy/skills/agent-team-protocol/SKILL.md` 仍写「派活后核成员信箱、空数组 ⇒ 判投递未发生 ⇒
      可立即重派」，而它自己引用的权威源 [`docs/research/2026-09-18-team-dispatch-not-delivered.md`](../research/2026-09-18-team-dispatch-not-delivered.md)
      **§4.2 已明确推翻该判据**（§3.1 更正：成员正常工作时信箱也可以是空的）⇒ 照它做会**据假信号重派**
      （= 重复劳动 + 同一文件被两人写，正是本项目最忌的失效）。本轮实测信箱为 `[]`，我按**权威源**判
      "投递成功（Task 返回成功）⇒ 只记未确认"、**未重派**。Skill 自身写着"若指针失效，以权威源为准，
      并就地修本 Skill"，但改 Skill 属"规则本身" ⇒ **先请所有者确认再动**（见 §3.22）
- [ ] **【下轮，`tests/` 域】`tests/integration/test_end_to_end.py` 关于"不同参数"的注释口径**：
      报告 §C.2 指出该处按判据字面写"覆盖 > 1 个不同工具或不同参数"，而实现里调用参数**进不了事件与审计**
      （`I8` / `D2`）⇒ "不同参数"只能以"不同读取内容"为**代理**观察。是否补一句近似说明交下一轮
      （**只把口径写准，不动断言语义**）
- [ ] **【`F` 类，待所有者决定】把"`git-workflow.md` §5.2 表 ↔ `pyproject.toml` 台账"的同步落成机器检查**：现状是**人工同步、漏改不报错**（见 §3.17 与 §3.18；已登记为一致性报告 **`A-18`**，其 §D 处置表同载）；落成检查 = **改门禁强度** ⇒ **须事先确认**，不得顺手新增
- [x] **`CODEBUDDY.md` / `AGENTS.md` §10 的常驻指针已落地**（2026-09-20，所有者批准 = `F` 类事前确认）：门禁假红的判别式已写入两处 §10（**逐字等价**，2026-09-20 实测，见 §3.19）
- [ ] **`ADR-0021` §7 第 4 条的机器检查**：领导已裁决**暂不落地**（理由见 §3.14）；若日后 `examples/` 内可能出现可导入模块，须重开此裁决
- [x] **审计落点的 `A1` / `A2` 已拍板：维持最保守取值**（2026-09-20，所有者）⇒ `architecture.md` §11 的 `G-5` 转「已拍板」、`interfaces/audit.md` §5 登记裁决；`ALLOWED_AUDIT_ROOTS` **不含**额外根、**保留** `audit.directory` 键，实现与单测**零改动**（见 §3.19）
- [ ] **`git-workflow.md` §4 的措辞量纲统一**（"5 处" vs "三者"，不影响分级集合）
- [ ] 镜像重建后核 `V-15`/`U11`、`U7`~`U12` 继续核验；
      `T3`（令牌轮换）是**所有者动作，代理不得代办**
- [ ] `agent-teams.md` §7 的三个未验证项、`S-8` 系列的"写了但没跑"扫描
- [ ] PR #6 的合并后复核（可选，低优先）：核对 `main` 上的 CI 结果与 PR 页面 `merged` 状态

### 已关闭（供回溯，不再维护）

- [x] **安全行为覆盖**（`tests/security/` **28 条**，验证角色独立完成，含 `S1`/`S1-b`/`S1-c`/`S3`/`S-new-1`~`6`）
- [x] **`cli/` 三件齐备**（`render` / `approval` / `app` + `[project.scripts]` 入口，`REQ-UX-03`）
- [x] **真模型端到端 `7 passed`**（273.47 s；`AGENT_SEC_PERF_E2E=1` 才跑）与 **`slow` 并入默认排除集**（F 类，所有者批准）
- [x] `ADR-0020` 获批准并落地（手写 JSON-Schema 子集校验器）+ `T6` 裁决（`specs()` = 全量）
- [x] harness 基线 9 件 + 契约收敛到第十一版 —— 见 0017 §3.7~§3.10、0018 §3.1~§3.11
