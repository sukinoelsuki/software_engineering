# 证据附录（随包证据最小集）

> **本文件回答一个问题**：`01` 里那些"实测 / 已验证 / 已实现"的判定，**证据在项目的哪个文件里、
> 用哪条命令能复跑**。
>
> 配套：[`01-需求分析文档.md`](01-需求分析文档.md)（结论）、[`03-人工核实说明.md`](03-人工核实说明.md)
> （谁核的、核到什么程度）、[`00-独立读者导读.md`](00-独立读者导读.md)（包外概念）、
> [`07-原始输出附录.md`](07-原始输出附录.md)（**本文件的"摘录"对应的机器原始输出**）、
> [`08-评审意见与处置对照.md`](08-评审意见与处置对照.md)（外部评审意见与逐条处置）。
>
> **为什么单列这一份**：`01` 的每条结论原本只能追到"文档内部的交叉引用"，包外读者看不到原始依据。
> 本附录把**最关键的那批**证据以**摘录 + 定位 + 复跑命令**的形式随包给出。
> ⚠️ **它是摘录，不是副本**：完整原文仍以项目仓库为准，本附录给出**可定位到文件与行**的指针。

---

## 0. 证据的分级（先说清楚每一条算什么）

不是所有证据强度相同。本附录对每条证据标**级别**，读者按级别决定信多少：

| 级别 | 含义 | 包外读者能做什么 |
| --- | --- | --- |
| **E-1 可一行复跑** | 给出命令，任何人拿到仓库即可复现同一输出 | 自己跑，完全独立复核 |
| **E-2 可定位到文件行** | 给出文件路径与关键行号/原文摘录，读即可核 | 打开文件对照本附录的摘录 |
| **E-3 需人工裁决** | 结论依赖所有者的一次裁决（原话/登记处已给） | 只能核"裁决确实存在"，不能复核裁决本身 |

> 凡本附录未标级别的内容，一律**不是证据**，只是导航说明。

**本包的诚实边界（先声明，避免被读成"全都验过了"）**：
本附录**不声称**下列三件事——① 图表渲染效果已验证；② 跨机/换模型可复跑；③ 用户画像经过调研。
这三项在 `03` §3 分别登记为 `N-14` / `N-09` / `N-13`。

---

## 1. 假设 A-1：4B 级模型在限定任务域内可产生实际价值

| 项 | 内容 |
| --- | --- |
| 级别 | **E-2**（原文可定位）+ **E-3**（阈值 30% 是本人设定的判据，非客观常数） |
| 依据文件 | `docs/research/2026-09-15-w1-4b-model-capability.md` |
| 复跑 | 需本地推理服务与 4B GGUF（**非一行可跑**）⇒ 本附录按 **E-2** 处理 |

**原文摘录**（`docs/research/2026-09-15-w1-4b-model-capability.md`）：

```text
| 严格：交付原样、零修改、全部客观判据通过 | 1 / 3 = 33% | 仅 T2 完全达标 |   （:114）

## 5. 结论
| Q1：能力是否够用 | 风险 R-1 未触发。最严格口径下 33%（略高于 30% 阈值），
  实用口径 100%。 | 置信度：中 | 依据强度：3 项任务、每项单次采样、`--reasoning off` 单一档位 |   （:140-142）

注意：这意味着结论只代表"非思考模式"下的能力，思考模式下的上限更高。   （:45）
```

**它支撑什么 / 不支撑什么**（`01` §1.4 的 A-1 行即按此口径写）：

- ✅ 支撑："4B 级模型在**限定任务域**内可产生实际价值"——严格口径 33% > 自设阈值 30%。
- ❌ 不支撑：**单样本**（每任务 1 次）、**单档位**（未开思考链）⇒ 置信度记为**中**，
  不得外推为"4B 在任意任务上可用"。

> ⚠️ 同一份笔记里另有一条**必须连读**的发现（`:133`）：一项失败是**测试夹具自身的 bug**，
> 若不排查就直接记为"模型失败"，结论会偏差很大。⇒ 这正是本项目坚持"**结论必须带口径**"的由来。

---

## 2. 假设 A-2：受限容器内的命名空间沙箱 —— **已证伪**（含失效方式）

| 项 | 内容 |
| --- | --- |
| 级别 | **E-2**（探针与原始输出在 ADR 内逐行收录） |
| 依据文件 | `docs/adr/0006-sandbox-execution-degradation.md` |
| 它改变了什么 | 直接改写 `REQ-SEC-05` 的验收标准（`01` §1.4 / §8.2） |

**原文摘录**（`docs/adr/0006-sandbox-execution-degradation.md`）：

```text
原定的沙箱路线是用户态命名空间隔离：主方案 bubblewrap，备选 firejail   （:21）
该假设不成立，且失败方式比预期更危险——其中一个方案是静默失效（fail-open）。  （:27）

| firejail 网络隔离 | `firejail --quiet --noprofile --net=none curl https://example.com`
  | `HTTP 200`（网络未被阻断） | ❌ 静默失效 |                                    （:93）
| firejail 诊断输出 | 去掉 `--quiet` 重跑
  | `Warning: an existing sandbox was detected. /bin/sh will run
     without any additional sandboxing features` | ❌ 根因确认 |                （:94）

关键教训（安全）：firejail 的失败方式是 fail-open ——                        （:105）
```

**为什么这条重要（读者最该带走的一点）**：
它不是"某个工具不好用"，而是**"以退出码判定隔离是否生效"这种判据本身会骗人**——
`firejail` 退出码 `0`、输出正常，但**网络根本没被隔离**。
⇒ `REQ-SEC-05` 因此从"用命名空间隔离"改写为"**L1/L2 分层 + 主动探针 + fail-secure 降级**"。

---

## 3. 假设 A-3：三档的内存与速度代价（部分成立）

| 项 | 内容 |
| --- | --- |
| 级别 | **E-2**（数字由推理服务自报，原文含服务日志） |
| 依据文件 | `docs/research/2026-09-15-tiers-s-m-l-comparison.md` |

**原文摘录**（`docs/research/2026-09-15-tiers-s-m-l-comparison.md:75-83`）：

```text
| S | MiniCPM5-2B-Q4_K_M | 1.56 GB | 1.28 s | 2.68 GiB | 119.1 tok/s | 27.2 tok/s | 35.9 s |
| M | Qwen3-4B-Q4_K_M    | 2.50 GB | 2.55 s | 4.85 GiB |  67.9 tok/s | 16.6 tok/s | 73.8 s |
| L | Qwen3-8B-Q4_K_M    | 5.03 GB | 3.57 s | 8.70 GiB |  35.8 tok/s |  9.4 tok/s |132.3 s |

2. 实测内存与档位预算相符：2.68 ≤ 4 GiB（S）、4.85 ≤ 8 GiB（M）、8.70 ≤ 16 GiB（L）。 （:83）
```

**"部分成立"究竟差在哪（写清楚，不含糊）**：

- ✅ 已证：三档**常驻内存**与档位预算相符 ⇒ `REQ-MODEL-02` / `REQ-PERF-01` 的预算口径站得住。
- ❌ 未证：**在 8 GiB 硬限制（cgroup）下端到端跑完一轮任务**未做 ⇒ `01` §1.4 的 A-3 标"部分成立"，
  在 `03` §3 登记为 `N-06`。**本附录不替它补实验**，只把缺口指到具体位置。

**另一条同样必须连读的负向结论**（`docs/research/2026-09-15-tiers-s-m-l-comparison.md`，见 `03` 的 V-04）：
**"能力随档位递增"在本任务集上未获支持**（任务对最小档偏易）⇒ `01` §8.2 把档位的价值改写为
"**按资源预算选可承受配置**"，而不是"更强硬件给更强模型"。

---

## 4. 威胁模型状态分布（1 / 10 / 2）

| 项 | 内容 |
| --- | --- |
| 级别 | **E-1**（`grep` 可一行复跑）+ **E-2**（表格可定位） |
| 依据文件 | `docs/design/threat-model/README.md` §4.1 |

**原文摘录**（`docs/design/threat-model/README.md:210-215`）：

```text
| 已缓解并验证 | 1  | T-02（2026-09-20 由「部分缓解」升，所有者裁决 P-2 —— 本项目首条） |
| 部分缓解     | 10 | T-01、T-03、T-05、T-06、T-07、T-08、T-09、T-11、T-12、T-13        |
| 未缓解       | 2  | T-04、T-10                                                        |
| 合计         | 13 |                                                                   |
```

**复跑**（E-1，不需任何环境依赖）：

```bash
grep -n "已缓解并验证\|部分缓解\|未缓解" docs/design/threat-model/README.md | head -20
```

**⚠️ 三个数字必须连读的限定**（原文同处，防止"1"被读成"安全已到位"）：

1. `T-02` 的"已缓解并验证"是**单条目、单调用面**的结论：只覆盖**会话内工具层**的路径穿越
   "被拒 **且** 留痕可回放"；
2. **未缓解**是 `T-04`（提示注入）与 `T-10`（网络出站默认拒绝失效）；
3. `T-02` 自身仍带**三条残余风险**（UNC/盘符跨平台未验、"别处自建路径判定"未覆盖、
   配置期/装配期调用点不进审计——后者由所有者裁决 `Q-1` 定为**范围外**）。

---

## 5. 门禁与安全层计数

| 项 | 内容 |
| --- | --- |
| 级别 | **E-1**（一行可复跑） |
| 复跑命令 | `make check` / `make test-security` |

**本包提交前的实测输出（复跑即得，数字带时间点）**：

```text
$ make check
  ...
  SKIPPED [1] tests/unit/test_release_policy.py:373: 台账里没有高于当前版本的里程碑作目标：
      当前即最高一级，这是合法状态，本用例无可检验的目标
  ======================= 956 passed, 1 skipped in 8.79s =======================
  uv run bandit -q -r src                        （无告警）
  detect private key ......................... Passed
  No known vulnerabilities found                 （pip-audit）

$ make test-security
  96 passed, 871 deselected in 1.25s
```

**三条必须知道的口径**（否则会对不上别的材料里的数字）：

1. **956 / 955 / 954 不是矛盾，是时间点不同**：
   `0.2.0` 落定**前**为 `955 passed`；落定**后**台账最高一级即当前版本，一致性用例进入
   **预期休眠** ⇒ `954 passed / 1 skipped`；`P-3` 落地（`758bad7`）**新增 2 条行为级用例**
   ⇒ 当前为 **`956 passed / 1 skipped`**。那个 skip **不是缺陷**
   （`docs/devlog/0020` §3.6 有逐字说明）。
2. **安全层零用例视为失败**：`make test-security` 的 `96` 是**门禁刻意设计**的 fail-secure 约束
   ——安全层若一条都没收集到，判失败而不是判通过。
3. **安全层的 deselected 数会随用例增长**（`869 → 871`）：它只是"本次未选中"的计数，
   **不是**通过/失败，**也不**表示用例被删。

> **独立复核**（`E-2`）：上述两个数字由 2026-09-21 的**独立复核报告**在同宿主各自复跑得到
> `955 passed / 8.29s` 与 `96 passed / 1.25s`（`docs/research/2026-09-21-handbook-reproducibility-review.md:88,100,313-314`），
> **通过数一致**，墙钟差属环境噪声。

---

## 6. 端到端主流程真的跑通了（独立复跑）

| 项 | 内容 |
| --- | --- |
| 级别 | **E-2**（报告原文含原始命令与原始输出）+ **E-3**（真模型会话需本地推理服务） |
| 依据文件 | `docs/research/2026-09-20-m2-exit-criteria-evidence.md`（提交 `df8d245` 的机器载体 + 独立实跑） |

**原文摘录**（`docs/research/2026-09-20-m2-exit-criteria-evidence.md`）：

```text
- 墙钟耗时：`184` 秒（起止 `date +%s` 差值）。                                        （:77）
- 退出码：0（pytest 汇总 `10 passed`）                                                （:253-254）
- passed / skipped：10 passed，0 skipped（在 AGENT_SEC_PERF_E2E=1 下集成用例全部启用）  （:255）
- 耗时：466.55 s（墙钟 468 s；与 df8d245 自报的 472.06 s 属同一量级、在单次运行方差内）  （:257）
```

**第二次独立实跑（不同日、不同用途，可交叉对照）**：
`docs/research/2026-09-21-handbook-reproducibility-review.md` 逐字照做手册的演示舞台，
得到 `EXIT=0 / ELAPSED=194s / 10 行 JSONL / 2 次工具调用均 ok`（`:153,190`）。

> ⚠️ **184 s 与 194 s 是两次独立运行，不是同一数字的两个版本**：前者是 `M2` 出口判据的实跑，
> 后者是手册可复跑性复核的实跑。`01` 引用的是前者（184 s），**两者都属单次采样**
> ⇒ 按本项目纪律**不得用于阈值判断**（`01` §5.2 的 `V-3`）。

---

## 7. ✅ 19 条需求 → 验证载体 → 提交哈希对照表

> 这是 `03` 的 `C-02`（"✅ 的 19 条是否每条都能在 30 秒内指出验证载体"）的答案。
> 下表**只列有可执行载体的那批**（完整 49 条的现状分布见 `01` §5.3）。
> 级别：**E-2**（`git log -1 <hash>` 可核每一笔确实存在）。

| 需求 | 验证载体 | 提交哈希 | 日期 |
| --- | --- | --- | --- |
| `REQ-SEC-06`（审计可回放） | `tests/security/test_s1_replayable_audit_link.py` | `05cb0af` | 2026-09-19 |
| `REQ-SEC-01/02`（默认拒绝、越权不执行） | `tests/security/test_harness_s1_authorization.py` | `e9e780c` | 2026-09-19 |
| `REQ-SEC-03`（路径越界拒绝） | `tests/security/test_path_traversal_rejected.py`（18 例） | `f436b0b` | 2026-09-18 |
| `REQ-SEC-03`（唯一路径校验入口接缝） | `tests/security/test_path_validation_seam.py` | `f436b0b` | 2026-09-18 |
| `REQ-SEC-03`（拒绝留痕·工具层） | `tests/security/test_t02_path_traversal_audit.py` | `4a35c61` | 2026-09-19 |
| `REQ-SEC-06`（审计落点白名单 `W1`~`W8`） | `tests/security/test_audit_landing_whitelist.py` | `91ea9d5`（`W3` 于 `9b37e99`） | 2026-09-19 |
| `REQ-SEC-06`（领域包不得含代码） | `tests/security/test_harness_s3_domain_pack.py` | `9a5689c` | 2026-09-19 |
| `REQ-SEC-01`（资源上限 `RLIMIT_AS`） | `tests/security/test_rlimit_isolation.py` | `5fddcfa` | 2026-09-18 |
| `REQ-SEC-09`（凭据不进子进程） | `tests/security/test_spawn_credentials_canary.py` / `test_t08_independent_spawn_env.py` | `5fddcfa` / `8da9ad4` | 2026-09-18 |
| `REQ-HARNESS-*` 端到端（多步 + 审计回放） | `tests/integration/test_end_to_end.py` + 独立实跑报告 | `df8d245` | 2026-09-20 |
| 分层与子进程封装（结构性机器检查） | `tests/unit/test_architecture_layers.py`、`test_bench_encapsulation.py` | 见 `docs/adr/0015` §9 | — |

> **复核方式**（E-1）：`git log -1 --format='%h %ad %s' --date=short <hash>`
> —— 上表每一笔都能核到"提交确实存在且日期相符"。

---

## 8. 本附录**不**证明什么（必须连读）

1. **不证明图表渲染正确**：28 张图只做了**结构自检**（`03` 的 `V-15`），语义一致性未逐张核对（`N-14`）。
2. **不证明跨机/换模型可复跑**：从未做过（`N-09`）；换模型必须重测 token 预算与超时。
3. **不证明用户画像与用户故事成立**：**未开展任何用户调研**，全部是设计假设（`N-13`）。
4. **不证明三个悬空指标能达标**：注入拦截率 ≥90%（`REQ-SEC-04`）、拒答正确率 ≥95%（`REQ-SEC-08`）、
   一致率 ≥80%（`REQ-MODEL-06`）**当前均无基线**，已显式降级为"待实测"，解除动作见 `01` §5.5。
5. **不证明"安全已到位"**：威胁模型 13 条里**只有 1 条**"已缓解并验证"，且是单条目、单调用面。

---

## 9. 一页纸复跑清单（给想自己验的读者）

| # | 命令 | 期望 | 对应本附录 |
| --- | --- | --- | --- |
| 1 | `make check` | `956 passed / 1 skipped`，bandit/密钥/pip-audit 全过 | §5 |
| 2 | `make test-security` | `96 passed`；**零用例视为失败** | §5 |
| 3 | `grep -n "已缓解并验证" docs/design/threat-model/README.md` | 分布 `1 / 10 / 2` | §4 |
| 4 | `git log -1 --format='%h %ad %s' --date=short 05cb0af` | 提交存在、日期 2026-09-19 | §7 |
| 5 | `ls tests/security/corpus/` | **只有** `traversal_payloads.txt` ⇒ 注入语料集确实不存在 | §8-4 |
| 6 | 打开 `docs/adr/0006-sandbox-execution-degradation.md:93` | `HTTP 200` ⇒ fail-open 实测 | §2 |
