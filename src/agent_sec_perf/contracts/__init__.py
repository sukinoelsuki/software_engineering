"""零行为契约层：纯类型与 Protocol。

打破"安全层需要工具形状、工具层需要安全层求权限"的循环依赖：把共享的**形状**
下沉到这里，两边都只依赖契约而不互相依赖（ADR-0015 §1、§5.4.1）。

硬约束（ADR-0015 §7.1 R3）：

* 本层**只允许依赖标准库**，不得依赖任何第三方包；
* 不得依赖 ``contracts`` 之外的本项目任何模块（子模块之间可以互相引用）；
* **零行为**：只有类型、Protocol 与常量，不含任何逻辑。
"""

from __future__ import annotations
